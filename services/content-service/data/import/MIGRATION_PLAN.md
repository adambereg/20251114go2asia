# План миграции данных из старой БД

**Дата создания:** 2025-11-14  
**Статус:** В процессе подготовки

---

## 📋 Обзор

Цель: Импортировать данные из предыдущей итерации проекта в новую БД с обновлённой схемой.

---

## 🔍 Анализ различий схем

### Countries (страны)

#### Текущая схема:
- `id` (text, PK)
- `name` (text)
- `code` (varchar(3))
- `flag` (text)
- `description` (text)
- `created_at` (timestamp)
- `updated_at` (timestamp)

#### Старая схема (из скриншотов):
- `id` (text, PK)
- `name` (text)
- `code` (varchar(3))
- `flag` (text)
- `description` (text)
- `capital` (text) ⚠️ **Отсутствует в новой схеме**
- `population` (text) ⚠️ **Отсутствует в новой схеме**
- `currency` (text) ⚠️ **Отсутствует в новой схеме**
- `created_at` (timestamp)
- `updated_at` (timestamp)

**Рекомендация:** Добавить поля `capital`, `population`, `currency` в миграцию, так как они полезны для отображения информации о странах.

---

### Cities (города)

#### Текущая схема:
- `id` (text, PK)
- `name` (text)
- `country_id` (text, FK → countries.id)
- `description` (text)
- `latitude` (text)
- `longitude` (text)
- `created_at` (timestamp)
- `updated_at` (timestamp)

#### Старая схема (из скриншотов):
- `id` (text, PK)
- `name` (text)
- `country_id` (text, FK → countries.id)
- `coordinates` (jsonb) ⚠️ **Нужно преобразовать в latitude/longitude**
- `image` (text) ⚠️ **Отсутствует в новой схеме**
- `description` (text)
- `created_at` (timestamp)
- `updated_at` (timestamp)

**Рекомендация:** 
- Преобразовать `coordinates` JSONB в отдельные поля `latitude` и `longitude`
- Рассмотреть добавление поля `image` для обложки города

---

### Places (места)

#### Текущая схема:
- `id` (text, PK)
- `name` (text)
- `city_id` (text, FK → cities.id)
- `description` (text)
- `latitude` (text, NOT NULL)
- `longitude` (text, NOT NULL)
- `type` (varchar(50))
- `categories` (text[])
- `photos` (text[])
- `created_at` (timestamp)
- `updated_at` (timestamp)

#### Старая схема (из скриншотов):
- `id` (text, PK)
- `name` (text)
- `city_id` (text, FK → cities.id)
- `coordinates` (jsonb) ⚠️ **Нужно преобразовать в latitude/longitude**
- `address` (text) ⚠️ **Отсутствует в новой схеме**
- `description` (text)
- `category` (varchar(58)) ⚠️ **В новой схеме: type + categories[]**
- `tags` (jsonb) ⚠️ **В новой схеме: categories[]**
- `images` (jsonb) ⚠️ **В новой схеме: photos[]**
- `rating` (integer) ⚠️ **Отсутствует в новой схеме**
- `created_at` (timestamp)
- `updated_at` (timestamp)

**Рекомендация:**
- Преобразовать `coordinates` JSONB в `latitude` и `longitude`
- Преобразовать `category` в `type`
- Преобразовать `tags` в `categories[]`
- Преобразовать `images` в `photos[]`
- Рассмотреть добавление полей `address` и `rating`

---

### Events (события)

#### Текущая схема:
- `id` (text, PK)
- `title` (text)
- `description` (text)
- `start_time` (timestamp)
- `end_time` (timestamp)
- `city_id` (text, FK → cities.id)
- `place_id` (text, FK → places.id, nullable)
- `type` (varchar(50))
- `category` (varchar(50))
- `organizer_id` (text)
- `created_at` (timestamp)
- `updated_at` (timestamp)

#### Старая схема (из скриншотов):
- `id` (text, PK)
- `text` (text) ⚠️ **В новой схеме: title**
- `title` (text) ⚠️ **Дублируется с text?**
- `description` (text)
- `type` (varchar(58))
- `start_date` (timestamp) ⚠️ **В новой схеме: start_time**
- `end_date` (timestamp) ⚠️ **В новой схеме: end_time**
- `coordinates` (jsonb) ⚠️ **Отсутствует в новой схеме (используется city_id/place_id)**
- `address` (text) ⚠️ **Отсутствует в новой схеме**
- `organizer` (text) ⚠️ **В новой схеме: organizer_id**
- `contact` (jsonb) ⚠️ **Отсутствует в новой схеме**
- `created_at` (timestamp)
- `updated_at` (timestamp)

**Рекомендация:**
- Использовать `title` из старой схемы
- Преобразовать `start_date` → `start_time`, `end_date` → `end_time`
- Преобразовать `organizer` → `organizer_id` (может потребоваться создание таблицы organizers)
- Рассмотреть добавление полей `address` и `contact`

---

### Articles (статьи)

#### Текущая схема:
- `id` (text, PK)
- `slug` (varchar(255), unique)
- `title` (text)
- `excerpt` (text)
- `content` (text)
- `author_id` (text)
- `category` (varchar(50))
- `tags` (text[])
- `cover_image` (text)
- `published_at` (timestamp, nullable)
- `created_at` (timestamp)
- `updated_at` (timestamp)

#### Старая схема (из скриншотов):
- `id` (text, PK)
- `slug` (varchar(255), unique)
- `title` (text)
- `content` (text)
- `excerpt` (text)
- `category` (varchar(50))
- `author_id` (text)
- `images` (jsonb) ⚠️ **В новой схеме: cover_image (одно изображение)**
- `tags` (jsonb) ⚠️ **В новой схеме: tags[]**
- `published_at` (timestamp)

**Рекомендация:**
- Использовать первое изображение из `images[]` как `cover_image`
- Преобразовать `tags` JSONB в `tags[]` массив

---

### Media Files (медиа файлы)

#### Использование:
- Источник URL для маппинга изображений
- Структура: `id`, `filename`, `url`, `type`, `mime_type`, `size`, `width`

#### Маппинг:
- **Countries:** Использовать media_files для обложек стран (если будет добавлено поле `image`)
- **Cities:** Использовать media_files для обложек городов (если будет добавлено поле `image`)
- **Places:** Использовать media_files для `photos[]` массива
- **Articles:** Использовать media_files для `cover_image` (первое изображение из `images[]`)

---

## 📝 План действий

### Шаг 1: Анализ CSV файлов
- [ ] Проверить структуру CSV файлов
- [ ] Сопоставить колонки со старой и новой схемой
- [ ] Выявить все различия

### Шаг 2: Создание миграций для недостающих полей
- [ ] Добавить `capital`, `population`, `currency` в `countries`
- [ ] Добавить `image` в `cities` (опционально)
- [ ] Добавить `address`, `rating` в `places` (опционально)
- [ ] Добавить `address`, `contact` в `events` (опционально)

### Шаг 3: Создание seed-скриптов
- [ ] Скрипт для импорта `countries`
- [ ] Скрипт для импорта `cities` (с преобразованием coordinates)
- [ ] Скрипт для импорта `places` (с преобразованием данных)
- [ ] Скрипт для импорта `events` (с преобразованием данных)
- [ ] Скрипт для импорта `articles` (с маппингом изображений)

### Шаг 4: Маппинг изображений
- [ ] Создать утилиту для маппинга media_files
- [ ] Привязать изображения к countries (если добавлено поле)
- [ ] Привязать изображения к cities (если добавлено поле)
- [ ] Привязать изображения к places (photos[])
- [ ] Привязать изображения к articles (cover_image)

### Шаг 5: Тестирование
- [ ] Проверить импорт на staging БД
- [ ] Проверить целостность данных
- [ ] Проверить foreign keys
- [ ] Проверить маппинг изображений

---

## 🔧 Технические детали

### Преобразование данных

#### Coordinates JSONB → latitude/longitude
```typescript
// Старый формат: { "lat": 13.4125, "lng": 123.456 }
// Новый формат: latitude = "13.4125", longitude = "123.456"
```

#### Tags JSONB → tags[]
```typescript
// Старый формат: ["tag1", "tag2"]
// Новый формат: ["tag1", "tag2"] (то же самое, но как text[])
```

#### Images JSONB → cover_image или photos[]
```typescript
// Старый формат: ["url1", "url2", "url3"]
// Новый формат для articles: cover_image = "url1"
// Новый формат для places: photos = ["url1", "url2", "url3"]
```

---

## 📊 Статистика данных (из скриншотов)

- **Countries:** 9 записей
- **Cities:** 12 записей
- **Places:** 18 записей
- **Events:** 8 записей
- **Articles:** 5 записей
- **Media Files:** 12+ записей

---

## ⚠️ Важные замечания

1. **Кодировка:** CSV файлы должны быть в UTF-8
2. **Даты:** Проверить формат дат в CSV (может потребоваться преобразование)
3. **NULL значения:** Обработать пустые значения корректно
4. **Foreign Keys:** Убедиться, что все ссылки на другие таблицы существуют
5. **Уникальность:** Проверить уникальность `slug` для articles, `code` для countries

---

**Дата создания:** 2025-11-14  
**Версия:** 1.0

