# Content Service API Documentation

## Обзор

Content Service предоставляет единый API для работы с контентом модулей Atlas, Pulse и Blog.

## Базовый URL

```
https://api.go2asia.space/v1
```

## Общие параметры

### Пагинация

Все endpoints списков поддерживают cursor-based пагинацию:

- `limit` (опционально, по умолчанию: 20, максимум: 100) - количество элементов на странице
- `cursor` (опционально) - курсор для получения следующей страницы

### Сортировка

Поддерживаются параметры:
- `sortBy` - поле для сортировки (зависит от endpoint)
- `sortOrder` - порядок сортировки: `asc` или `desc` (по умолчанию зависит от endpoint)

### Поиск

- `search` (опционально) - поиск по названию и описанию (case-insensitive)

### Фильтры

Зависят от конкретного endpoint.

## Endpoints

### Countries (Страны)

#### GET /v1/countries

Получить список стран.

**Query параметры:**
- `search` (опционально) - поиск по названию, английскому названию или описанию
- `sortBy` (опционально) - поле сортировки: `id`, `name`, `createdAt`, `citiesCount`, `placesCount` (по умолчанию: `name`)
- `sortOrder` (опционально) - порядок: `asc` или `desc` (по умолчанию: `asc`)
- `limit` (опционально) - количество элементов (по умолчанию: 20)
- `cursor` (опционально) - курсор для пагинации

**Пример запроса:**
```bash
GET /v1/countries?search=тайланд&sortBy=name&sortOrder=asc&limit=10
```

**Пример ответа:**
```json
{
  "items": [
    {
      "id": "thailand",
      "name": "Таиланд",
      "code": "TH",
      "flag": "🇹🇭",
      "description": "Королевство Таиланд",
      "citiesCount": 15,
      "placesCount": 245,
      "createdAt": "2024-01-01T00:00:00Z",
      "updatedAt": "2024-01-01T00:00:00Z"
    }
  ],
  "hasMore": false,
  "nextCursor": null
}
```

#### GET /v1/countries/:id

Получить страну по ID.

**Пример запроса:**
```bash
GET /v1/countries/thailand
```

---

### Cities (Города)

#### GET /v1/cities

Получить список городов.

**Query параметры:**
- `countryId` (опционально) - фильтр по стране
- `search` (опционально) - поиск по названию или описанию
- `sortBy` (опционально) - поле сортировки: `id`, `name`, `createdAt`, `placesCount` (по умолчанию: `name`)
- `sortOrder` (опционально) - порядок: `asc` или `desc` (по умолчанию: `asc`)
- `limit` (опционально) - количество элементов (по умолчанию: 20)
- `cursor` (опционально) - курсор для пагинации

**Пример запроса:**
```bash
GET /v1/cities?countryId=thailand&search=бангкок&sortBy=name
```

---

### Places (Места)

#### GET /v1/places

Получить список мест.

**Query параметры:**
- `cityId` (опционально) - фильтр по городу
- `types` (опционально) - фильтр по типам (можно указать несколько через запятую: `type1,type2`)
- `search` (опционально) - поиск по названию, описанию или адресу
- `sortBy` (опционально) - поле сортировки: `id`, `name`, `rating`, `createdAt` (по умолчанию: `name`)
- `sortOrder` (опционально) - порядок: `asc` или `desc` (по умолчанию: `asc`)
- `limit` (опционально) - количество элементов (по умолчанию: 20)
- `cursor` (опционально) - курсор для пагинации

**Пример запроса:**
```bash
GET /v1/places?cityId=bangkok&types=restaurant,cafe&sortBy=rating&sortOrder=desc
```

---

### Events (События)

#### GET /v1/events

Получить список событий.

**Query параметры:**
- `cityId` (опционально) - фильтр по городу
- `date` (опционально) - фильтр по дате: `today`, `tomorrow`, `week`
- `category` (опционально) - фильтр по категории
- `type` (опционально) - фильтр по типу
- `search` (опционально) - поиск по названию или описанию
- `sortBy` (опционально) - поле сортировки: `startTime`, `title`, `createdAt` (по умолчанию: `startTime`)
- `sortOrder` (опционально) - порядок: `asc` или `desc` (по умолчанию: `asc` - ближайшие сначала)
- `limit` (опционально) - количество элементов (по умолчанию: 20)
- `cursor` (опционально) - курсор для пагинации

**Пример запроса:**
```bash
GET /v1/events?cityId=bangkok&date=today&sortBy=startTime&sortOrder=asc
```

---

### Articles (Статьи)

#### GET /v1/articles

Получить список статей.

**Query параметры:**
- `category` (опционально) - фильтр по категории
- `tags` (опционально) - фильтр по тегам (можно указать несколько через запятую: `tag1,tag2`)
- `featured` (опционально) - фильтр по featured статьям: `true` или `false`
- `search` (опционально) - поиск по названию, excerpt или содержимому
- `sortBy` (опционально) - поле сортировки: `publishedAt`, `title`, `views`, `createdAt` (по умолчанию: `publishedAt`)
- `sortOrder` (опционально) - порядок: `asc` или `desc` (по умолчанию: `desc` - новые сначала)
- `limit` (опционально) - количество элементов (по умолчанию: 20)
- `cursor` (опционально) - курсор для пагинации

**Пример запроса:**
```bash
GET /v1/articles?category=travel&tags=thailand,beach&featured=true&sortBy=publishedAt&sortOrder=desc
```

#### GET /v1/articles/:slug

Получить статью по slug.

**Пример запроса:**
```bash
GET /v1/articles/top-10-beaches-in-thailand
```

---

## Обработка ошибок

Все ошибки возвращаются в едином формате:

```json
{
  "error": {
    "code": "ERROR_CODE",
    "message": "Human-readable error message",
    "details": [
      {
        "path": "field.path",
        "message": "Validation error message"
      }
    ],
    "traceId": "request-id-for-debugging"
  }
}
```

### Коды ошибок

- `VALIDATION_ERROR` (400) - Неверные параметры запроса
- `NOT_FOUND` (404) - Ресурс не найден
- `INTERNAL_ERROR` (500) - Внутренняя ошибка сервера

---

## Кэширование

Все endpoints устанавливают заголовки кэширования:
- `Cache-Control: public, s-maxage=600, stale-while-revalidate=600` (для публичных данных)
- `Vary: Accept, Accept-Encoding`

---

## Примеры использования

### Поиск стран по названию
```bash
curl "https://api.go2asia.space/v1/countries?search=тайланд"
```

### Получить события на сегодня в Бангкоке
```bash
curl "https://api.go2asia.space/v1/events?cityId=bangkok&date=today&sortBy=startTime&sortOrder=asc"
```

### Получить популярные статьи (по просмотрам)
```bash
curl "https://api.go2asia.space/v1/articles?sortBy=views&sortOrder=desc&limit=10"
```

### Получить места с высоким рейтингом
```bash
curl "https://api.go2asia.space/v1/places?sortBy=rating&sortOrder=desc&limit=20"
```


