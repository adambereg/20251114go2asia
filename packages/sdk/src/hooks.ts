/**
 * React hooks для работы с API
 * Эти hooks будут автоматически сгенерированы Orval из OpenAPI спецификаций
 * 
 * Пример использования:
 * 
 * ```tsx
 * import { useGetBalance } from '@go2asia/sdk';
 * 
 * function BalanceWidget() {
 *   const { data, isLoading, error } = useGetBalance();
 *   
 *   if (isLoading) return <div>Loading...</div>;
 *   if (error) return <div>Error: {error.message}</div>;
 *   
 *   return <div>Balance: {data?.points} points</div>;
 * }
 * ```
 */

// Hooks будут автоматически сгенерированы через `pnpm gen:sdk`
// Этот файл служит документацией и местом для кастомных hooks

export {};

