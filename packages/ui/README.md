# @go2asia/ui

> **Компонентная библиотека Go2Asia — источник правды для всех UI компонентов**

Компонентная библиотека Go2Asia на основе [Style Guide v1.1](../../docs/overview/go2asia_uiux_style_guide.md).

## 🎯 Назначение

Эта библиотека является **единственным источником правды** для всех UI компонентов в экосистеме Go2Asia. Все компоненты:

- ✅ Строго следуют Style Guide v1.1
- ✅ Используют единые дизайн-токены
- ✅ Документированы в Storybook
- ✅ Типобезопасны (TypeScript)
- ✅ Доступны (a11y)

## 📦 Установка

```bash
pnpm add @go2asia/ui
```

## 🚀 Использование

```tsx
import { Badge, Button, Card, Chip, EmptyState, Skeleton } from '@go2asia/ui';

// Badge
<Badge variant="ugc">UGC</Badge>
<Badge variant="verified" icon={<CheckCircle2 size={12} />}>
  Проверено
</Badge>

// Button
<Button variant="primary">Начать</Button>
<Button variant="secondary" size="sm">Отмена</Button>

// Card
<Card>
  <CardHeader>
    <h3>Заголовок</h3>
  </CardHeader>
  <CardContent>
    Контент карточки
  </CardContent>
</Card>

// Chip
<Chip selected={isActive} onClick={handleClick}>
  Фильтр
</Chip>

// EmptyState
<EmptyState
  icon={<MessageSquare size={32} />}
  title="Пока нет постов"
  description="Станьте первым, кто поделится опытом"
  action={{ label: 'Создать пост', onClick: createPost }}
/>

// Skeleton
<Skeleton className="h-4 w-full" />
<SkeletonCard />
```

## 📚 Компоненты

### Badge
Бейджи для отображения статусов и меток.

**Варианты:** `ugc`, `verified`, `editor`, `russian-friendly`, `new`, `popular`, `prices`, `dates`, `photos`, `info`

### Button
Кнопки для действий пользователя.

**Варианты:** `primary`, `secondary`, `ghost`, `danger`  
**Размеры:** `sm`, `md`, `lg`

### Card
Универсальные карточки для группировки контента.

**Составные части:** `CardHeader`, `CardContent`, `CardFooter`

### Chip
Чипы для фильтров, тегов и выбора опций.

**Размеры:** `sm`, `md`  
**Состояния:** `selected`, с `onRemove`

### EmptyState
Пустые состояния для отсутствия данных.

Поддерживает иконку, заголовок, описание и действие.

### Skeleton
Скелетоны для состояний загрузки.

**Варианты:** `text`, `circular`, `rectangular`  
**Готовые:** `SkeletonCard`

## 📖 Storybook

Все компоненты документированы в Storybook:

```bash
cd packages/ui
pnpm storybook
```

Storybook доступен по адресу: `http://localhost:6006`

### Структура Storybook

- **Design System/** — все компоненты библиотеки
- **Examples/** — примеры использования в контексте модулей

## 🎨 Style Guide

Все компоненты строго следуют [Style Guide v1.1](../../docs/overview/go2asia_uiux_style_guide.md):

- ✅ Дизайн-токены (colors, typography, spacing)
- ✅ Состояния (hover, focus, disabled)
- ✅ Адаптивность (mobile-first)
- ✅ Доступность (a11y)

## 🔒 Правила использования

### ✅ Можно

- Использовать компоненты из `@go2asia/ui` во всех модулях
- Расширять компоненты через `className` prop
- Создавать композиции из базовых компонентов

### ❌ Нельзя

- Создавать локальные копии компонентов
- Переопределять дизайн-токены без обоснования
- Использовать компоненты, не соответствующие Style Guide

## 🧪 Тестирование

```bash
pnpm test
```

## 📝 Добавление нового компонента

1. Создать компонент в `src/components/[ComponentName]/`
2. Экспортировать из `src/index.ts`
3. Создать Story в `src/components/[ComponentName]/[ComponentName].stories.tsx`
4. Обновить документацию

## 🔗 Связанные документы

- [Style Guide v1.1](../../docs/overview/go2asia_uiux_style_guide.md)
- [Frontend Vision](../../docs/frontend/FRONTEND_VISION.md)
- [Implementation Status](../../docs/frontend/IMPLEMENTATION_STATUS.md)

---

**Версия:** 1.0  
**Статус:** Активная разработка  
**Источник правды:** Style Guide v1.1
