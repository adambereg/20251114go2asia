# 🔐 План интеграции авторизации и auth-dependent функционала

> **Документ описывает поэтапный план интеграции Clerk и auth-dependent endpoints в фронтенд Go2Asia.**

**Дата создания:** 2024-11-15  
**Статус:** План действий

---

## 📊 Текущее состояние

### ✅ Что готово:
- **UI компонентная библиотека:** Badge, Button, Card, Chip, EmptyState, Skeleton
- **App Shell:** TopAppBar, BottomNav, SideDrawer
- **Публичные модули:** Atlas, Pulse, Blog (базовые страницы)
- **Бэкенд готов:** Auth Service, Token Service, Referral Service работают
- **Документация:** AUTH_FLOW_CLERK.md, API_MOCKING_CONTRACTS.md

### ❌ Что отсутствует:
- **UI компоненты:** Toast, UserMenu, GuestPrompt, Form components (Input, Textarea, Select)
- **Интеграция Clerk:** ClerkProvider, middleware, защита роутов
- **Auth-dependent endpoints:** интеграция с `/v1/profile`, `/v1/balance`, `/v1/referrals`
- **Личные кабинеты:** страницы профиля, баланса, рефералов
- **Валидация форм:** Zod схемы и компоненты

---

## 🎯 Рекомендуемый порядок действий

### **Вариант 1: Поэтапный подход (РЕКОМЕНДУЕТСЯ)**

**Преимущества:**
- ✅ Каждый этап завершён и протестирован
- ✅ Можно деплоить постепенно
- ✅ Меньше риска ошибок
- ✅ Легче отлаживать

**Недостатки:**
- ⏱️ Занимает больше времени
- 🔄 Требует больше коммитов

---

## 📋 Фаза 1: Завершение критичных UI компонентов (1-2 дня)

### Цель: Подготовить UI компоненты, необходимые для авторизации и форм

#### 1.1 Toast Notifications
**Зачем:** Уведомления об успехе/ошибке при авторизации и операциях

**Компоненты:**
- `Toast` — базовый компонент уведомления
- `ToastContainer` — контейнер для отображения тостов
- `useToast` — хук для управления тостами

**Требования:**
- Поддержка типов: `success`, `error`, `warning`, `info`
- Автоматическое скрытие через 3-5 секунд
- Анимации появления/исчезновения
- Позиционирование (top-right, bottom-right)
- Соответствие Style Guide (цвета, типографика)

**Файлы:**
```
packages/ui/src/components/Toast/
├── Toast.tsx
├── ToastContainer.tsx
├── Toast.stories.tsx
└── index.ts
```

#### 1.2 UserMenu компонент
**Зачем:** Отображение профиля пользователя в TopAppBar

**Компоненты:**
- `UserMenu` — выпадающее меню с профилем
- `UserAvatar` — аватар пользователя
- `UserMenuItem` — элемент меню

**Требования:**
- Аватар с fallback на инициалы
- Меню: Профиль, Настройки, Выйти
- Badge для уровня пользователя (Spacer, VIP, PRO)
- Адаптивность (mobile/desktop)
- Соответствие Style Guide

**Файлы:**
```
packages/ui/src/components/UserMenu/
├── UserMenu.tsx
├── UserAvatar.tsx
├── UserMenu.stories.tsx
└── index.ts
```

#### 1.3 GuestPrompt компонент
**Зачем:** Приглашение войти для гостевого режима

**Компоненты:**
- `GuestPrompt` — компонент с приглашением войти
- `GuestGate` — HOC для защиты контента

**Требования:**
- Иконка, заголовок, описание действия
- Кнопка "Войти" (откроет Clerk модал)
- Варианты: inline, card, modal
- Соответствие Style Guide

**Файлы:**
```
packages/ui/src/components/GuestPrompt/
├── GuestPrompt.tsx
├── GuestGate.tsx
├── GuestPrompt.stories.tsx
└── index.ts
```

#### 1.4 Form Components
**Зачем:** Компоненты для форм (профиль, настройки, админка)

**Компоненты:**
- `Input` — текстовое поле
- `Textarea` — многострочное поле
- `Select` — выпадающий список
- `Checkbox` — чекбокс
- `Radio` — радиокнопка
- `Label` — метка поля
- `FormField` — обёртка для поля с ошибкой

**Требования:**
- Состояния: default, focus, error, disabled
- Валидация через Zod (опционально)
- Соответствие Style Guide (цвета, радиусы, тени)
- Доступность (ARIA атрибуты)

**Файлы:**
```
packages/ui/src/components/Form/
├── Input.tsx
├── Textarea.tsx
├── Select.tsx
├── Checkbox.tsx
├── Radio.tsx
├── Label.tsx
├── FormField.tsx
├── Form.stories.tsx
└── index.ts
```

#### 1.5 Валидация форм (Zod)
**Зачем:** Типобезопасная валидация форм

**Установка:**
```bash
cd packages/ui
pnpm add zod
```

**Создать:**
- `packages/ui/src/utils/validation.ts` — утилиты для валидации
- Примеры схем для профиля, настроек

**Критерии готовности Фазы 1:**
- ✅ Все компоненты созданы и экспортированы
- ✅ Storybook stories написаны для всех компонентов
- ✅ Компоненты соответствуют Style Guide
- ✅ Компоненты протестированы визуально
- ✅ TypeScript типы корректны

---

## 📋 Фаза 2: Интеграция Clerk (1-2 дня)

### Цель: Настроить Clerk SSO для единой авторизации во всех модулях

#### 2.1 Установка и настройка Clerk
**Шаги:**
1. Установить `@clerk/nextjs` (уже установлен в `package.json`)
2. Получить ключи из Clerk Dashboard
3. Добавить environment variables в `.env.local`
4. Обновить `netlify.toml` для production переменных

**Файлы:**
```
apps/go2asia-pwa-shell/.env.local
apps/go2asia-pwa-shell/.env.example
```

#### 2.2 ClerkProvider в App Shell
**Зачем:** Обёртка для всего приложения с Clerk контекстом

**Изменения:**
- `apps/go2asia-pwa-shell/app/layout.tsx` — добавить `ClerkProvider`
- Настроить `publishableKey` из env
- Настроить `signInUrl`, `signUpUrl`, `afterSignInUrl`, `afterSignUpUrl`

**Код:**
```typescript
import { ClerkProvider } from '@clerk/nextjs';

export default function RootLayout({ children }) {
  return (
    <ClerkProvider
      publishableKey={process.env.NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY}
      signInUrl="/auth/sign-in"
      signUpUrl="/auth/sign-up"
      afterSignInUrl="/"
      afterSignUpUrl="/"
    >
      {children}
    </ClerkProvider>
  );
}
```

#### 2.3 Middleware для защиты роутов
**Зачем:** Автоматическая проверка авторизации для защищённых роутов

**Создать:**
- `apps/go2asia-pwa-shell/middleware.ts`

**Роуты:**
- **Публичные:** `/`, `/atlas(.*)`, `/pulse(.*)`, `/blog(.*)`, `/guru(.*)`, `/rf(.*)`, `/rielt(.*)`
- **Защищённые:** `/space/me`, `/connect`, `/profile`, `/quest/my-quests`
- **Admin:** `/admin(.*)` — только для роли `admin`
- **PRO:** `/quest/pro`, `/rf/pro` — только для роли `pro` или `admin`

**Код:**
```typescript
import { clerkMiddleware, createRouteMatcher } from '@clerk/nextjs/server';

const isPublicRoute = createRouteMatcher([
  '/',
  '/atlas(.*)',
  '/pulse(.*)',
  '/blog(.*)',
  // ...
]);

const isAdminRoute = createRouteMatcher(['/admin(.*)']);

export default clerkMiddleware(async (auth, req) => {
  if (!isPublicRoute(req)) {
    await auth.protect();
  }

  if (isAdminRoute(req)) {
    const { sessionClaims } = await auth();
    const role = sessionClaims?.metadata?.role;
    if (role !== 'admin') {
      return NextResponse.redirect('/');
    }
  }
});

export const config = {
  matcher: ['/((?!.+\\.[\\w]+$|_next).*)', '/', '/(api|trpc)(.*)'],
};
```

#### 2.4 Auth Pages (Clerk)
**Зачем:** Страницы входа и регистрации через Clerk

**Создать:**
- `apps/go2asia-pwa-shell/app/(auth)/sign-in/[[...sign-in]]/page.tsx`
- `apps/go2asia-pwa-shell/app/(auth)/sign-up/[[...sign-up]]/page.tsx`

**Код:**
```typescript
import { SignIn } from '@clerk/nextjs';

export default function SignInPage() {
  return (
    <div className="flex min-h-screen items-center justify-center">
      <SignIn />
    </div>
  );
}
```

#### 2.5 Интеграция UserMenu в TopAppBar
**Зачем:** Отображение профиля пользователя в шапке

**Изменения:**
- `apps/go2asia-pwa-shell/components/app-shell/TopAppBar.tsx`
- Использовать `useUser()` из Clerk
- Показывать `UserMenu` для авторизованных, `SignInButton` для гостей

#### 2.6 Интеграция с SDK
**Зачем:** Настройка SDK для автоматической передачи JWT в API запросы

**Изменения:**
- `packages/sdk/src/clerk-integration.ts` — функция `setupClerkAuth()`
- Обновить `packages/sdk/src/client.ts` — добавить JWT в заголовки

**Код:**
```typescript
// packages/sdk/src/clerk-integration.ts
import { getAuth } from '@clerk/nextjs/server';

export function setupClerkAuth() {
  // SDK автоматически будет использовать getToken() из Clerk
}

// packages/sdk/src/client.ts
import { getAuth } from '@clerk/nextjs/server';

export async function apiRequest(url: string, options?: RequestInit) {
  const { getToken } = getAuth();
  const token = await getToken();
  
  return fetch(url, {
    ...options,
    headers: {
      ...options?.headers,
      Authorization: token ? `Bearer ${token}` : undefined,
    },
  });
}
```

**Критерии готовности Фазы 2:**
- ✅ ClerkProvider настроен и работает
- ✅ Middleware защищает роуты корректно
- ✅ Страницы входа/регистрации работают
- ✅ UserMenu отображается для авторизованных пользователей
- ✅ SDK передаёт JWT в API запросы
- ✅ Тестирование: вход, выход, защита роутов

---

## 📋 Фаза 3: Auth-dependent endpoints (2-3 дня)

### Цель: Интегрировать API endpoints, требующие авторизации

#### 3.1 Profile API (`/v1/profile`)
**Endpoints:**
- `GET /v1/profile` — получить профиль пользователя
- `PUT /v1/profile` — обновить профиль

**Реализация:**
- `apps/go2asia-pwa-shell/app/profile/page.tsx` — страница профиля
- `apps/go2asia-pwa-shell/app/profile/edit/page.tsx` — редактирование профиля
- Использовать SDK hooks для запросов
- Обработка loading/error состояний
- Toast уведомления при успехе/ошибке

**Компоненты:**
- Форма редактирования профиля (Input, Textarea, Select)
- Валидация через Zod
- Оптимистичные обновления

#### 3.2 Balance API (`/v1/balance`, `/v1/transactions`)
**Endpoints:**
- `GET /v1/balance` — получить баланс токенов
- `GET /v1/transactions` — история транзакций

**Реализация:**
- `apps/go2asia-pwa-shell/app/connect/balance/page.tsx` — страница баланса
- Компонент `BalanceCard` для отображения баланса
- Компонент `TransactionsList` для истории
- Пагинация для транзакций

#### 3.3 Referrals API (`/v1/referrals/stats`, `/v1/referrals/tree`)
**Endpoints:**
- `GET /v1/referrals/stats` — статистика рефералов
- `GET /v1/referrals/tree` — дерево рефералов

**Реализация:**
- `apps/go2asia-pwa-shell/app/connect/referrals/page.tsx` — страница рефералов
- Компонент `ReferralStats` для статистики
- Компонент `ReferralTree` для дерева
- Компонент `ReferralCode` для отображения реферального кода

**Критерии готовности Фазы 3:**
- ✅ Все auth-dependent endpoints интегрированы
- ✅ Страницы профиля, баланса, рефералов работают
- ✅ Обработка ошибок и loading states реализована
- ✅ Toast уведомления работают
- ✅ Валидация форм работает
- ✅ Тестирование: все endpoints работают корректно

---

## 📋 Фаза 4: Личные кабинеты (3-4 дня)

### Цель: Создать полноценные личные кабинеты пользователей

#### 4.1 Профиль пользователя (`/profile`)
**Страницы:**
- `/profile` — просмотр профиля
- `/profile/edit` — редактирование профиля
- `/profile/settings` — настройки аккаунта

**Функционал:**
- Просмотр/редактирование: имя, фамилия, bio, аватар, локация
- Настройки: уведомления, приватность, язык
- Уровень пользователя (Spacer, VIP, PRO) с badge
- Статистика: количество постов, лайков, рефералов

#### 4.2 Connect модуль (`/connect`)
**Страницы:**
- `/connect` — главная страница модуля
- `/connect/balance` — баланс токенов
- `/connect/transactions` — история транзакций
- `/connect/referrals` — реферальная программа
- `/connect/network` — сеть контактов

**Функционал:**
- Отображение баланса Points и G2A токенов
- История начислений/списаний
- Реферальное дерево (1-й и 2-й уровень)
- Реферальный код для копирования
- Статистика по рефералам

#### 4.3 Space модуль (личные посты)
**Страницы:**
- `/space/me` — мои посты
- `/space/create` — создание поста

**Функционал:**
- Список моих постов с фильтрацией
- Создание/редактирование постов
- Загрузка медиа (drag & drop)
- Публикация/снятие с публикации

**Критерии готовности Фазы 4:**
- ✅ Все страницы личных кабинетов созданы
- ✅ Функционал работает корректно
- ✅ UI соответствует Style Guide
- ✅ Адаптивность для mobile/desktop
- ✅ Тестирование: все функции работают

---

## 📋 Фаза 5: Админ-панели (4-5 дней)

### Цель: Создать админ-панели для управления контентом и пользователями

#### 5.1 Admin Dashboard (`/admin`)
**Страницы:**
- `/admin` — главная панель
- `/admin/content` — управление контентом
- `/admin/users` — управление пользователями
- `/admin/moderation` — модерация
- `/admin/analytics` — аналитика

**Функционал:**
- Дашборд с метриками (DAU/MAU, популярность модулей)
- Управление странами, городами, местами
- Управление событиями и статьями
- Модерация постов Space
- Управление партнёрами RF
- Аналитика и отчёты

#### 5.2 PRO-панели
**Страницы:**
- `/quest/pro` — PRO-панель квестов
- `/rf/pro` — PRO-панель RF

**Функционал:**
- Создание/редактирование квестов
- Управление партнёрами RF
- Статистика по квестам/партнёрам

**Критерии готовности Фазы 5:**
- ✅ Все админ-панели созданы
- ✅ Функционал работает корректно
- ✅ Защита роутов работает (только для admin/PRO)
- ✅ UI соответствует Style Guide
- ✅ Тестирование: все функции работают

---

## 🎯 Итоговый план (рекомендуемый порядок)

### **Этап 1: UI компоненты (1-2 дня)** ⭐ ПРИОРИТЕТ 1
1. Toast Notifications
2. UserMenu компонент
3. GuestPrompt компонент
4. Form Components (Input, Textarea, Select, etc.)
5. Валидация форм (Zod)

### **Этап 2: Интеграция Clerk (1-2 дня)** ⭐ ПРИОРИТЕТ 2
1. Установка и настройка Clerk
2. ClerkProvider в App Shell
3. Middleware для защиты роутов
4. Auth Pages (sign-in, sign-up)
5. Интеграция UserMenu в TopAppBar
6. Интеграция с SDK

### **Этап 3: Auth-dependent endpoints (2-3 дня)** ⭐ ПРИОРИТЕТ 3
1. Profile API (`/v1/profile`)
2. Balance API (`/v1/balance`, `/v1/transactions`)
3. Referrals API (`/v1/referrals/stats`, `/v1/referrals/tree`)

### **Этап 4: Личные кабинеты (3-4 дня)** ⭐ ПРИОРИТЕТ 4
1. Профиль пользователя (`/profile`)
2. Connect модуль (`/connect`)
3. Space модуль (личные посты)

### **Этап 5: Админ-панели (4-5 дней)** ⭐ ПРИОРИТЕТ 5
1. Admin Dashboard (`/admin`)
2. PRO-панели (`/quest/pro`, `/rf/pro`)

---

## ⚠️ Альтернативный подход (НЕ РЕКОМЕНДУЕТСЯ)

### **Вариант 2: Сначала Clerk, потом UI**

**Преимущества:**
- ✅ Можно сразу тестировать авторизацию
- ✅ Меньше переключений между задачами

**Недостатки:**
- ❌ Придётся использовать временные компоненты
- ❌ Больше рефакторинга позже
- ❌ Сложнее отлаживать без готовых UI компонентов

**Рекомендация:** Использовать **Вариант 1** (поэтапный подход)

---

## 📝 Чеклист готовности к интеграции Clerk

Перед началом Фазы 2 (интеграция Clerk) убедитесь, что:

- [ ] Все UI компоненты из Фазы 1 созданы и протестированы
- [ ] Storybook работает для всех компонентов
- [ ] Компоненты соответствуют Style Guide
- [ ] TypeScript типы корректны
- [ ] Компоненты экспортированы из `packages/ui/src/index.ts`

---

## 🔗 Связанные документы

- `/docs/frontend/AUTH_FLOW_CLERK.md` — детальное руководство по Clerk
- `/docs/frontend/FRONTEND_VISION.md` — полное видение фронтенда
- `/docs/overview/go2asia_uiux_style_guide.md` — Style Guide
- `/docs/frontend/API_MOCKING_CONTRACTS.md` — API контракты

---

**Версия:** 1.0  
**Дата:** 2024-11-15  
**Статус:** План действий — готов к реализации


