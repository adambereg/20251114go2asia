# 🏠 План разработки модуля Rielt.Market Asia

**Дата создания:** 26 ноября 2025  
**Ветка:** `feature/rielt-market-asia` (будет создана)  
**Статус:** 📋 План готов к реализации  
**Версия:** 1.0

---

## 📋 Содержание

1. [Обзор модуля](#обзор-модуля)
2. [Архитектура и структура](#архитектура-и-структура)
3. [Детальный план разработки](#детальный-план-разработки)
4. [Технические решения](#технические-решения)
5. [Интеграции с модулями](#интеграции-с-модулями)
6. [Дизайн и UI/UX](#дизайн-и-uiux)
7. [Демо-контент](#демо-контент)
8. [Этапы реализации](#этапы-реализации)
9. [Acceptance Criteria](#acceptance-criteria)
10. [Риски и зависимости](#риски-и-зависимости)

---

## Обзор модуля

### Цель модуля

**Rielt.Market Asia** — модуль поиска и аренды жилья (краткосрочной и долгосрочной) в регионе Юго-Восточной Азии. Модуль позволяет:
- **Пользователям**: находить жильё (квартиры, дома, апартаменты) для аренды
- **Владельцам/агентам**: размещать объявления о сдаче жилья
- **RF-партнёрам**: выделяться специальным бейджем и предлагать ваучеры
- **PRO-кураторам**: верифицировать объекты и получать вознаграждения

### Ключевые особенности

- **Два типа аренды**: Краткосрочная (по дням) и долгосрочная (от 1 месяца)
- **RF-интеграция**: Специальные бейджи для Russian Friendly партнёров, ваучеры на скидки
- **PRO-верификация**: Проверка объектов PRO-кураторами с чек-листом и вознаграждениями
- **Интеграция с Guru**: Отображение жилья "рядом" на карте Guru Asia
- **Интеграция с Atlas**: Выбор города/района из справочника Atlas
- **Интеграция с Space**: Чат с владельцем, профиль PRO-куратора
- **Интеграция с Connect**: Вознаграждения за проверки и онбординг партнёров
- **Интеграция с Pulse**: События рядом в даты бронирования

### Роль в экосистеме

Rielt.Market Asia является **мостом между путешественниками и владельцами жилья**, обеспечивая:
- **Доверие**: через RF-бейджи и PRO-проверки
- **Безопасность**: через систему отзывов и модерацию
- **Удобство**: через интеграцию с другими модулями (Guru, Atlas, Space)
- **Мотивацию**: через ваучеры RF и вознаграждения PRO

---

## Архитектура и структура

### Файловая структура

```
apps/go2asia-pwa-shell/
├── app/
│   └── (public)/
│       └── rielt/
│           ├── page.tsx                    # Главная страница (Home)
│           ├── layout.tsx                  # Layout с метаданными
│           ├── search/
│           │   └── page.tsx                # Страница результатов поиска
│           ├── listings/
│           │   ├── [id]/
│           │   │   └── page.tsx            # Детальная карточка жилья
│           │   └── new/
│           │       └── page.tsx            # Wizard создания объявления
│           └── dashboard/
│               └── page.tsx                # Кабинет владельца (authenticated)
│
└── components/
    └── rielt/
        ├── index.ts                        # Экспорты
        ├── types.ts                        # TypeScript типы
        ├── mockListings.ts                 # Демо-данные
        │
        ├── SearchBar.tsx                   # Поисковая строка (Home)
        ├── QuickFilters.tsx                # Быстрые фильтры-чипы
        ├── PopularCities.tsx               # Секция "Популярно сейчас"
        ├── EditorPicks.tsx                 # Секция "Подборки редакции"
        ├── NewListings.tsx                 # Секция "Новое на этой неделе"
        │
        ├── SearchResults/
        │   ├── SearchResultsView.tsx       # Главный компонент результатов
        │   ├── ListingsList.tsx            # Список объявлений
        │   ├── ListingsMap.tsx             # Карта с маркерами
        │   ├── ListingCard.tsx             # Карточка объявления (универсальная)
        │   ├── FiltersPanel.tsx            # Панель фильтров
        │   └── SortDropdown.tsx            # Выпадающий список сортировок
        │
        ├── ListingDetail/
        │   ├── ListingDetailView.tsx       # Главный компонент детальной страницы
        │   ├── Gallery.tsx                 # Hero-галерея с фото
        │   ├── Summary.tsx                 # Блок сводки (цена, правила, вместимость)
        │   ├── Description.tsx             # Описание + "Для кого подходит"
        │   ├── Amenities.tsx                # Удобства (грид иконок)
        │   ├── HouseRules.tsx               # Домашние правила
        │   ├── AvailabilityCalendar.tsx     # Календарь доступности (краткосрок)
        │   ├── LongTermConditions.tsx       # Условия долгосрока
        │   ├── Location.tsx                 # Локация + "Что рядом" (Atlas/Pulse)
        │   ├── Owner.tsx                    # Владелец/Агент (профиль, чат)
        │   ├── Reviews.tsx                  # Отзывы (UGC)
        │   ├── Verification.tsx            # Блок проверок (PRO, чек-лист)
        │   └── CTAPanel.tsx                 # Sticky панель действий
        │
        ├── CreateListing/
        │   ├── CreateListingWizard.tsx      # Главный компонент wizard
        │   ├── StepType.tsx                 # Шаг 1: Тип жилья
        │   ├── StepLocation.tsx             # Шаг 2: Адрес (Atlas)
        │   ├── StepPhotos.tsx                # Шаг 3: Фото
        │   ├── StepDescription.tsx          # Шаг 4: Описание
        │   ├── StepAmenities.tsx            # Шаг 5: Удобства
        │   ├── StepRules.tsx                # Шаг 6: Правила
        │   ├── StepPricing.tsx              # Шаг 7: Цена
        │   ├── StepAvailability.tsx         # Шаг 8: Доступность/календарь
        │   ├── StepContacts.tsx             # Шаг 9: Контакты/профиль
        │   ├── StepRFStatus.tsx             # Шаг 10: RF-статус (если партнёр)
        │   └── StepSubmit.tsx               # Шаг 11: Отправка на модерацию
        │
        ├── Dashboard/
        │   ├── OwnerDashboard.tsx           # Главный компонент кабинета
        │   ├── ListingsTable.tsx            # Таблица объявлений
        │   ├── AvailabilityCalendar.tsx     # Календарь занятости
        │   ├── IncomingRequests.tsx         # Входящие запросы
        │   ├── Reviews.tsx                  # Отзывы владельца
        │   └── Vouchers.tsx                 # Управление RF-ваучерами
        │
        ├── Shared/
        │   ├── RFBadge.tsx                  # Бейдж Russian Friendly
        │   ├── PROVerifiedBadge.tsx         # Бейдж PRO-проверки
        │   ├── VoucherPopover.tsx            # Popover с условиями ваучера
        │   ├── PriceDisplay.tsx              # Отображение цены (ночь/месяц)
        │   ├── RatingStars.tsx              # Компонент рейтинга
        │   └── EmptyState.tsx                # Пустое состояние
        │
        └── utils/
            ├── index.ts                     # Экспорты
            ├── filters.ts                   # Фильтрация объявлений
            ├── sorting.ts                   # Сортировка объявлений
            ├── geo.ts                       # Геолокация и расстояния
            ├── calendar.ts                  # Работа с календарём
            └── validation.ts                # Валидация форм
```

### Типы данных (TypeScript)

```typescript
// components/rielt/types.ts

// Базовые типы
export interface Coordinates {
  lat: number;
  lng: number;
}

export interface Address {
  country: string;      // Из Atlas
  city: string;         // Из Atlas
  district?: string;    // Район из Atlas
  street?: string;
  building?: string;
  coordinates: Coordinates;
}

// Тип жилья
export type ListingType = 
  | 'apartment' 
  | 'house' 
  | 'studio' 
  | 'room' 
  | 'coliving';

// Тип аренды
export type RentalType = 'short-term' | 'long-term';

// Удобства
export interface Amenities {
  wifi?: boolean;
  workspace?: boolean;
  kitchen?: boolean;
  washingMachine?: boolean;
  parking?: boolean;
  childFriendly?: boolean;
  petsAllowed?: boolean;
  pool?: boolean;
  ac?: boolean;
  // ... другие
}

// Правила
export interface HouseRules {
  smoking?: boolean;
  pets?: boolean;
  parties?: boolean;
  deposit?: number;        // Залог
  prepayment?: number;     // Предоплата
  minStay?: number;        // Минимальный срок (дни)
  maxStay?: number;        // Максимальный срок (дни)
  checkIn?: string;        // Время заезда "14:00"
  checkOut?: string;       // Время выезда "11:00"
}

// Цена
export interface Pricing {
  perNight?: number;        // Для краткосрока
  perMonth?: number;        // Для долгосрока
  currency: string;         // 'USD', 'RUB', 'THB', etc.
  deposit?: number;         // Залог
  cleaningFee?: number;     // Плата за уборку
  serviceFee?: number;     // Сервисный сбор платформы
}

// Доступность (для краткосрока)
export interface Availability {
  calendar: Record<string, boolean>;  // Дата -> доступно/занято
  instantBooking?: boolean;            // Мгновенное бронирование
  advanceBooking?: number;             // За сколько дней можно бронировать
}

// Условия долгосрока
export interface LongTermConditions {
  minMonths: number;       // Минимум месяцев
  maxMonths?: number;       // Максимум месяцев
  deposit?: number;         // Залог
  utilities?: 'included' | 'separate';  // Коммунальные включены/отдельно
}

// PRO-проверка
export interface PROVerification {
  verified: boolean;
  verifiedBy?: string;     // ID PRO-куратора
  verifiedAt?: string;     // ISO date
  checklist?: {
    photosMatch: boolean;      // Фото соответствуют реальности
    condition: boolean;        // Состояние соответствует описанию
    description: boolean;      // Описание точное
    noise: boolean;            // Уровень шума приемлемый
    internet: boolean;         // Интернет работает
  };
  notes?: string;          // Дополнительные заметки PRO
}

// RF-ваучер
export interface RFVoucher {
  id: string;
  title: string;
  description: string;
  discount?: number;       // Скидка в %
  discountAmount?: number; // Фиксированная скидка
  conditions: string;      // Условия использования
  validFrom: string;      // ISO date
  validUntil: string;      // ISO date
  usageLimit?: number;     // Лимит использований
  usedCount?: number;      // Сколько раз использовано
}

// Владелец/Агент
export interface Owner {
  id: string;
  name: string;
  avatar?: string;
  isRFPartner?: boolean;   // RF-партнёр
  isPRO?: boolean;         // PRO-куратор
  rating?: number;         // Рейтинг владельца
  responseTime?: string;   // "В течение часа"
  joinedAt?: string;       // ISO date
}

// Отзыв
export interface Review {
  id: string;
  authorId: string;
  authorName: string;
  authorAvatar?: string;
  rating: number;          // 1-5
  text: string;
  photos?: string[];        // Фото от гостя
  verifiedBooking?: boolean; // Проверенная бронь
  curatorVerified?: boolean; // Проверено куратором
  createdAt: string;       // ISO date
}

// Основной тип объявления
export interface Listing {
  id: string;
  
  // Основная информация
  title: string;
  description: string;
  type: ListingType;
  rentalType: RentalType;
  
  // Локация
  address: Address;
  
  // Фото
  photos: string[];        // Массив URL
  coverPhoto?: string;     // Обложка
  
  // Параметры
  bedrooms?: number;
  beds?: number;
  bathrooms?: number;
  area?: number;            // кв.м
  maxGuests: number;
  
  // Цена
  pricing: Pricing;
  
  // Удобства и правила
  amenities: Amenities;
  houseRules: HouseRules;
  
  // Доступность
  availability?: Availability;           // Для краткосрока
  longTermConditions?: LongTermConditions; // Для долгосрока
  
  // Владелец
  owner: Owner;
  
  // Бейджи и статусы
  isRF?: boolean;                          // Russian Friendly
  rfVoucher?: RFVoucher;                    // RF-ваучер
  proVerification?: PROVerification;       // PRO-проверка
  isNew?: boolean;                         // Новое объявление
  isInstant?: boolean;                     // Мгновенное бронирование
  
  // Метаданные
  rating?: number;                         // Средний рейтинг
  reviewsCount?: number;                   // Количество отзывов
  viewsCount?: number;                     // Просмотры
  savesCount?: number;                     // Сохранения
  createdAt: string;                       // ISO date
  updatedAt: string;                       // ISO date
  
  // Статус модерации
  status: 'draft' | 'pending' | 'approved' | 'rejected';
}

// Фильтры поиска
export interface SearchFilters {
  // Локация
  location?: {
    country?: string;
    city?: string;
    district?: string;
    radius?: number;        // км от точки
    coordinates?: Coordinates;
  };
  
  // Даты (для краткосрока)
  checkIn?: string;         // ISO date
  checkOut?: string;        // ISO date
  
  // Гости
  guests?: number;
  
  // Тип аренды
  rentalType?: RentalType;
  
  // Тип жилья
  types?: ListingType[];
  
  // Цена
  priceRange?: {
    min?: number;
    max?: number;
    currency?: string;
  };
  
  // Удобства
  amenities?: Partial<Amenities>;
  
  // Правила
  rules?: Partial<HouseRules>;
  
  // Доступность
  availableToday?: boolean;
  availableThisWeek?: boolean;
  instantBooking?: boolean;
  
  // Надёжность
  onlyRF?: boolean;
  onlyPROVerified?: boolean;
  
  // Долгосрок
  minMonths?: number;      // Минимум месяцев для долгосрока
  
  // Сортировка
  sortBy?: 'recommended' | 'price-asc' | 'price-desc' | 'distance' | 'newest' | 'rating';
}
```

---

## Детальный план разработки

### Этап 1: Базовая структура и типы

#### Задачи:
1. ✅ Создать ветку `feature/rielt-market-asia`
2. ✅ Создать файловую структуру компонентов
3. ✅ Определить TypeScript типы (`types.ts`)
4. ✅ Создать демо-данные (`mockListings.ts`) — 20-30 объявлений для разных городов ЮВА
5. ✅ Настроить базовые страницы (`page.tsx`, `layout.tsx`)

#### Результат:
- Готова структура модуля
- Определены все типы данных
- Есть демо-контент для разработки

---

### Этап 2: Главная страница (Home)

#### Компоненты:
- `SearchBar.tsx` — поисковая строка с полями:
  - Город/район (выбор из Atlas)
  - Даты (check-in / check-out)
  - Гости
  - Переключатель "Краткосрочно / Долгосрочно"
- `QuickFilters.tsx` — быстрые фильтры-чипы:
  - "Только RF"
  - "Проверено PRO"
  - "Сегодня заселение"
  - "До $X"
  - "С животными"
  - "Wi-Fi"
  - "Рабочее место"
- `PopularCities.tsx` — секция "Популярно сейчас" (города/районы)
- `EditorPicks.tsx` — секция "Подборки редакции" (для семей, nomads, долгосрок)
- `NewListings.tsx` — секция "Новое на этой неделе"

#### Задачи:
1. Реализовать `SearchBar` с интеграцией Atlas для выбора локации
2. Реализовать `QuickFilters` с чипами
3. Реализовать секции контента (PopularCities, EditorPicks, NewListings)
4. Добавить адаптивность для mobile
5. Добавить состояния загрузки (skeleton)

#### Результат:
- Работающая главная страница с поиском
- Быстрые фильтры
- Секции контента

---

### Этап 3: Страница результатов поиска (Search Results)

#### Компоненты:
- `SearchResultsView.tsx` — главный компонент с split-layout
- `ListingsList.tsx` — список объявлений (слева на desktop, сверху на mobile)
- `ListingsMap.tsx` — карта OSM с маркерами (справа на desktop, снизу на mobile)
- `ListingCard.tsx` — универсальная карточка объявления:
  - Фото 3:2
  - Бейджи: RF, PRO-verified, New, Instant
  - Заголовок
  - Мета: цена (ночь/месяц), рейтинг, "сегодня доступно"/календарь
  - Чипы удобств (Wi-Fi, кухня, рабочее место и т.д.)
  - CTA: "Открыть", "Сохранить"
- `FiltersPanel.tsx` — панель фильтров (sticky сверху):
  - Цена (слайдер)
  - Тип жилья
  - Удобства
  - Правила
  - "Только RF", "Проверено PRO"
  - "Долгосрочно ≥ 1 мес"
  - "Залога нет"
- `SortDropdown.tsx` — сортировки:
  - Рекомендуемые
  - Дешевле
  - Ближе (к точке)
  - Новые
  - Рейтинг

#### Задачи:
1. Реализовать split-layout (List + Map)
2. Реализовать `ListingCard` с унифицированным дизайном
3. Реализовать `ListingsMap` с маркерами и кластерами (аналогично Guru)
4. Реализовать `FiltersPanel` с полным набором фильтров
5. Реализовать логику фильтрации (`utils/filters.ts`)
6. Реализовать логику сортировки (`utils/sorting.ts`)
7. Добавить пагинацию ("Показать ещё" cursor-based)
8. Добавить синхронизацию фильтров с URL
9. Добавить состояния: skeleton, empty, error

#### Результат:
- Работающая страница результатов поиска
- Фильтрация и сортировка
- Карта с маркерами
- Пагинация

---

### Этап 4: Детальная карточка жилья (Listing Detail)

#### Компоненты:
- `ListingDetailView.tsx` — главный компонент
- `Gallery.tsx` — Hero-галерея с фото (swiper)
- `Summary.tsx` — сводка:
  - Цена (ночь/месяц)
  - Правила (заезд/выезд, минимальный срок)
  - Вместимость (гости, спальни, кровати, ванные)
  - Тип жилья
  - "Instant/Запрос"
- `Description.tsx` — описание + "Для кого подходит" (иконки: семья, nomad, long-stay)
- `Amenities.tsx` — удобства (грид иконок)
- `HouseRules.tsx` — домашние правила
- `AvailabilityCalendar.tsx` — календарь доступности (для краткосрока)
- `LongTermConditions.tsx` — условия долгосрока (срок, депозит)
- `Location.tsx` — локация:
  - Страна/город/район из Atlas (линки)
  - "Что рядом" — Places/Events из Atlas/Pulse (компактный виджет)
- `Owner.tsx` — владелец/агент:
  - Бейдж RF-партнёр (если есть)
  - "Написать" (в Space/чат)
  - Рейтинг, время ответа
- `Reviews.tsx` — отзывы:
  - UGC с фото
  - Бейдж "Проверено куратором"
  - Сортировка по свежести
- `Verification.tsx` — безопасность/проверки:
  - Кто и когда проверил (PRO)
  - Чек-лист проверки
- `CTAPanel.tsx` — sticky панель действий:
  - Выбрать даты → рассчитать цену
  - "Забронировать"/"Отправить запрос"
  - Кнопки "Сохранить", "Поделиться"

#### Задачи:
1. Реализовать все блоки детальной страницы
2. Интегрировать календарь доступности
3. Интегрировать виджет "Что рядом" (Atlas/Pulse)
4. Реализовать чат с владельцем (интеграция Space)
5. Реализовать отображение RF-ваучера (popover)
6. Реализовать отображение PRO-проверки
7. Реализовать CTA-панель с расчётом цены
8. Добавить адаптивность для mobile
9. Добавить состояния загрузки

#### Результат:
- Полная детальная страница объявления
- Все блоки информации
- Интеграции с Atlas, Pulse, Space

---

### Этап 5: Wizard создания объявления (Create Listing)

#### Компоненты (шаги wizard):
- `CreateListingWizard.tsx` — главный компонент с навигацией по шагам
- `StepType.tsx` — Шаг 1: Тип жилья (apartment, house, studio, room, coliving)
- `StepLocation.tsx` — Шаг 2: Адрес (интеграция Atlas: страна/город/район/улица)
- `StepPhotos.tsx` — Шаг 3: Загрузка фото (drag & drop, превью)
- `StepDescription.tsx` — Шаг 4: Описание (заголовок, текст, "Для кого подходит")
- `StepAmenities.tsx` — Шаг 5: Удобства (чекбоксы)
- `StepRules.tsx` — Шаг 6: Правила (курить, животные, вечеринки, залог, предоплата)
- `StepPricing.tsx` — Шаг 7: Цена (ночь/месяц, валюта, депозит, сборы)
- `StepAvailability.tsx` — Шаг 8: Доступность/календарь (для краткосрока)
- `StepContacts.tsx` — Шаг 9: Контакты/профиль (имя, телефон, email)
- `StepRFStatus.tsx` — Шаг 10: RF-статус (если партнёр, ваучер)
- `StepSubmit.tsx` — Шаг 11: Отправка на модерацию + опция "Запросить проверку PRO"

#### Задачи:
1. Реализовать wizard с навигацией по шагам
2. Реализовать все шаги с валидацией
3. Интегрировать Atlas для выбора локации
4. Реализовать загрузку фото
5. Реализовать календарь доступности
6. Реализовать опцию "Запросить проверку PRO" (вызов, чек-лист, вознаграждение через Connect)
7. Добавить сохранение черновика
8. Добавить адаптивность для mobile

#### Результат:
- Полный wizard создания объявления
- Валидация всех полей
- Интеграция с Atlas и Connect

---

### Этап 6: Кабинет владельца (Dashboard)

#### Компоненты:
- `OwnerDashboard.tsx` — главный компонент кабинета
- `ListingsTable.tsx` — таблица объявлений:
  - Статусы (черновик, на модерации, одобрено, отклонено)
  - Действия (редактировать, удалить, деактивировать)
- `AvailabilityCalendar.tsx` — календарь занятости (все объявления)
- `IncomingRequests.tsx` — входящие запросы на бронирование
- `Reviews.tsx` — отзывы владельца (все объявления)
- `Vouchers.tsx` — управление RF-ваучерами:
  - Создание ваучеров
  - Лимиты и условия
  - Статистика использований

#### Задачи:
1. Реализовать кабинет владельца (только для authenticated)
2. Реализовать таблицу объявлений с действиями
3. Реализовать календарь занятости
4. Реализовать управление запросами
5. Реализовать управление ваучерами
6. Добавить статистику (просмотры, запросы, бронирования)

#### Результат:
- Полный кабинет владельца
- Управление объявлениями
- Управление ваучерами

---

### Этап 7: Интеграции и финальная полировка

#### Интеграции:
1. **Guru Asia**: Отображение жилья "рядом" на карте Guru
2. **Atlas**: Выбор города/района, виджет "Что рядом"
3. **Space**: Чат с владельцем, профиль PRO
4. **Connect**: Вознаграждения за проверки PRO
5. **Pulse**: События рядом в даты бронирования

#### Задачи:
1. Интегрировать отображение жилья в Guru Asia
2. Реализовать deeplink из Guru в Rielt.Market
3. Реализовать виджет "Что рядом" (Atlas/Pulse)
4. Реализовать чат с владельцем (Space)
5. Реализовать отображение RF-бейджей и ваучеров
6. Реализовать отображение PRO-проверок
7. Добавить i18n (русский/английский)
8. Добавить accessibility (ARIA, клавиатурная навигация)
9. Оптимизировать производительность (lazy loading, мемоизация)
10. Добавить SEO метаданные

#### Результат:
- Все интеграции работают
- Модуль готов к production

---

## Технические решения

### Картография

- **Библиотека**: `react-leaflet` + `leaflet` (аналогично Guru Asia)
- **Карта**: OpenStreetMap (OSM) tiles
- **Маркеры**: Цветовая кодировка по типу жилья
- **Кластеризация**: `leaflet.markercluster` для группировки близких объектов
- **Попапы**: Мини-карточки с кнопкой "Подробнее"

### Геолокация

- **Хук**: `useGeolocation` (переиспользовать из Guru)
- **Радиус поиска**: Настраиваемый (1-50 км)
- **Расстояния**: Вычисление через `utils/geo.ts` (Haversine formula)

### Фильтрация и сортировка

- **Фильтры**: `utils/filters.ts` — функции применения фильтров
- **Сортировка**: `utils/sorting.ts` — функции сортировки (цена, расстояние, рейтинг, новизна)
- **URL синхронизация**: Фильтры отражаются в query string

### Календарь доступности

- **Библиотека**: `react-calendar` или кастомный компонент
- **Логика**: Отметка занятых дат, выбор диапазона check-in/check-out
- **Расчёт цены**: На основе выбранных дат и типа аренды

### Загрузка фото

- **Библиотека**: `react-dropzone` или нативный `<input type="file">`
- **Преобразование**: Сжатие на клиенте перед загрузкой
- **Хранение**: Предполагается загрузка на CDN (через API)

### Формы и валидация

- **Библиотека**: `react-hook-form` + `zod` для валидации
- **Валидация**: Все поля формы валидируются перед отправкой
- **Ошибки**: Отображение ошибок под полями

### Состояния загрузки

- **Skeleton**: Компоненты-скелетоны для всех списков и карточек
- **Empty states**: Пустые состояния с CTA
- **Error states**: Обработка ошибок с retry

---

## Интеграции с модулями

### Guru Asia

**Как**: Жильё отображается на карте Guru как объекты типа `housing`

**Реализация**:
- Расширить `GuruObject` типом `housing` (уже есть в Guru)
- Добавить жильё в список объектов Guru при фильтрации
- Карточка жилья в Guru ведёт на `/rielt/listings/[id]`

**Компоненты**:
- Переиспользовать `ObjectCard` из Guru для жилья
- Добавить специфичные бейджи (RF, PRO-verified)

### Atlas

**Как**: Выбор города/района из справочника Atlas

**Реализация**:
- Компонент выбора локации использует API Atlas
- В детальной странице — виджет "Что рядом" (Places из Atlas)

**API**:
- `GET /api/atlas/cities` — список городов
- `GET /api/atlas/districts?cityId=xxx` — районы города
- `GET /api/atlas/places/nearby?lat=xxx&lng=xxx&radius=xxx` — места рядом

### Space

**Как**: Чат с владельцем, профиль PRO-куратора

**Реализация**:
- Кнопка "Написать" открывает чат в Space
- Профиль владельца/PRO отображается через Space API

**API**:
- `GET /api/space/users/[id]` — профиль пользователя
- `POST /api/space/chats` — создание чата

### Connect

**Как**: Вознаграждения за проверки PRO

**Реализация**:
- При запросе проверки PRO создаётся задача в Connect
- После проверки PRO получает Points/G2A через Connect API

**API**:
- `POST /api/connect/tasks` — создание задачи проверки
- `POST /api/connect/rewards` — начисление вознаграждения

### Pulse

**Как**: События рядом в даты бронирования

**Реализация**:
- Виджет "События рядом" в детальной странице
- Фильтрация событий по датам check-in/check-out

**API**:
- `GET /api/pulse/events/nearby?lat=xxx&lng=xxx&startDate=xxx&endDate=xxx` — события рядом

---

## Дизайн и UI/UX

### Цветовая схема

- **Основной цвет**: Emerald (`from-emerald-500 to-emerald-600`)
- **Акценты**: Sky Blue для действий, Green для RF-бейджей
- **Состояния**: Success (green), Warning (amber), Error (red)

### Компоненты из Design System

- **Карточки**: `border-2 border-slate-200`, `rounded-xl`, hover эффекты
- **Кнопки**: Primary (emerald), Secondary (slate), Ghost
- **Бейджи**: RF (green), PRO (purple), Verified (green)
- **Чипы**: Фильтры в виде чипов с hover эффектами

### Адаптивность

- **Mobile first**: Все компоненты сначала для mobile, затем desktop
- **Breakpoints**: Tailwind стандартные (sm, md, lg, xl)
- **Split layout**: На mobile — вертикальный стек, на desktop — горизонтальный split

### Анимации

- **Transitions**: `transition-all duration-200`
- **Hover эффекты**: `hover:-translate-y-1`, `hover:shadow-xl`
- **Loading**: Skeleton анимации

### Доступность

- **ARIA labels**: Все интерактивные элементы
- **Клавиатурная навигация**: Tab, Enter, Escape
- **Контраст**: Минимум AA (WCAG 2.1)
- **Фокус-кольца**: Видимые для всех фокусируемых элементов

---

## Демо-контент

### Объявления (mockListings.ts)

**Количество**: 20-30 объявлений

**Города**:
- Бангкок, Таиланд (5-7 объявлений)
- Пхукет, Таиланд (4-5 объявлений)
- Бали, Индонезия (4-5 объявлений)
- Хошимин, Вьетнам (3-4 объявления)
- Куала-Лумпур, Малайзия (2-3 объявления)
- Сингапур (2-3 объявления)

**Типы**:
- Апартаменты (40%)
- Студии (30%)
- Дома (15%)
- Комнаты (10%)
- Co-living (5%)

**Рент типы**:
- Краткосрочная (60%)
- Долгосрочная (40%)

**RF-партнёры**: 30% объявлений с RF-бейджем

**PRO-проверенные**: 20% объявлений с PRO-проверкой

**Ваучеры**: 10% RF-объявлений с ваучерами

---

## Этапы реализации

### Фаза 1: Подготовка (1-2 дня)
- ✅ Создание ветки
- ✅ Настройка структуры
- ✅ Определение типов
- ✅ Создание демо-данных

### Фаза 2: Главная страница (2-3 дня)
- ✅ SearchBar
- ✅ QuickFilters
- ✅ Секции контента

### Фаза 3: Результаты поиска (3-4 дня)
- ✅ Split layout
- ✅ ListingCard
- ✅ Карта с маркерами
- ✅ Фильтры и сортировка

### Фаза 4: Детальная страница (4-5 дней)
- ✅ Все блоки информации
- ✅ Календарь
- ✅ Интеграции

### Фаза 5: Wizard создания (3-4 дня)
- ✅ Все шаги wizard
- ✅ Валидация
- ✅ Интеграции

### Фаза 6: Кабинет владельца (2-3 дня)
- ✅ Таблица объявлений
- ✅ Календарь занятости
- ✅ Управление ваучерами

### Фаза 7: Интеграции и полировка (3-4 дня)
- ✅ Интеграции с модулями
- ✅ i18n
- ✅ Accessibility
- ✅ SEO

**Общая оценка**: 18-25 рабочих дней (~4-5 недель)

---

## Acceptance Criteria

### Главная страница
- [ ] Поисковая строка работает с выбором локации из Atlas
- [ ] Быстрые фильтры применяются корректно
- [ ] Секции контента отображаются с демо-данными
- [ ] Адаптивность для mobile и desktop

### Результаты поиска
- [ ] Split layout работает на desktop и mobile
- [ ] Карточки объявлений отображаются корректно
- [ ] Карта показывает маркеры и кластеры
- [ ] Фильтры работают и синхронизируются с URL
- [ ] Сортировка работает корректно
- [ ] Пагинация работает ("Показать ещё")

### Детальная страница
- [ ] Все блоки информации отображаются
- [ ] Календарь доступности работает
- [ ] Виджет "Что рядом" показывает места и события
- [ ] Чат с владельцем открывается в Space
- [ ] RF-ваучер отображается с popover
- [ ] PRO-проверка отображается с чек-листом
- [ ] CTA-панель рассчитывает цену корректно

### Wizard создания
- [ ] Все шаги wizard работают
- [ ] Валидация всех полей работает
- [ ] Выбор локации из Atlas работает
- [ ] Загрузка фото работает
- [ ] Календарь доступности работает
- [ ] Опция "Запросить проверку PRO" работает

### Кабинет владельца
- [ ] Таблица объявлений отображается
- [ ] Действия (редактировать, удалить) работают
- [ ] Календарь занятости работает
- [ ] Управление ваучерами работает

### Интеграции
- [ ] Жильё отображается в Guru Asia
- [ ] Deeplink из Guru в Rielt.Market работает
- [ ] Виджет "Что рядом" работает (Atlas/Pulse)
- [ ] Чат с владельцем работает (Space)
- [ ] RF-бейджи и ваучеры отображаются корректно
- [ ] PRO-проверки отображаются корректно

### Качество
- [ ] TypeScript типы корректны (no errors)
- [ ] Адаптивность для всех экранов
- [ ] Accessibility (ARIA, клавиатурная навигация)
- [ ] SEO метаданные
- [ ] Производительность (lazy loading, мемоизация)
- [ ] Обработка ошибок и пустых состояний

---

## Риски и зависимости

### Риски

1. **Интеграция с Atlas API**: Может быть не готов API для выбора локации
   - **Решение**: Использовать демо-данные Atlas или мок API

2. **Интеграция с Space API**: Чат может быть не реализован
   - **Решение**: Использовать мок или заглушку с переходом в Space

3. **Интеграция с Connect API**: Система вознаграждений может быть не готова
   - **Решение**: Использовать мок или заглушку

4. **Загрузка фото**: CDN может быть не настроен
   - **Решение**: Использовать временное хранилище или мок

5. **Календарь доступности**: Сложная логика для краткосрока
   - **Решение**: Упростить на первом этапе, доработать позже

### Зависимости

1. **Guru Asia**: Должен быть реализован для интеграции жилья
2. **Atlas API**: Должен быть готов для выбора локации
3. **Space API**: Должен быть готов для чата
4. **Connect API**: Должен быть готов для вознаграждений
5. **Pulse API**: Должен быть готов для событий рядом

### Альтернативы

- Если API не готовы, использовать моки и заглушки
- Если интеграции сложны, реализовать базовый функционал без интеграций, добавить позже

---

## Следующие шаги

1. **Создать ветку**: `feature/rielt-market-asia`
2. **Начать с Этапа 1**: Базовая структура и типы
3. **Постепенно реализовывать**: По этапам согласно плану
4. **Тестировать**: После каждого этапа
5. **Интегрировать**: После завершения базового функционала

---

**План готов к реализации!** 🚀

