# 📊 Анализ проекта Go2Asia: Промежуточные итоги и рекомендации по оптимизации

**Дата:** 2025-11-09  
**Статус проекта:** Phase 0 завершена ✅ | Phase 1 в процессе (~85% готовности)  
**Версия отчёта:** 1.0

---

## 📋 Содержание

1. [Краткие промежуточные итоги](#краткие-промежуточные-итоги)
2. [Что сделано правильно](#что-сделано-правильно)
3. [Ошибки и проблемы](#ошибки-и-проблемы)
4. [Рекомендации по оптимизации (если бы начинать с Фазы 0)](#рекомендации-по-оптимизации)

---

## 📊 Краткие промежуточные итоги

### Общий прогресс: ~85% готовности к MVP

**Фаза 0 (Архитектурный фундамент):** ✅ 100% завершено
- Инфраструктура настроена и работает
- Монорепозиторий структурирован
- Общие пакеты созданы
- CI/CD настроен
- Документация создана

**Фаза 1 (Pilot Modules):** 🟡 ~85% завершено
- ✅ API Gateway: 100% (задеплоен и работает)
- ✅ Auth Service: 90% (код готов, webhook частично работает)
- ✅ Content Service: 95% (работает, данные загружены)
- ✅ Token Service: 90% (работает, требует демо-данных)
- ✅ Referral Service: 90% (работает, требует демо-данных)
- 🟡 Frontend Integration: 90% (интегрирован, требует тестирования)

### Статистика проекта

- **Frontend:** ~137 файлов (119 .tsx, 17 .ts, 1 .css)
- **Backend:** 5 микросервисов (Cloudflare Workers)
- **Общие пакеты:** 4 пакета (contracts, common-db, logger, contracts-openapi)
- **Документация:** 30+ документов
- **База данных:** Neon PostgreSQL с миграциями Drizzle ORM

---

## ✅ Что сделано правильно

### 1. Архитектура и структура проекта

#### ✅ Монорепозиторий с правильной организацией
- Чёткое разделение на `apps/`, `services/`, `packages/`
- Использование npm workspaces для управления зависимостями
- Единый lockfile в корне проекта
- TypeScript path aliases настроены корректно

**Плюсы:**
- Легко поддерживать общие типы и утилиты
- Упрощённая разработка и тестирование
- Единая версия зависимостей

#### ✅ Модульная архитектура микросервисов
- Каждый сервис независим и может деплоиться отдельно
- Единообразная структура всех сервисов
- Использование общих пакетов (`@go2asia/contracts`, `@go2asia/logger`)

**Плюсы:**
- Масштабируемость
- Независимое развитие сервисов
- Простота поддержки

### 2. Технологический стек

#### ✅ Правильный выбор технологий
- **Cloudflare Workers** для serverless микросервисов (быстро, масштабируемо, дёшево)
- **Hono** для роутинга (легковесный, быстрый, совместим с Workers)
- **Drizzle ORM** для работы с БД (TypeScript-first, простые миграции)
- **Neon PostgreSQL** для БД (serverless, автоматическое масштабирование)
- **TypeScript** везде (типобезопасность)

**Плюсы:**
- Современный стек
- Хорошая производительность
- Низкие затраты на инфраструктуру

### 3. Инфраструктура и деплой

#### ✅ Правильная настройка инфраструктуры
- Домен `go2asia.space` настроен и работает
- Cloudflare для CDN и DNS
- Netlify для фронтенда
- Cloudflare Workers для бэкенда
- Автоматический деплой через GitHub Actions

**Плюсы:**
- Минимальные затраты на инфраструктуру
- Автоматизация деплоя
- Хорошая производительность за счёт CDN

#### ✅ Правильная доменная архитектура
- Фронтенд на основном домене (`go2asia.space`)
- API на поддоменах (`api.go2asia.space`, `auth.go2asia.space`, и т.д.)
- Единый SSL wildcard сертификат

**Плюсы:**
- Простота настройки CORS
- Единые cookies для фронтенда
- Изоляция API сервисов

### 4. Документация

#### ✅ Обширная и структурированная документация
- Архитектурная документация
- Планы разработки по фазам
- Инструкции по деплою
- Отчёты о проблемах и их решениях
- Чек-листы готовности

**Плюсы:**
- Легко онбордить новых разработчиков
- История решений сохраняется
- Упрощённая поддержка

### 5. Безопасность

#### ✅ Правильная реализация авторизации
- Интеграция с Clerk для user authentication
- Service-to-Service JWT для межсервисных вызовов
- Разделение публичных и приватных endpoints
- CORS настроен правильно

**Плюсы:**
- Безопасная архитектура
- Централизованная аутентификация
- Защита межсервисных вызовов

### 6. Обработка ошибок и логирование

#### ✅ Единый подход к логированию
- Пакет `@go2asia/logger` для всех сервисов
- Correlation ID (requestId) для трейсинга
- Структурированное логирование

**Плюсы:**
- Легко отслеживать запросы через сервисы
- Единый формат логов
- Упрощённая отладка

---

## ❌ Ошибки и проблемы

### 1. Проблемы с кешированием

#### Проблема
- Cloudflare кешировал ответы API Gateway по умолчанию
- Фронтенд получал устаревшие данные при первой загрузке
- Пришлось полностью отключить кеширование через Cloudflare Cache Rules

#### Причина
- Не было продумано кеширование на этапе проектирования
- Заголовки `Cache-Control` были настроены в коде, но Cloudflare их игнорировал
- Не было тестирования кеширования перед деплоем

#### Решение
- Создано правило Cloudflare Cache Rules для полного обхода кеша
- Документирована стратегия кеширования (`docs/planning/CACHE_STRATEGY.md`)
- Планируется включить частичное кеширование после MVP

**Урок:** Нужно продумывать кеширование на этапе проектирования и тестировать его до production.

---

### 2. Проблемы с webhook от Clerk

#### Проблема
- 50% webhook запросов от Clerk завершались ошибкой
- Пользователи не синхронизировались с БД
- Таблицы `users` и `webhook_events` оставались пустыми

#### Возможные причины
- Проблемы с проверкой подписи Svix (неправильный `CLERK_WEBHOOK_SECRET`)
- Потеря заголовков `svix-*` при проксировании через API Gateway
- Проблемы с регистром заголовков

#### Решение
- Добавлено детальное логирование webhook запросов
- Исправлена приоритизация `svix-id` для eventId
- Документирован процесс отладки (`docs/webhook-debugging/WEBHOOK_DEBUGGING_REPORT.md`)

**Урок:** Критичные интеграции (webhook) нужно тестировать сразу после настройки, а не откладывать.

---

### 3. Проблемы с Drizzle ORM и GROUP BY

#### Проблема
- Эндпоинт `/countries` возвращал 500 ошибку из-за неправильного GROUP BY
- PostgreSQL требует все не-агрегированные поля в GROUP BY
- Drizzle ORM не всегда корректно генерирует SQL для сложных запросов

#### Причина
- Попытка использовать ORM для сложных запросов с агрегациями
- Недостаточное тестирование SQL запросов перед деплоем

#### Решение
- Переписан запрос на прямой SQL с правильным GROUP BY
- Добавлена обработка ошибок с детальным логированием
- Документировано решение (`docs/planning/FIX_CONTENT_SERVICE_ERRORS.md`)

**Урок:** Для сложных запросов с агрегациями лучше использовать прямой SQL вместо ORM.

---

### 4. Проблемы с логированием в Cloudflare Workers

#### Проблема
- Ошибка "The _write() method is not implemented" в Winston logger
- Winston использовал Node.js streams, которые не полностью реализованы в Cloudflare Workers

#### Причина
- Не было проверки совместимости библиотек с Cloudflare Workers
- Winston был выбран без учёта ограничений Workers runtime

#### Решение
- Создан кастомный транспорт `CloudflareWorkersTransport`
- Используется `console.log` напрямую вместо streams
- Документировано решение (`docs/planning/FIX_LOGGING_AND_AUTH_SUMMARY.md`)

**Урок:** Нужно проверять совместимость библиотек с целевой платформой (Cloudflare Workers) до начала разработки.

---

### 5. Проблемы с Service JWT

#### Проблема
- Ошибка "Invalid service token" / "token must consist of 3 parts"
- Несовместимость библиотек: API Gateway использовал `jose`, а `common-db` использовал `@tsndr/cloudflare-worker-jwt`

#### Причина
- Разные библиотеки для генерации и проверки JWT токенов
- Не было единого стандарта для работы с JWT

#### Решение
- Унифицированы библиотеки: все используют `jose`
- Обновлён `common-db` для использования `jose`
- Документировано решение (`docs/planning/FIX_LOGGING_AND_AUTH_SUMMARY.md`)

**Урок:** Нужно использовать единые библиотеки для критичных операций (JWT) во всех сервисах.

---

### 6. Проблемы с моковыми данными

#### Проблема
- Фронтенд не делал запросы к API, использовались моковые данные
- Переменные окружения не были настроены в Netlify
- Не было явного переключения между моками и API

#### Причина
- Не было явного механизма переключения между моками и API
- Переменные окружения не были документированы как обязательные
- Не было проверки конфигурации при старте приложения

#### Решение
- Добавлена переменная `VITE_USE_MOCKS` для явного переключения
- Добавлено логирование инициализации API клиента
- Документирована настройка (`docs/planning/FIX_MOCK_DATA_ISSUE.md`)

**Урок:** Нужно явно контролировать переключение между моками и реальным API, и документировать это.

---

### 7. Проблемы с кодировкой текста

#### Проблема
- Искажённые символы в ответах API (например, `ÐµÑÐµÑÑ` вместо правильного текста)
- Проблемы с кодировкой данных из seed файлов

#### Причина
- Seed файлы не были сохранены в UTF-8
- Не было проверки кодировки БД перед загрузкой данных

#### Решение
- Проверка кодировки БД через SQL
- Пересохранение seed файлов в UTF-8
- Документировано решение (`docs/planning/FIX_CONTENT_SERVICE_ERRORS.md`)

**Урок:** Нужно проверять кодировку файлов и БД перед загрузкой данных.

---

### 8. Отсутствие тестов

#### Проблема
- Нет unit тестов для сервисов
- Нет integration тестов для API
- Нет E2E тестов для критичных сценариев

#### Причина
- Фокус на быстрой разработке MVP
- Тесты отложены на "потом"

#### Последствия
- Ошибки обнаруживаются только в production
- Сложнее рефакторить код
- Нет уверенности в стабильности изменений

**Урок:** Нужно писать тесты параллельно с разработкой, особенно для критичных функций.

---

### 9. Недостаточный мониторинг

#### Проблема
- Workers Logs не были включены изначально
- Нет метрик производительности
- Нет алертов на ошибки
- Нет интеграции с Sentry

#### Причина
- Мониторинг отложен на "после MVP"
- Фокус на функциональности

#### Последствия
- Сложно отлаживать проблемы в production
- Нет понимания производительности системы
- Ошибки обнаруживаются пользователями

**Урок:** Мониторинг нужно настраивать с первого дня, это критично для production системы.

---

### 10. Проблемы с адаптерами данных

#### Проблема
- Несоответствие между типами API и типами фронтенда
- Пришлось создавать адаптеры для преобразования данных
- Несоответствие между `id` (string) и числовыми ID в моках

#### Причина
- Типы в `@go2asia/contracts` не всегда соответствуют реальным API ответам
- Моки использовали числовые ID, а API использует строковые

#### Решение
- Созданы адаптеры данных (`adapters.ts`)
- Генерация числовых ID из строковых для совместимости
- Документировано решение (`docs/planning/FRONTEND_BACKEND_INTEGRATION.md`)

**Урок:** Нужно синхронизировать типы между фронтендом и бэкендом с самого начала, использовать единые контракты.

---

## 🚀 Рекомендации по оптимизации (если бы начинать с Фазы 0)

### 1. Планирование и проектирование

#### ✅ Раннее планирование кеширования
**Что сделать:**
- Определить стратегию кеширования на этапе проектирования
- Документировать, какие endpoints кешируются и с каким TTL
- Протестировать кеширование в staging перед production

**Результат:** Избежать проблем с устаревшими данными и необходимости полного отключения кеша.

#### ✅ Единые стандарты для библиотек
**Что сделать:**
- Создать документ с рекомендуемыми библиотеками для каждого типа задач
- Для JWT: использовать только `jose` везде
- Для логирования: проверить совместимость с Cloudflare Workers
- Для работы с БД: определить, когда использовать ORM, а когда прямой SQL

**Результат:** Избежать проблем с несовместимостью библиотек.

#### ✅ Раннее определение контрактов API
**Что сделать:**
- Создать OpenAPI спецификации ДО начала разработки сервисов
- Генерировать TypeScript типы из OpenAPI автоматически
- Использовать единые типы в фронтенде и бэкенде

**Результат:** Избежать проблем с несоответствием типов и необходимости адаптеров.

---

### 2. Инфраструктура и деплой

#### ✅ Настройка мониторинга с первого дня
**Что сделать:**
- Включить Workers Logs сразу после первого деплоя
- Настроить базовые метрики (latency, throughput, errors)
- Интегрировать Sentry для отслеживания ошибок
- Настроить алерты на критические ошибки

**Результат:** Быстрое обнаружение и исправление проблем.

#### ✅ Автоматизация тестирования
**Что сделать:**
- Написать unit тесты для критичных функций (JWT, webhook, транзакции)
- Написать integration тесты для API endpoints
- Настроить E2E тесты для критичных сценариев (регистрация, авторизация)
- Интегрировать тесты в CI/CD

**Результат:** Уверенность в стабильности кода и возможность безопасного рефакторинга.

#### ✅ Staging окружение
**Что сделать:**
- Создать staging окружение для тестирования перед production
- Использовать staging для тестирования webhook, кеширования, деплоя
- Настроить автоматический деплой в staging при создании PR

**Результат:** Обнаружение проблем до production.

---

### 3. Разработка

#### ✅ Проверка совместимости библиотек
**Что сделать:**
- Перед выбором библиотеки проверить её совместимость с Cloudflare Workers
- Создать список проверенных библиотек
- Использовать только проверенные библиотеки

**Результат:** Избежать проблем с несовместимостью (например, Winston streams).

#### ✅ Раннее тестирование интеграций
**Что сделать:**
- Тестировать webhook от Clerk сразу после настройки
- Тестировать интеграцию между сервисами сразу после создания
- Не откладывать тестирование на "потом"

**Результат:** Быстрое обнаружение проблем с интеграциями.

#### ✅ Явное управление конфигурацией
**Что сделать:**
- Создать явный механизм переключения между моками и API
- Документировать все обязательные переменные окружения
- Добавить проверку конфигурации при старте приложения
- Выводить понятные ошибки при неправильной конфигурации

**Результат:** Избежать проблем с моковыми данными в production.

#### ✅ Использование прямого SQL для сложных запросов
**Что сделать:**
- Определить, когда использовать ORM, а когда прямой SQL
- Для сложных запросов с агрегациями использовать прямой SQL
- Документировать решение

**Результат:** Избежать проблем с GROUP BY и сложными запросами.

---

### 4. Безопасность и качество

#### ✅ Валидация данных
**Что сделать:**
- Использовать Zod или Valibot для валидации всех входящих запросов
- Валидировать данные на уровне API Gateway
- Валидировать данные в каждом сервисе

**Результат:** Защита от некорректных данных и SQL инъекций.

#### ✅ Обработка ошибок
**Что сделать:**
- Создать единый формат ошибок для всех API
- Добавить обработку ошибок на всех уровнях
- Логировать все ошибки с контекстом

**Результат:** Упрощённая отладка и лучший UX.

#### ✅ Проверка кодировки
**Что сделать:**
- Проверять кодировку всех файлов (UTF-8)
- Проверять кодировку БД перед загрузкой данных
- Документировать требования к кодировке

**Результат:** Избежать проблем с искажёнными символами.

---

### 5. Документация

#### ✅ Документация по деплою
**Что сделать:**
- Создать пошаговые инструкции по деплою каждого сервиса
- Документировать все обязательные переменные окружения
- Создать чек-лист перед деплоем в production

**Результат:** Упрощённый деплой и меньше ошибок.

#### ✅ Документация по отладке
**Что сделать:**
- Создать руководство по отладке типичных проблем
- Документировать, как получить логи из Cloudflare Workers
- Создать FAQ по частым проблемам

**Результат:** Быстрое решение проблем.

---

## 📋 Чек-лист оптимизаций для нового проекта

### Фаза 0: Подготовка

- [ ] Определить стратегию кеширования
- [ ] Создать список рекомендуемых библиотек
- [ ] Проверить совместимость всех библиотек с целевой платформой
- [ ] Создать OpenAPI спецификации для всех API
- [ ] Настроить staging окружение
- [ ] Настроить мониторинг (логи, метрики, алерты)
- [ ] Настроить автоматическое тестирование в CI/CD

### Фаза 1: Разработка

- [ ] Использовать единые библиотеки для критичных операций (JWT, логирование)
- [ ] Тестировать интеграции сразу после настройки
- [ ] Писать тесты параллельно с разработкой
- [ ] Использовать прямой SQL для сложных запросов
- [ ] Явно управлять конфигурацией (моки vs API)
- [ ] Валидировать все входящие данные
- [ ] Проверять кодировку файлов и БД

### Постоянно

- [ ] Обновлять документацию
- [ ] Мониторить метрики и логи
- [ ] Рефакторить код на основе уроков
- [ ] Улучшать тесты

---

## 🎯 Выводы

### Что сделано хорошо

1. **Архитектура:** Правильная структура монорепозитория и микросервисов
2. **Технологии:** Современный и подходящий стек
3. **Инфраструктура:** Правильная настройка доменов и деплоя
4. **Документация:** Обширная и структурированная
5. **Безопасность:** Правильная реализация авторизации

### Основные проблемы

1. **Кеширование:** Не было продумано на этапе проектирования
2. **Интеграции:** Недостаточное тестирование (webhook от Clerk)
3. **Библиотеки:** Несовместимость с Cloudflare Workers (Winston)
4. **Типы:** Несоответствие между фронтендом и бэкендом
5. **Мониторинг:** Отложен на "потом"
6. **Тесты:** Отсутствуют

### Рекомендации

1. **Планировать заранее:** Кеширование, мониторинг, тесты
2. **Тестировать сразу:** Интеграции, библиотеки, конфигурацию
3. **Использовать единые стандарты:** Библиотеки, типы, форматы
4. **Документировать:** Деплой, отладку, конфигурацию
5. **Мониторить с первого дня:** Логи, метрики, алерты

---

## 📝 Заключение

Проект Go2Asia находится в хорошем состоянии с правильной архитектурой и современным стеком технологий. Основные проблемы связаны с недостаточным планированием на ранних этапах (кеширование, мониторинг, тесты) и недостаточным тестированием интеграций.

**Рекомендация:** Применить описанные оптимизации в текущем проекте и использовать их как основу для будущих проектов.

---

## 🔍 Дополнение: Проработка критических слепых зон

**Дата дополнения:** 2025-11-09  
**Статус:** Критические области, требующие детальной проработки

---

### 1. Операционная наблюдаемость как система

#### Текущее состояние
- ✅ Workers Logs включены для всех сервисов
- ✅ Performance middleware добавлен (логирование duration)
- ✅ RequestId для трейсинга
- ❌ Нет SLO/SLI и error budgets
- ❌ Нет дашбордов с метриками
- ❌ Нет алертов и runbooks

#### Что нужно сделать

**1.1. Определить SLO/SLI и Error Budgets**

**Service Level Objectives (SLO):**
- **API Gateway:**
  - Availability: 99.9% (≤8.76 часов downtime в месяц)
  - Latency p95: <200ms для публичных GET, <500ms для POST
  - Error rate: <0.1% (5xx ошибки)
- **Content Service:**
  - Availability: 99.5%
  - Latency p95: <300ms для всех запросов
  - Error rate: <0.2%
- **Auth Service:**
  - Availability: 99.95% (критично для авторизации)
  - Latency p95: <150ms
  - Error rate: <0.05%
- **Token Service:**
  - Availability: 99.9% (критично для экономики)
  - Latency p95: <200ms
  - Error rate: <0.1%
  - Data consistency: 100% (транзакции должны быть атомарными)

**Error Budgets:**
- Месячный error budget = 100% - SLO
- При исчерпании 50% бюджета — предупреждение
- При исчерпании 100% бюджета — блокировка новых фич, фокус на стабильности

**1.2. Создать дашборды**

**Обязательные метрики для дашборда:**

```
┌─────────────────────────────────────────────────────────┐
│ Go2Asia API - Production Dashboard                      │
├─────────────────────────────────────────────────────────┤
│                                                          │
│ Availability (Last 24h): 99.92% ████████████░░░░░░░░   │
│ Error Rate: 0.08% █░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░  │
│                                                          │
│ Latency (p50/p95/p99):                                   │
│   Gateway:     45ms / 180ms / 320ms                      │
│   Content:     120ms / 280ms / 450ms                    │
│   Auth:        80ms / 140ms / 220ms                     │
│   Token:       95ms / 190ms / 310ms                     │
│                                                          │
│ Throughput (req/min):                                    │
│   Gateway:     1,250                                    │
│   Content:     850                                      │
│   Auth:        120                                      │
│   Token:       180                                      │
│                                                          │
│ Top Errors (Last 1h):                                    │
│   1. 500 Internal Server Error (Content): 12            │
│   2. 401 Unauthorized (Auth): 8                         │
│   3. 429 Too Many Requests (Gateway): 5                │
│                                                          │
│ Error Budget Status:                                      │
│   Gateway:     ████████████░░░░░░░░ 60% used           │
│   Content:     ████████████████░░░░ 80% used ⚠️        │
│   Auth:        ████████░░░░░░░░░░░░ 40% used           │
│   Token:       ████████████░░░░░░░░ 60% used           │
└─────────────────────────────────────────────────────────┘
```

**Инструменты:**
- **Cloudflare Analytics** — базовые метрики
- **Grafana Cloud** или **Datadog** — расширенные дашборды
- **Prometheus** + **Grafana** — если нужен self-hosted вариант

**1.3. Настроить алерты**

**Критические алерты (P0 — немедленное реагирование):**
- Error rate > 1% в течение 5 минут
- Availability < 99% в течение 10 минут
- Latency p95 > 1000ms в течение 5 минут
- Token Service: ошибки транзакций > 0.1%
- Auth Service: ошибки webhook от Clerk > 5%

**Предупреждающие алерты (P1 — реагирование в течение часа):**
- Error rate > 0.5% в течение 15 минут
- Latency p95 > 500ms в течение 15 минут
- Error budget исчерпан на 50%
- Необычный всплеск трафика (>200% от среднего)

**1.4. Создать Runbooks**

**Runbook: Ошибки 5xx в Content Service**
1. Проверить логи Cloudflare Workers для Content Service
2. Проверить подключение к Neon DB (`/ready` endpoint)
3. Проверить метрики БД в Neon Dashboard
4. Если проблема с БД — проверить connection pool, таймауты
5. Если проблема с кодом — проверить последний деплой, откатить при необходимости

**Runbook: Всплеск латентности**
1. Проверить дашборд latency по маршрутам
2. Определить проблемный маршрут
3. Проверить логи для этого маршрута
4. Проверить метрики БД (slow queries)
5. Проверить Cloudflare Cache hit ratio
6. При необходимости — увеличить кеширование или оптимизировать запрос

**Runbook: Ошибки Clerk Webhook**
1. Проверить логи Auth Service для webhook запросов
2. Проверить `CLERK_WEBHOOK_SECRET` в Cloudflare Secrets
3. Проверить Clerk Dashboard → Webhooks → Error Rate
4. Проверить, что заголовки `svix-*` передаются через API Gateway
5. Протестировать webhook вручную через Clerk Dashboard

**Инструменты для runbooks:**
- Документировать в `docs/runbooks/`
- Интегрировать в PagerDuty или Opsgenie
- Автоматизировать через Cloudflare Workers или GitHub Actions

---

### 2. API-контракты и тесты

#### Текущее состояние
- ✅ OpenAPI спецификации созданы (`packages/contracts-openapi`)
- ✅ TypeScript типы генерируются из OpenAPI
- ❌ Нет contract-тестов (Dredd/Schemathesis)
- ❌ Нет fuzz-тестов
- ❌ Нет минимального покрытия тестами (80%)

#### Что нужно сделать

**2.1. OpenAPI-first подход**

**Требования:**
- Все изменения API сначала в OpenAPI спецификации
- Генерация типов автоматически при изменении OpenAPI
- CI/CD проверяет соответствие кода OpenAPI спецификации

**Процесс:**
1. Разработчик изменяет `packages/contracts-openapi/src/*.yaml`
2. Генерируются TypeScript типы: `npm run generate:types`
3. Код сервиса обновляется с использованием новых типов
4. CI/CD проверяет, что код соответствует OpenAPI

**2.2. Contract-тесты**

**Использовать Schemathesis для автоматических тестов:**

```yaml
# .github/workflows/contract-tests.yml
name: Contract Tests

on: [push, pull_request]

jobs:
  contract-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run Schemathesis
        run: |
          pip install schemathesis
          schemathesis run \
            --base-url https://api.go2asia.space \
            --checks all \
            --hypothesis-max-examples=50 \
            packages/contracts-openapi/src/content.yaml
```

**Минимальное покрытие:**
- Публичные GET endpoints: 80% покрытие
- POST/PUT/DELETE endpoints: 90% покрытие
- Критичные endpoints (Token, Auth): 95% покрытие

**2.3. Fuzz-тесты**

**Использовать для поиска edge cases:**
- Невалидные payload'ы
- Очень длинные строки
- Специальные символы
- Граничные значения (0, -1, MAX_INT)

**2.4. Интеграция в CI/CD**

**Workflow:**
1. При изменении OpenAPI → запуск contract-тестов
2. При изменении кода сервиса → запуск unit-тестов + contract-тестов
3. Перед деплоем в production → полный набор тестов

---

### 3. Стратегия кэширования

#### Текущее состояние
- ❌ Кеш полностью отключен через Cloudflare Cache Rules
- ✅ Заголовки `Cache-Control` настроены в коде
- ❌ Нет матрицы TTL/инвалидации
- ❌ Нет плана повторного включения

#### Что нужно сделать

**3.1. Матрица TTL и инвалидации**

| Endpoint | Тип | TTL | Инвалидация | Примечание |
|----------|-----|-----|-------------|------------|
| `GET /v1/api/content/countries` | Публичный | 10 мин | При изменении через API | Редко меняется |
| `GET /v1/api/content/cities` | Публичный | 10 мин | При изменении через API | Редко меняется |
| `GET /v1/api/content/places` | Публичный | 5 мин | При изменении через API | Может меняться чаще |
| `GET /v1/api/content/events` | Публичный | 2 мин | При изменении через API | Часто меняется |
| `GET /v1/api/content/articles` | Публичный | 15 мин | При изменении через API | Редко меняется |
| `GET /v1/api/token/balance` | Приватный | no-store | - | Персональные данные |
| `GET /v1/api/token/transactions` | Приватный | no-store | - | Персональные данные |
| `GET /v1/api/referral/stats` | Приватный | no-store | - | Персональные данные |
| `POST /v1/api/token/balance/add` | Приватный | no-store | - | Изменяет состояние |

**3.2. План повторного включения кеша**

**Этап 1: Включить кеш для статических данных (Week 1)**
- Endpoints: `/countries`, `/cities`
- TTL: 10 минут
- Мониторить cache hit ratio (цель: >80%)

**Этап 2: Включить кеш для контента (Week 2)**
- Endpoints: `/places`, `/articles`
- TTL: 5-15 минут (в зависимости от типа)
- Мониторить cache hit ratio и актуальность данных

**Этап 3: Включить кеш для событий (Week 3)**
- Endpoints: `/events`
- TTL: 2 минуты (короткий, т.к. часто меняется)
- Мониторить актуальность данных

**3.3. Инвалидация кеша**

**Автоматическая инвалидация:**
- При изменении данных через API → инвалидировать соответствующий кеш
- Использовать Cloudflare Cache Tags или Purge API

**Пример:**
```typescript
// При обновлении места
await updatePlace(id, data);
// Инвалидировать кеш
await cloudflare.purgeCache({
  tags: [`place:${id}`, 'places:list']
});
```

**3.4. Мониторинг кеша**

**Метрики:**
- Cache hit ratio (цель: >80% для публичных endpoints)
- Cache miss latency (время ответа при промахе)
- Stale data rate (сколько раз отдавались устаревшие данные)

---

### 4. Безопасность и злоупотребления

#### Текущее состояние
- ✅ CORS настроен правильно
- ✅ Service JWT для межсервисных вызовов
- ⚠️ Rate limiting упоминается, но не детализирован
- ❌ Нет защиты от накруток рефералок/поинтов
- ⚠️ Валидация упоминается, но не везде реализована

#### Что нужно сделать

**4.1. Rate Limiting на API Gateway**

**Матрица лимитов:**

| Endpoint | Лимит | Период | По чему | Примечание |
|----------|-------|--------|---------|------------|
| Публичные GET | 100 req | 1 мин | IP | Защита от DDoS |
| Приватные GET | 200 req | 1 мин | User ID | Нормальное использование |
| POST /token/add | 10 req | 1 мин | User ID | Защита от накруток |
| POST /token/subtract | 20 req | 1 мин | User ID | Нормальное использование |
| POST /referral/register | 5 req | 1 час | User ID | Защита от накруток рефералок |
| Webhook Clerk | 100 req | 1 мин | IP | Защита от спама |

**Реализация:**
- Использовать Cloudflare Rate Limiting Rules
- Или реализовать через Redis (если нужен более гибкий контроль)

**4.2. Защита от накруток рефералок**

**Механизмы:**
- Проверка уникальности IP при регистрации по реферальному коду
- Velocity limits: максимум 5 регистраций по одному коду в час
- Проверка на ботов: CAPTCHA при подозрительной активности
- Анализ паттернов: одинаковые IP, email домены, имена

**Реализация:**
```typescript
// В Referral Service
async function registerReferral(code: string, userId: string, ip: string) {
  // Проверка velocity limit
  const recentRegistrations = await getRecentRegistrationsByCode(code, '1h');
  if (recentRegistrations.length >= 5) {
    throw new Error('Rate limit exceeded');
  }
  
  // Проверка на дубликаты IP
  const existingByIP = await getRegistrationsByIP(ip, '24h');
  if (existingByIP.length >= 3) {
    // Требовать CAPTCHA
    throw new Error('CAPTCHA required');
  }
  
  // ... остальная логика
}
```

**4.3. Защита от накруток поинтов**

**Механизмы:**
- Velocity limits для начисления поинтов
- Проверка на автоматизацию (слишком быстрые запросы)
- Анализ паттернов (одинаковые суммы, одинаковые причины)
- Sanity checks: максимум поинтов за действие, максимум за день

**Реализация:**
```typescript
// В Token Service
async function addPoints(userId: string, amount: number, reason: string) {
  // Velocity limit: максимум 1000 поинтов в час
  const hourlyTotal = await getHourlyTotal(userId);
  if (hourlyTotal + amount > 1000) {
    throw new Error('Hourly limit exceeded');
  }
  
  // Sanity check: максимум 100 поинтов за одно действие
  if (amount > 100) {
    throw new Error('Amount too large');
  }
  
  // Проверка на автоматизацию: слишком быстрые запросы
  const lastRequest = await getLastRequestTime(userId);
  if (Date.now() - lastRequest < 1000) {
    throw new Error('Too many requests');
  }
  
  // ... остальная логика
}
```

**4.4. Валидация входящих payload'ов**

**Использовать Zod для всех входящих запросов:**

```typescript
// Пример валидации
import { z } from 'zod';

const addPointsSchema = z.object({
  userId: z.string().uuid(),
  amount: z.number().int().positive().max(100),
  reason: z.string().min(1).max(100),
  metadata: z.record(z.unknown()).optional()
});

// В middleware
app.post('/token/add', async (c) => {
  const body = await c.req.json();
  const validated = addPointsSchema.parse(body);
  // ... обработка
});
```

**Требования:**
- Все POST/PUT/DELETE запросы должны валидироваться
- Валидация на уровне API Gateway (первая линия защиты)
- Валидация в каждом сервисе (вторая линия защиты)

---

### 5. Данные и эксплуатация

#### Текущее состояние
- ✅ Миграции Drizzle настроены
- ❌ Нет стратегии бэкапов
- ❌ Нет плана recovery
- ❌ Нет плана отката релиза

#### Что нужно сделать

**5.1. Стратегия бэкапов Neon**

**Требования:**
- Ежедневные автоматические бэкапы (Neon делает это автоматически)
- Хранение бэкапов: минимум 30 дней
- Тестирование восстановления: раз в месяц

**Процесс:**
1. Neon автоматически создаёт бэкапы каждые 24 часа
2. Бэкапы хранятся в Neon (можно экспортировать в S3)
3. Раз в месяц тестировать восстановление на staging

**5.2. План Recovery**

**Сценарий 1: Потеря данных в production**

1. Остановить все запросы на запись (read-only mode)
2. Определить точку восстановления (последний валидный бэкап)
3. Восстановить БД из бэкапа на staging
4. Проверить целостность данных
5. Восстановить на production
6. Проверить работу приложения

**Время восстановления (RTO):** <4 часов  
**Допустимая потеря данных (RPO):** <24 часов

**Сценарий 2: Коррупция данных**

1. Определить затронутые таблицы
2. Восстановить только затронутые таблицы из бэкапа
3. Проверить целостность foreign keys
4. Применить миграции, если необходимо

**5.3. Миграции "вперёд/назад"**

**Требования:**
- Все миграции должны быть обратимыми
- Тестировать откат миграций на staging перед production

**Процесс:**
```bash
# Применение миграции
npm run db:migrate

# Откат миграции (если что-то пошло не так)
npm run db:rollback
```

**5.4. План отката релиза**

**Автоматический откат:**
- Если error rate > 5% в течение 5 минут после деплоя → автоматический откат
- Если availability < 95% в течение 10 минут → автоматический откат

**Ручной откат:**
1. Определить проблемную версию
2. Откатить код к предыдущей версии
3. Задеплоить предыдущую версию
4. Проверить работоспособность
5. Проанализировать проблему

**Инструменты:**
- Cloudflare Workers поддерживает версионирование
- Можно использовать feature flags для постепенного rollout

---

### 6. SEO/продуктовая готовность фронта

#### Текущее состояние
- ✅ PWA настроен
- ✅ SPA работает
- ❌ Нет SSR/пререндера для SEO
- ❌ Ключевые страницы не индексируются

#### Что нужно сделать

**6.1. SSR/Пререндер для ключевых страниц**

**Критичные страницы для SEO:**
- `/atlas/countries` — список стран
- `/atlas/countries/:id` — страница страны
- `/atlas/cities/:id` — страница города
- `/blog` — список статей
- `/blog/:slug` — статья

**Варианты реализации:**

**Вариант 1: Cloudflare Pages с SSR**
- Использовать Cloudflare Pages Functions для SSR
- Пререндерить статические страницы при деплое

**Вариант 2: Netlify с Prerendering**
- Использовать Netlify Prerendering для статических страниц
- Настроить правила для динамических страниц

**Вариант 3: Отдельный SSR сервис**
- Создать отдельный сервис на Cloudflare Workers для SSR
- Использовать React Server Components

**Рекомендация:** Начать с Варианта 2 (Netlify Prerendering) — самый простой.

**6.2. Мета-теги и Open Graph**

**Требования:**
- Уникальные title и description для каждой страницы
- Open Graph теги для социальных сетей
- Structured Data (JSON-LD) для поисковых систем

**Пример:**
```tsx
// Для страницы страны
<Head>
  <title>{country.name} - Go2Asia Atlas</title>
  <meta name="description" content={country.description} />
  <meta property="og:title" content={`${country.name} - Go2Asia`} />
  <meta property="og:description" content={country.description} />
  <meta property="og:image" content={country.flag} />
  <script type="application/ld+json">
    {JSON.stringify({
      "@context": "https://schema.org",
      "@type": "Country",
      "name": country.name,
      "description": country.description
    })}
  </script>
</Head>
```

**6.3. Sitemap и Robots.txt**

**Требования:**
- Автоматическая генерация sitemap.xml из данных БД
- Правильный robots.txt для поисковых систем
- Обновление sitemap при изменении контента

---

### 7. Контент-модерация и политика

#### Текущее состояние
- ❌ Нет модерационных флоу
- ❌ Нет системы жалоб
- ❌ Нет audit-лога
- ❌ Нет политики хранения данных

#### Что нужно сделать

**7.1. Модерационные флоу**

**Для Space Module (посты, комментарии):**
- Автоматическая модерация через AI (опционально)
- Ручная модерация для подозрительного контента
- Модерация по жалобам пользователей

**Для Blog Module (статьи):**
- Предмодерация для новых авторов
- Постмодерация для проверенных авторов
- Автоматическая проверка на плагиат

**Для Quest Module (квесты):**
- Предмодерация всех квестов
- Проверка на соответствие правилам
- Проверка наложений (дубликаты)

**7.2. Система жалоб**

**Типы жалоб:**
- Спам
- Оскорбления
- Неподходящий контент
- Нарушение авторских прав
- Мошенничество

**Процесс обработки:**
1. Пользователь подаёт жалобу
2. Жалоба попадает в очередь модерации
3. Модератор проверяет жалобу (в течение 24 часов)
4. Принимается решение (удалить/предупреждение/отклонить)
5. Уведомление пользователю о результате

**7.3. Audit-лог**

**Что логировать:**
- Все действия модераторов (удаление, блокировка, предупреждение)
- Все жалобы пользователей
- Все изменения контента (кто, когда, что изменил)
- Все транзакции поинтов (для аудита)

**Хранение:**
- Минимум 1 год для audit-лога
- Минимум 90 дней для логов действий пользователей
- Соответствие GDPR (право на удаление данных)

**7.4. Политика хранения данных**

**Требования:**
- Документировать, какие данные хранятся и как долго
- Реализовать право на удаление данных (GDPR)
- Реализовать право на экспорт данных (GDPR)
- Автоматическое удаление неактивных аккаунтов (через 2 года)

---

### 8. Экономика токенов в проде

#### Текущее состояние
- ✅ Базовая логика начисления/списания поинтов
- ❌ Нет анти-игровых механик
- ❌ Нет симуляций наград/сгорания
- ❌ Нет санити-чеков транзакций

#### Что нужно сделать

**8.1. Анти-игровые механики**

**Velocity Limits:**
- Максимум поинтов за действие (зависит от типа действия)
- Максимум поинтов за день (зависит от уровня пользователя)
- Максимум поинтов за неделю

**Незащищённые пути начисления:**
- Все начисления должны проходить через Token Service
- Нет прямого доступа к БД для начисления поинтов
- Все начисления логируются с причиной и метаданными

**8.2. Симуляции наград/сгорания**

**Механика сгорания:**
- Поинты сгорают, если не используются в течение 90 дней
- Уведомление пользователю за 7 дней до сгорания
- Возможность "спасти" поинты через активность

**Симуляция экономики:**
- Моделировать различные сценарии начисления/списания
- Проверять баланс экономики (не должно быть инфляции)
- Анализировать паттерны использования поинтов

**8.3. Санити-чек транзакций**

**Автоматические проверки:**
- Проверка на отрицательный баланс (не должно быть)
- Проверка на подозрительные паттерны (слишком быстрые транзакции)
- Проверка на дубликаты (идемпотентность)

**Ручные проверки:**
- Ежедневный аудит крупных транзакций (>1000 поинтов)
- Еженедельный аудит топ-10 пользователей по начислениям
- Ежемесячный аудит всей экономики

**Реализация:**
```typescript
// В Token Service
async function addPoints(userId: string, amount: number, reason: string) {
  // Санити-чек 1: проверка на отрицательный баланс
  const balance = await getBalance(userId);
  if (balance + amount < 0) {
    throw new Error('Negative balance not allowed');
  }
  
  // Санити-чек 2: проверка на подозрительные паттерны
  const recentTransactions = await getRecentTransactions(userId, '1h');
  if (recentTransactions.length > 50) {
    // Флаг для ручной проверки
    await flagForReview(userId, 'Too many transactions');
  }
  
  // Санити-чек 3: проверка на дубликаты
  const duplicate = await checkDuplicate(userId, amount, reason, '5m');
  if (duplicate) {
    throw new Error('Duplicate transaction');
  }
  
  // ... остальная логика
}
```

---

## 📋 Приоритизация внедрения

### Критический приоритет (до MVP)
1. ✅ Операционная наблюдаемость (базовые метрики и алерты)
2. ✅ Стратегия кэширования (матрица TTL и план включения)
3. ✅ Безопасность (rate limiting и валидация)
4. ✅ Данные (бэкапы и план recovery)

### Высокий приоритет (после MVP)
5. API-контракты и тесты (contract-тесты и покрытие)
6. SEO (SSR/пререндер для ключевых страниц)
7. Контент-модерация (базовые флоу)

### Средний приоритет (Фаза 2)
8. Экономика токенов (анти-игровые механики и симуляции)

---

**Дата создания отчёта:** 2025-11-09  
**Дата дополнения:** 2025-11-09  
**Автор:** AI Assistant (Composer)  
**Версия:** 2.0 (с дополнением по слепым зонам)

