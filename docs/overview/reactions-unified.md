# 📘 Унифицированная модель реакций в Go2Asia

В экосистеме **Go2Asia** все пользовательские реакции (на публикации, места, события, квесты и т.д.) реализуются в виде **отдельной публикации** с сохранением ссылки на исходный объект. Это **заменяет классические комментарии** и делает систему реакций более гибкой, масштабируемой и геймифицированной.

---

## 🧠 Принцип

* **Любая реакция** = `Post` с полем `reply_to`, указывающим на объект реакции (другой пост, место, событие, квест и т.д.)
* Публикация создаётся через обычную форму поста
* Подставляется карточка-превью объекта (в стиле Twitter Card)
* Публикация сначала остаётся приватной (Space: Моё), затем может быть открыта
* Возможен шаринг в тематические группы

---

## 🎯 Где применяется

| Модуль           | Объект                 | Поведение                           |
| ---------------- | ---------------------- | ----------------------------------- |
| **Blog Asia**    | Статья, пост           | Ответ-мнение, аналитическая реакция |
| **Atlas Asia**   | Место, подборка        | Отзыв, личный опыт                  |
| **Pulse Asia**   | Событие                | Фото/видео-отчёт                    |
| **Quest Asia**   | Квест                  | Отчёт о прохождении, впечатления    |
| **RF Asia**      | Бизнес-партнёр (место) | Отзыв клиента                       |
| **Guru Asia**    | Место «рядом»          | Заметка, рекомендация               |
| **Connect Asia** | Миссия, награда        | Публикация-достижение               |

---

## 🧭 UX-механика

* У любого объекта доступна кнопка **«Откликнуться» / «Оставить отзыв»**
* При нажатии открывается форма создания поста
* В поле `reply_to` автоматически передаётся ID объекта
* Подставляется карточка объекта (заголовок, краткое описание, фото)
* Пользователь заполняет текст, добавляет медиа
* Публикация создаётся в `post-core`, отображается в Space
* Может быть расшарена в группы или отобрана модераторами в контентные блоки

---

## 🎁 Вознаграждения и геймификация

* Реакции учитываются в Connect Asia как действия:

  * +Points (если соответствует миссии или набирает лайки)
  * +XP (если входит в сезонные активности)
  * +NFT (например, «10 отзывов на RF-партнёров»)

---

## 🔐 Безопасность и модерация

* Все реакции — обычные посты с `reply_to`
* Поддерживается модерация постов и цепочек ответов
* Возможна автопроверка связей (например, запрещён спам на один и тот же объект)

---

## 📦 Технические детали

* Поле `reply_to` может быть:

  * `post:<post_id>`
  * `place:<place_id>`
  * `event:<pulse_event_id>`
  * `quest:<quest_id>`
  * `partner:<rf_id>`
  * `mission:<mission_id>`
* API: `POST /v1/posts` с `reply_to`
* UI: компонент `ReplyCard`
* Статистика реакции учитывается в Connect по типу и ID объекта

---

## 📌 Преимущества подхода

* ✅ Упрощает архитектуру (нет отдельной сущности комментариев)
* ✅ Объединяет поведение по всей экосистеме
* ✅ Поддерживает приватность, геймификацию и продвижение
* ✅ Позволяет легко внедрять модерацию, deeplink, оповещения
* ✅ Единая система отслеживания активности

---

## 📍Следующие шаги

* Обновить схемы данных `post-core` (добавить `reply_to` с типами)
* Добавить компонент `ReplyButton` на карточки объектов
* Расширить Feed-сервисы: отображение связанного объекта
* Интегрировать в Connect: учёт наград за отклики
