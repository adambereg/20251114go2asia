# 📋 Подробный план разработки Фазы 0

**Дата создания:** 2025-11-14  
**Статус:** Готов к реализации  
**Версия:** 1.0

> **Цель:** Создать детальный, пошаговый план разработки Фазы 0 с конкретными задачами, критериями готовности и зависимостями.

---

## 📑 Содержание

1. [Обзор Фазы 0](#1-обзор-фазы-0)
2. [День 1: Инициализация монорепо](#2-день-1-инициализация-монорепо)
3. [День 2: OpenAPI каркас](#3-день-2-openapi-каркас)
4. [День 3: Безопасность и логирование](#4-день-3-безопасность-и-логирование)
5. [День 4: CI/CD Pipeline](#5-день-4-cicd-pipeline)
6. [День 5: Neon и данные](#6-день-5-neon-и-данные)
7. [День 6: Кэширование и алёрты](#7-день-6-кэширование-и-алёрты)
8. [День 7: Фронт и финализация](#8-день-7-фронт-и-финализация)
9. [Критерии готовности](#9-критерии-готовности)
10. [Риски и митигация](#10-риски-и-митигация)
11. [Чек-лист прогресса](#11-чек-лист-прогресса)

---

## 1. Обзор Фазы 0

### 🎯 Цели Фазы 0

**Основная цель:** Создать предсказуемый каркас проекта с чистыми контрактами и готовностью к масштабированию, избежав всех выявленных проблем из предыдущей итерации.

**Ключевые результаты:**
- ✅ Единый монорепо со строгими капсулами
- ✅ OpenAPI-first подход с автогенерацией типов и SDK
- ✅ Полноценный CI/CD pipeline
- ✅ Observability Day-1 (логи, метрики, алёрты)
- ✅ Базовая безопасность и анти-абьюз
- ✅ Кэш-политика и стратегия
- ✅ Настроенная БД с миграциями и бэкапами
- ✅ PWA-оболочка с SSR/SSG для SEO

### 📊 Метрики успеха

| Метрика | Целевое значение | Измерение |
|---------|------------------|-----------|
| Время сборки монорепо | < 5 минут | CI/CD pipeline |
| Покрытие тестами | > 60% | Unit + E2E |
| Lighthouse Performance | ≥ 85 | Lighthouse CI |
| Lighthouse SEO | ≥ 85 | Lighthouse CI |
| API Response Time (p95) | < 500ms | Cloudflare Analytics |
| Error Rate | < 0.1% | Cloudflare Analytics |
| Uptime | > 99.5% | Cloudflare Analytics |

### 🔗 Зависимости от других документов

- **PHASE0_RESTART_PLAYBOOK.md** — основной playbook с архитектурой
- **PHASE0_CHECKLIST.md** — чек-лист для отслеживания прогресса
- **CACHE_STRATEGY.md** — стратегия кэширования
- **MVP_ROADMAP.md** — roadmap до MVP

---

## 2. День 1: Инициализация монорепо

### 🎯 Цель дня

Создать базовую структуру монорепо с правильной организацией капсул, настроить инструменты разработки и создать каркасы основных приложений.

### 📋 Задачи

#### 2.1. Инициализация монорепо (2 часа)

**Задачи:**
- [ ] Создать корневую директорию `go2asia-monorepo/`
- [ ] Инициализировать `pnpm` workspace
- [ ] Настроить `package.json` в корне с workspace конфигурацией
- [ ] Установить и настроить Turborepo
- [ ] Создать базовую структуру директорий

**Структура:**
```
go2asia-monorepo/
├── package.json          # Корневой package.json с workspaces
├── pnpm-workspace.yaml   # Конфигурация pnpm workspaces
├── turbo.json           # Конфигурация Turborepo
├── .gitignore
├── .gitattributes
├── apps/
│   └── go2asia-pwa-shell/
├── services/
│   ├── api-gateway/
│   ├── auth-service/
│   ├── content-service/
│   ├── token-service/
│   └── referral-service/
├── packages/
│   ├── ui/
│   ├── types/
│   ├── sdk/
│   ├── config/
│   ├── logger/
│   └── schemas/
└── docs/
```

**Критерии готовности:**
- ✅ `pnpm install` выполняется без ошибок
- ✅ Turborepo видит все пакеты
- ✅ Структура директорий соответствует плану

**Команды:**
```bash
# Инициализация pnpm workspace
pnpm init
# Создание pnpm-workspace.yaml
echo "packages:
  - 'apps/*'
  - 'services/*'
  - 'packages/*'" > pnpm-workspace.yaml

# Установка Turborepo
pnpm add -D -w turbo

# Создание turbo.json
# (см. шаблон ниже)
```

**Файлы для создания:**
- `pnpm-workspace.yaml`
- `turbo.json` (конфигурация Turborepo)
- `.gitignore` (с правилами для монорепо)

---

#### 2.2. Настройка TypeScript и базовых конфигов (1.5 часа)

**Задачи:**
- [ ] Создать `packages/config/` с общими конфигами
- [ ] Настроить `tsconfig.json` базовый и расширяемые конфиги
- [ ] Настроить ESLint конфигурацию
- [ ] Настроить Prettier конфигурацию
- [ ] Настроить `.editorconfig`

**Структура `packages/config/`:**
```
packages/config/
├── package.json
├── tsconfig.base.json      # Базовый tsconfig
├── tsconfig.nextjs.json    # Для Next.js приложений
├── tsconfig.worker.json    # Для Cloudflare Workers
├── eslint.base.js          # Базовый ESLint
└── prettier.config.js      # Prettier конфиг
```

**Критерии готовности:**
- ✅ Все пакеты могут использовать общие конфиги
- ✅ TypeScript компилируется без ошибок
- ✅ ESLint и Prettier работают корректно

**Пример `tsconfig.base.json`:**
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true
  }
}
```

---

#### 2.3. Создание каркаса Next.js PWA Shell (2 часа)

**Задачи:**
- [ ] Инициализировать Next.js 15 проект в `apps/go2asia-pwa-shell/`
- [ ] Настроить App Router структуру
- [ ] Создать базовые layout компоненты
- [ ] Настроить Tailwind CSS + shadcn/ui
- [ ] Создать структуру роутов (public/private)

**Структура:**
```
apps/go2asia-pwa-shell/
├── package.json
├── next.config.js
├── tailwind.config.js
├── tsconfig.json
├── app/
│   ├── layout.tsx
│   ├── page.tsx
│   ├── (public)/
│   │   ├── atlas/
│   │   ├── blog/
│   │   └── pulse/
│   └── (private)/
│       ├── connect/
│       └── profile/
├── components/
└── lib/
```

**Критерии готовности:**
- ✅ `pnpm dev` запускает Next.js без ошибок
- ✅ Базовые страницы отображаются
- ✅ Tailwind CSS работает
- ✅ Структура роутов соответствует плану

**Команды:**
```bash
cd apps/go2asia-pwa-shell
pnpm create next-app@latest . --typescript --tailwind --app --no-src-dir
pnpm add -D @types/node
pnpm add shadcn-ui
```

---

#### 2.4. Создание каркаса API Gateway (1.5 часа)

**Задачи:**
- [ ] Инициализировать Cloudflare Worker проект
- [ ] Настроить Hono framework
- [ ] Создать базовую структуру роутов
- [ ] Настроить `wrangler.toml`

**Структура:**
```
services/api-gateway/
├── package.json
├── wrangler.toml
├── tsconfig.json
├── src/
│   ├── index.ts           # Точка входа
│   ├── routes/
│   │   ├── content.ts
│   │   ├── auth.ts
│   │   ├── token.ts
│   │   └── referral.ts
│   └── utils/
│       └── proxy.ts
└── test/
```

**Критерии готовности:**
- ✅ `wrangler dev` запускает Worker локально
- ✅ Базовые роуты отвечают
- ✅ Структура соответствует плану

**Команды:**
```bash
cd services/api-gateway
pnpm create cloudflare@latest . --template=worker-typescript
pnpm add hono
pnpm add -D @cloudflare/workers-types
```

**Пример `wrangler.toml`:**
```toml
name = "go2asia-api-gateway"
main = "src/index.ts"
compatibility_date = "2024-01-01"

[env.staging]
name = "go2asia-api-gateway-staging"

[env.production]
name = "go2asia-api-gateway-prod"
```

---

#### 2.5. Настройка скриптов и линтеров (1 час)

**Задачи:**
- [ ] Добавить скрипты в корневой `package.json`
- [ ] Настроить lint-staged для pre-commit хуков
- [ ] Настроить Husky для Git hooks
- [ ] Добавить скрипты для сборки всех пакетов

**Скрипты в корневом `package.json`:**
```json
{
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "clean": "turbo run clean",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,jsx,json,md}\""
  }
}
```

**Критерии готовности:**
- ✅ Все скрипты работают
- ✅ Pre-commit hooks проверяют код
- ✅ Форматирование применяется автоматически

---

### ✅ Критерии завершения Дня 1

- [ ] Монорепо инициализирован и работает
- [ ] TypeScript конфиги настроены для всех пакетов
- [ ] Next.js PWA Shell создан и запускается
- [ ] API Gateway создан и запускается локально
- [ ] Линтеры и форматтеры настроены
- [ ] Все зависимости установлены без ошибок

**Время выполнения:** ~8 часов

---

## 3. День 2: OpenAPI каркас

### 🎯 Цель дня

Создать OpenAPI спецификации для всех сервисов, настроить генерацию типов и SDK, обеспечить валидацию спецификаций.

### 📋 Задачи

#### 3.1. Создание структуры OpenAPI (1 час)

**Задачи:**
- [ ] Создать директорию `docs/openapi/`
- [ ] Создать базовые файлы спецификаций для каждого сервиса
- [ ] Настроить общие компоненты (schemas, responses)

**Структура:**
```
docs/openapi/
├── content.yaml          # Content Service API
├── auth.yaml            # Auth Service API
├── token.yaml           # Token Service API
├── referral.yaml        # Referral Service API
└── components/
    ├── schemas.yaml     # Общие схемы
    ├── responses.yaml   # Общие ответы
    └── security.yaml    # Схемы безопасности
```

**Критерии готовности:**
- ✅ Все файлы созданы
- ✅ Базовая структура OpenAPI 3.1 соблюдена
- ✅ Файлы валидны (можно проверить через spectral)

---

#### 3.2. Описание Content Service API (2 часа)

**Задачи:**
- [ ] Описать эндпоинты для стран (`GET /v1/countries`)
- [ ] Описать эндпоинты для городов (`GET /v1/cities`)
- [ ] Описать эндпоинты для мест (`GET /v1/places`)
- [ ] Описать эндпоинты для событий (`GET /v1/events`)
- [ ] Описать эндпоинты для статей (`GET /v1/articles`)
- [ ] Определить схемы данных (Country, City, Place, Event, Article)

**Пример структуры:**
```yaml
openapi: 3.1.0
info:
  title: Content Service API
  version: 1.0.0
paths:
  /v1/countries:
    get:
      summary: Получить список стран
      responses:
        '200':
          description: Список стран
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Country'
components:
  schemas:
    Country:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        code:
          type: string
        citiesCount:
          type: integer
        placesCount:
          type: integer
```

**Критерии готовности:**
- ✅ Все публичные эндпоинты описаны
- ✅ Схемы данных определены
- ✅ Примеры запросов/ответов добавлены
- ✅ Валидация через spectral проходит

---

#### 3.3. Описание Auth Service API (1 час)

**Задачи:**
- [ ] Описать эндпоинт профиля (`GET /v1/profile`)
- [ ] Описать эндпоинт обновления профиля (`PATCH /v1/profile`)
- [ ] Описать webhook эндпоинт для Clerk (`POST /v1/webhooks/clerk`)
- [ ] Определить схемы (User, Profile, WebhookEvent)

**Критерии готовности:**
- ✅ Все эндпоинты описаны
- ✅ Схемы определены
- ✅ Webhook схема соответствует Clerk формату

---

#### 3.4. Описание Token Service API (1.5 часа)

**Задачи:**
- [ ] Описать эндпоинт баланса (`GET /v1/balance`)
- [ ] Описать эндпоинт транзакций (`GET /v1/transactions`)
- [ ] Описать эндпоинт добавления поинтов (`POST /v1/add`)
- [ ] Описать эндпоинт списания поинтов (`POST /v1/subtract`)
- [ ] Определить схемы (Balance, Transaction, AddPointsRequest)

**Критерии готовности:**
- ✅ Все эндпоинты описаны
- ✅ Схемы определены
- ✅ Правила валидации описаны

---

#### 3.5. Описание Referral Service API (1 час)

**Задачи:**
- [ ] Описать эндпоинт статистики (`GET /v1/stats`)
- [ ] Описать эндпоинт дерева рефералов (`GET /v1/tree`)
- [ ] Описать эндпоинт регистрации реферала (`POST /v1/register`)
- [ ] Определить схемы (ReferralStats, ReferralTree, RegisterRequest)

**Критерии готовности:**
- ✅ Все эндпоинты описаны
- ✅ Схемы определены
- ✅ Логика реферальной программы отражена

---

#### 3.6. Настройка Orval для генерации (1.5 часа)

**Задачи:**
- [ ] Установить Orval
- [ ] Создать `orval.config.ts`
- [ ] Настроить генерацию типов в `packages/types/`
- [ ] Настроить генерацию SDK в `packages/sdk/`
- [ ] Добавить скрипты генерации

**Конфигурация `orval.config.ts`:**
```typescript
import { defineConfig } from 'orval';

export default defineConfig({
  content: {
    input: './docs/openapi/content.yaml',
    output: {
      target: './packages/sdk/src/content.ts',
      client: 'react-query',
      mode: 'tags-split',
    },
    types: {
      output: './packages/types/src/content.ts',
    },
  },
  auth: {
    input: './docs/openapi/auth.yaml',
    output: {
      target: './packages/sdk/src/auth.ts',
      client: 'react-query',
      mode: 'tags-split',
    },
    types: {
      output: './packages/types/src/auth.ts',
    },
  },
  // ... другие сервисы
});
```

**Скрипты в корневом `package.json`:**
```json
{
  "scripts": {
    "gen:types": "orval --types",
    "gen:sdk": "orval --sdk",
    "gen:all": "pnpm gen:types && pnpm gen:sdk"
  }
}
```

**Критерии готовности:**
- ✅ Orval установлен и настроен
- ✅ Генерация типов работает
- ✅ Генерация SDK работает
- ✅ Типы и SDK соответствуют OpenAPI спецификациям

---

#### 3.7. Настройка валидации OpenAPI (0.5 часа)

**Задачи:**
- [ ] Установить Spectral CLI
- [ ] Создать `spectral.yaml` конфигурацию
- [ ] Добавить скрипт валидации в CI
- [ ] Протестировать валидацию

**Конфигурация `spectral.yaml`:**
```yaml
extends: ["spectral:oas"]
rules:
  operation-operationId: error
  operation-tags: error
  operation-description: warn
  tag-description: warn
```

**Скрипт валидации:**
```json
{
  "scripts": {
    "validate:openapi": "spectral lint docs/openapi/**/*.yaml"
  }
}
```

**Критерии готовности:**
- ✅ Spectral установлен
- ✅ Валидация работает
- ✅ Все спецификации проходят валидацию

---

### ✅ Критерии завершения Дня 2

- [ ] Все OpenAPI спецификации созданы
- [ ] Все публичные эндпоинты описаны
- [ ] Типы генерируются автоматически
- [ ] SDK генерируется автоматически
- [ ] Валидация OpenAPI настроена и работает
- [ ] CI проверяет соответствие типов

**Время выполнения:** ~8 часов

---

## 4. День 3: Безопасность и логирование

### 🎯 Цель дня

Настроить безопасность (JWT, валидация), создать единый логгер с трассировкой, реализовать health/ready эндпоинты.

### 📋 Задачи

#### 4.1. Создание пакета logger (1.5 часа)

**Задачи:**
- [ ] Создать `packages/logger/` пакет
- [ ] Реализовать генерацию requestId
- [ ] Реализовать функции логирования
- [ ] Добавить поддержку структурированных логов
- [ ] Экспортировать типы и функции

**Структура:**
```
packages/logger/
├── package.json
├── src/
│   ├── index.ts
│   ├── request-id.ts
│   └── logger.ts
└── tsconfig.json
```

**Пример реализации:**
```typescript
// packages/logger/src/request-id.ts
export function generateRequestId(): string {
  return crypto.randomUUID();
}

// packages/logger/src/logger.ts
export interface LogContext {
  requestId?: string;
  userId?: string;
  [key: string]: unknown;
}

export function logInfo(message: string, context?: LogContext) {
  console.log(JSON.stringify({
    level: 'info',
    message,
    timestamp: new Date().toISOString(),
    ...context,
  }));
}

export function logError(message: string, error: Error, context?: LogContext) {
  console.error(JSON.stringify({
    level: 'error',
    message,
    error: {
      name: error.name,
      message: error.message,
      stack: error.stack,
    },
    timestamp: new Date().toISOString(),
    ...context,
  }));
}
```

**Критерии готовности:**
- ✅ Пакет создан и экспортирует функции
- ✅ RequestId генерируется корректно
- ✅ Логи структурированы (JSON)
- ✅ Пакет используется в Gateway

---

#### 4.2. Настройка jose для JWT (1.5 часа)

**Задачи:**
- [ ] Установить `jose` в необходимые пакеты
- [ ] Создать утилиты для работы с JWT
- [ ] Реализовать верификацию JWT
- [ ] Реализовать подпись JWT
- [ ] Запретить другие JWT библиотеки через pnpm overrides

**Структура:**
```
packages/logger/
├── src/
│   └── jwt.ts
```

**Пример реализации:**
```typescript
// packages/logger/src/jwt.ts
import * as jose from 'jose';

export async function verifyJWT(token: string, secret: string) {
  const secretKey = new TextEncoder().encode(secret);
  const { payload } = await jose.jwtVerify(token, secretKey);
  return payload;
}

export async function signJWT(payload: object, secret: string, expiresIn = '2h') {
  const secretKey = new TextEncoder().encode(secret);
  return await new jose.SignJWT(payload)
    .setProtectedHeader({ alg: 'HS256' })
    .setIssuedAt()
    .setExpirationTime(expiresIn)
    .sign(secretKey);
}
```

**Настройка pnpm overrides:**
```json
// package.json (корневой)
{
  "pnpm": {
    "overrides": {
      "jsonwebtoken": "npm:jose@latest",
      "@tsndr/cloudflare-worker-jwt": "npm:jose@latest"
    }
  }
}
```

**Критерии готовности:**
- ✅ jose установлен
- ✅ Утилиты для JWT работают
- ✅ Другие JWT библиотеки запрещены
- ✅ CI проверяет отсутствие других JWT пакетов

---

#### 4.3. Внедрение Zod-валидации в Gateway (2 часа)

**Задачи:**
- [ ] Установить Zod в Gateway
- [ ] Создать схемы валидации для запросов
- [ ] Реализовать middleware для валидации
- [ ] Добавить обработку ошибок валидации

**Пример middleware:**
```typescript
// services/api-gateway/src/middleware/validation.ts
import { z } from 'zod';
import { Context, Next } from 'hono';

export function validateBody<T extends z.ZodType>(schema: T) {
  return async (c: Context, next: Next) => {
    try {
      const body = await c.req.json();
      const validated = schema.parse(body);
      c.set('validatedBody', validated);
      await next();
    } catch (error) {
      if (error instanceof z.ZodError) {
        return c.json({
          error: 'Validation failed',
          details: error.errors,
        }, 400);
      }
      throw error;
    }
  };
}
```

**Критерии готовности:**
- ✅ Zod установлен
- ✅ Middleware валидации работает
- ✅ Ошибки валидации обрабатываются корректно
- ✅ Примеры использования добавлены

---

#### 4.4. Реализация /health и /ready эндпоинтов (1 час)

**Задачи:**
- [ ] Реализовать `/health` во всех сервисах
- [ ] Реализовать `/ready` во всех сервисах
- [ ] Добавить проверку подключения к БД в `/ready`
- [ ] Добавить метаданные в ответы

**Пример реализации:**
```typescript
// services/api-gateway/src/routes/health.ts
import { Hono } from 'hono';

const app = new Hono();

app.get('/health', (c) => {
  return c.json({
    status: 'ok',
    timestamp: new Date().toISOString(),
    service: 'api-gateway',
  });
});

app.get('/ready', async (c) => {
  // Проверка подключения к БД (если есть)
  try {
    // await db.query('SELECT 1');
    return c.json({
      status: 'ready',
      timestamp: new Date().toISOString(),
    });
  } catch (error) {
    return c.json({
      status: 'not ready',
      error: error instanceof Error ? error.message : 'Unknown error',
    }, 503);
  }
});

export default app;
```

**Критерии готовности:**
- ✅ `/health` работает во всех сервисах
- ✅ `/ready` работает во всех сервисах
- ✅ Проверка БД реализована где необходимо
- ✅ Ответы соответствуют формату

---

#### 4.5. Настройка сквозной трассировки (1 час)

**Задачи:**
- [ ] Реализовать middleware для requestId в Gateway
- [ ] Передавать requestId в заголовках запросов
- [ ] Логировать requestId во всех запросах
- [ ] Добавить requestId в ответы API

**Пример middleware:**
```typescript
// services/api-gateway/src/middleware/request-id.ts
import { Context, Next } from 'hono';
import { generateRequestId } from '@go2asia/logger';

export async function requestIdMiddleware(c: Context, next: Next) {
  const requestId = c.req.header('X-Request-Id') || generateRequestId();
  c.set('requestId', requestId);
  c.header('X-Request-Id', requestId);
  await next();
}
```

**Критерии готовности:**
- ✅ RequestId генерируется для каждого запроса
- ✅ RequestId передаётся в заголовках
- ✅ RequestId логируется во всех запросах
- ✅ RequestId возвращается в ответах

---

### ✅ Критерии завершения Дня 3

- [ ] Пакет logger создан и работает
- [ ] jose настроен как единственная JWT библиотека
- [ ] Zod-валидация работает в Gateway
- [ ] `/health` и `/ready` реализованы во всех сервисах
- [ ] Сквозная трассировка работает
- [ ] Все логи содержат requestId

**Время выполнения:** ~7 часов

---

## 5. День 4: CI/CD Pipeline

### 🎯 Цель дня

Настроить полноценный CI/CD pipeline с проверками, тестами и автоматическим деплоем.

### 📋 Задачи

#### 5.1. Настройка GitHub Actions для PR (2 часа)

**Задачи:**
- [ ] Создать `.github/workflows/pr-checks.yml`
- [ ] Настроить проверку линтинга
- [ ] Настроить проверку типов
- [ ] Настроить сборку
- [ ] Настроить валидацию OpenAPI
- [ ] Настроить генерацию и проверку типов/SDK

**Пример workflow:**
```yaml
name: PR Checks

on: [pull_request]

jobs:
  lint-and-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v2
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'
      
      - run: pnpm install
      - run: pnpm lint
      - run: pnpm typecheck
      - run: pnpm build

  validate-openapi:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          npm install -g @stoplight/spectral-cli
          spectral lint docs/openapi/**/*.yaml

  generate-and-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v2
      - run: pnpm install
      - run: pnpm gen:all
      - run: |
          if [ -n "$(git diff --exit-code)" ]; then
            echo "Generated types/SDK differ from committed files"
            exit 1
          fi
```

**Критерии готовности:**
- ✅ Workflow создан и работает
- ✅ Все проверки выполняются
- ✅ PR блокируется при ошибках

---

#### 5.2. Настройка деплоя превью (1.5 часа)

**Задачи:**
- [ ] Настроить Netlify для превью деплоев
- [ ] Настроить Cloudflare для превью Workers
- [ ] Добавить шаги деплоя в workflow
- [ ] Настроить переменные окружения для превью

**Пример workflow:**
```yaml
deploy-preview:
  runs-on: ubuntu-latest
  needs: [lint-and-build]
  steps:
    - uses: actions/checkout@v4
    - uses: pnpm/action-setup@v2
    - run: pnpm install
    - run: pnpm build
    
    # Deploy to Netlify
    - uses: netlify/actions/cli@master
      with:
        args: deploy --dir=apps/go2asia-pwa-shell/.next --prod=false
      env:
        NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
        NETLIFY_SITE_ID: ${{ secrets.NETLIFY_SITE_ID }}
    
    # Deploy to Cloudflare Workers
    - uses: cloudflare/wrangler-action@v3
      with:
        apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
        accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        workingDirectory: services/api-gateway
        command: deploy --env preview
```

**Критерии готовности:**
- ✅ Превью деплои работают
- ✅ URL превью доступен в PR
- ✅ Переменные окружения настроены

---

#### 5.3. Настройка contract-тестов (1.5 часа)

**Задачи:**
- [ ] Установить Schemathesis
- [ ] Создать скрипт для contract-тестов
- [ ] Добавить шаг в CI для запуска тестов
- [ ] Настроить запуск на превью-деплое

**Пример скрипта:**
```bash
#!/bin/bash
# scripts/contract-tests.sh

PREVIEW_URL=$1

if [ -z "$PREVIEW_URL" ]; then
  echo "Usage: $0 <preview-url>"
  exit 1
fi

pip install schemathesis

schemathesis run \
  --base-url "$PREVIEW_URL" \
  --checks all \
  docs/openapi/content.yaml \
  docs/openapi/auth.yaml \
  docs/openapi/token.yaml \
  docs/openapi/referral.yaml
```

**Критерии готовности:**
- ✅ Schemathesis установлен
- ✅ Contract-тесты запускаются
- ✅ Тесты выполняются в CI

---

#### 5.4. Настройка E2E тестов (1.5 часа)

**Задачи:**
- [ ] Установить Playwright
- [ ] Создать базовые E2E тесты
- [ ] Настроить запуск в CI
- [ ] Добавить скрипты для тестов

**Пример теста:**
```typescript
// apps/go2asia-pwa-shell/e2e/home.spec.ts
import { test, expect } from '@playwright/test';

test('home page loads', async ({ page }) => {
  await page.goto('/');
  await expect(page).toHaveTitle(/Go2Asia/);
});
```

**Критерии готовности:**
- ✅ Playwright установлен
- ✅ Базовые тесты созданы
- ✅ Тесты запускаются в CI

---

#### 5.5. Настройка деплоя в staging (1 час)

**Задачи:**
- [ ] Создать workflow для деплоя в staging
- [ ] Настроить автоматический деплой при merge в main
- [ ] Добавить smoke-тесты после деплоя
- [ ] Настроить rollback при ошибках

**Пример workflow:**
```yaml
name: Deploy to Staging

on:
  push:
    branches: [main]

jobs:
  deploy-staging:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v2
      - run: pnpm install
      - run: pnpm build
      - run: pnpm db:migrate:staging
      - run: pnpm deploy:staging
      - run: pnpm test:smoke:staging
```

**Критерии готовности:**
- ✅ Деплой в staging работает автоматически
- ✅ Smoke-тесты выполняются
- ✅ Rollback настроен

---

#### 5.6. Настройка ручного деплоя в prod (0.5 часа)

**Задачи:**
- [ ] Создать workflow для ручного деплоя
- [ ] Настроить workflow_dispatch
- [ ] Добавить проверки перед деплоем
- [ ] Настроить rollback

**Пример workflow:**
```yaml
name: Promote to Production

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to deploy'
        required: true

jobs:
  deploy-prod:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: pnpm install
      - run: pnpm build
      - run: pnpm db:migrate:prod
      - run: pnpm deploy:prod
      - run: pnpm test:smoke:prod
      - run: |
          if [ $? -ne 0 ]; then
            pnpm db:rollback:prod
            pnpm deploy:rollback:prod
          fi
```

**Критерии готовности:**
- ✅ Ручной деплой работает
- ✅ Проверки выполняются
- ✅ Rollback настроен

---

### ✅ Критерии завершения Дня 4

- [ ] PR workflow работает полностью
- [ ] Превью деплои настроены
- [ ] Contract-тесты работают
- [ ] E2E тесты работают
- [ ] Деплой в staging автоматический
- [ ] Деплой в prod ручной с проверками

**Время выполнения:** ~8 часов

---

## 6. День 5: Neon и данные

### 🎯 Цель дня

Настроить базу данных Neon, миграции, семена, бэкапы и процедуры восстановления.

### 📋 Задачи

#### 6.1. Настройка Neon окружений (1 час)

**Задачи:**
- [ ] Создать Neon проект для staging
- [ ] Создать Neon проект для production
- [ ] Настроить подключения
- [ ] Сохранить connection strings в секретах

**Критерии готовности:**
- ✅ Оба окружения созданы
- ✅ Connection strings сохранены
- ✅ Доступ настроен

---

#### 6.2. Настройка Drizzle Kit для миграций (2 часа)

**Задачи:**
- [ ] Установить Drizzle Kit в сервисы
- [ ] Создать `drizzle.config.ts` для каждого сервиса
- [ ] Настроить схемы данных (schema.ts)
- [ ] Создать первую миграцию
- [ ] Настроить команды миграции

**Структура:**
```
services/content-service/
├── drizzle.config.ts
├── src/
│   └── db/
│       ├── schema.ts
│       └── index.ts
└── migrations/
    └── 0000_initial.sql
```

**Пример `drizzle.config.ts`:**
```typescript
import type { Config } from 'drizzle-kit';

export default {
  schema: './src/db/schema.ts',
  out: './migrations',
  driver: 'pg',
  dbCredentials: {
    connectionString: process.env.DATABASE_URL!,
  },
} satisfies Config;
```

**Команды:**
```json
{
  "scripts": {
    "db:migrate:generate": "drizzle-kit generate",
    "db:migrate:up": "drizzle-kit migrate",
    "db:migrate:down": "drizzle-kit drop",
    "db:migrate:status": "drizzle-kit check"
  }
}
```

**Критерии готовности:**
- ✅ Drizzle Kit настроен
- ✅ Схемы созданы
- ✅ Миграции генерируются
- ✅ Миграции применяются

---

#### 6.3. Создание seed файлов (2 часа)

**Задачи:**
- [ ] Создать seed файлы для Content Service
- [ ] Создать seed файлы для Token Service
- [ ] Создать seed файлы для Referral Service
- [ ] Проверить UTF-8 кодировку
- [ ] Сделать seed файлы идемпотентными

**Структура:**
```
services/content-service/
└── seeds/
    ├── countries.sql
    ├── cities.sql
    └── places.sql
```

**Пример seed файла:**
```sql
-- services/content-service/seeds/countries.sql
INSERT INTO countries (id, name, code, created_at)
VALUES 
  ('th', 'Thailand', 'TH', NOW()),
  ('id', 'Indonesia', 'ID', NOW())
ON CONFLICT (id) DO NOTHING;
```

**Критерии готовности:**
- ✅ Seed файлы созданы
- ✅ UTF-8 кодировка проверена
- ✅ Файлы идемпотентны
- ✅ CI проверяет кодировку

---

#### 6.4. Настройка PITR бэкапов (1 час)

**Задачи:**
- [ ] Включить Point-in-Time Recovery в Neon
- [ ] Настроить автоматические бэкапы
- [ ] Настроить хранение бэкапов (30 дней)
- [ ] Документировать процесс восстановления

**Критерии готовности:**
- ✅ PITR включен
- ✅ Автоматические бэкапы работают
- ✅ Документация создана

---

#### 6.5. Создание rollback playbook (1 час)

**Задачи:**
- [ ] Создать документ `docs/ops/DATABASE_MIGRATIONS.md`
- [ ] Описать процесс rollback
- [ ] Создать скрипты для rollback
- [ ] Протестировать rollback на staging

**Критерии готовности:**
- ✅ Документация создана
- ✅ Скрипты rollback работают
- ✅ Rollback протестирован

---

#### 6.6. Тестирование восстановления (1 час)

**Задачи:**
- [ ] Протестировать восстановление из бэкапа на staging
- [ ] Проверить целостность данных
- [ ] Документировать результаты

**Критерии готовности:**
- ✅ Восстановление протестировано
- ✅ Данные целостны
- ✅ Процесс документирован

---

### ✅ Критерии завершения Дня 5

- [ ] Neon окружения настроены
- [ ] Drizzle Kit настроен и работает
- [ ] Миграции создаются и применяются
- [ ] Seed файлы созданы и проверены
- [ ] Бэкапы настроены
- [ ] Rollback протестирован

**Время выполнения:** ~8 часов

---

## 7. День 6: Кэширование и алёрты

### 🎯 Цель дня

Реализовать кэш-политику, настроить мониторинг и алёрты.

### 📋 Задачи

#### 7.1. Реализация кэш-матрицы в Gateway (2 часа)

**Задачи:**
- [ ] Реализовать установку Cache-Control заголовков
- [ ] Настроить TTL для публичных эндпоинтов
- [ ] Настроить no-store для приватных эндпоинтов
- [ ] Добавить Vary заголовки

**Пример реализации:**
```typescript
// services/api-gateway/src/utils/cache.ts
export function setCacheHeaders(
  response: Response,
  isPublic: boolean,
  ttl: number = 300
) {
  if (isPublic) {
    response.headers.set(
      'Cache-Control',
      `public, s-maxage=${ttl}, stale-while-revalidate=${ttl}`
    );
    response.headers.set('Vary', 'Accept, Accept-Encoding');
  } else {
    response.headers.set('Cache-Control', 'no-store, no-cache, must-revalidate');
    response.headers.set('Pragma', 'no-cache');
    response.headers.set('Expires', '0');
  }
}
```

**Критерии готовности:**
- ✅ Заголовки устанавливаются корректно
- ✅ Публичные эндпоинты кешируются
- ✅ Приватные эндпоинты не кешируются

---

#### 7.2. Написание тестов для кэша (1 час)

**Задачи:**
- [ ] Создать тесты для проверки заголовков
- [ ] Проверить публичные эндпоинты
- [ ] Проверить приватные эндпоинты
- [ ] Добавить тесты в CI

**Пример теста:**
```typescript
// tests/cache-headers.test.ts
import { test, expect } from 'vitest';

test('public endpoints have cache headers', async () => {
  const response = await fetch('/v1/api/content/countries');
  expect(response.headers.get('Cache-Control')).toContain('s-maxage=600');
});

test('private endpoints have no-store', async () => {
  const response = await fetch('/v1/api/token/balance', {
    headers: { Authorization: `Bearer ${token}` }
  });
  expect(response.headers.get('Cache-Control')).toContain('no-store');
});
```

**Критерии готовности:**
- ✅ Тесты созданы
- ✅ Тесты проходят
- ✅ Тесты в CI

---

#### 7.3. Настройка Cloudflare Alerts (2 часа)

**Задачи:**
- [ ] Настроить алерт на error rate > 1%
- [ ] Настроить алерт на latency p95 > порога
- [ ] Настроить алерт на availability < 99%
- [ ] Настроить уведомления (email/Slack)

**Критерии готовности:**
- ✅ Алёрты настроены
- ✅ Уведомления работают
- ✅ Пороги установлены корректно

---

#### 7.4. Создание базовых runbooks (2 часа)

**Задачи:**
- [ ] Создать runbook для ошибок 5xx
- [ ] Создать runbook для всплеска латентности
- [ ] Создать runbook для ошибок Clerk Webhook
- [ ] Создать runbook для проблем с БД

**Структура:**
```
docs/ops/RUNBOOKS.md
```

**Критерии готовности:**
- ✅ Runbooks созданы
- ✅ Runbooks содержат пошаговые инструкции
- ✅ Runbooks доступны команде

---

#### 7.5. Настройка дашбордов (1 час)

**Задачи:**
- [ ] Создать дашборд в Cloudflare Analytics
- [ ] Добавить метрики latency per endpoint
- [ ] Добавить метрики hits/errors
- [ ] Настроить визуализацию

**Критерии готовности:**
- ✅ Дашборды созданы
- ✅ Метрики отображаются
- ✅ Дашборды доступны команде

---

### ✅ Критерии завершения Дня 6

- [ ] Кэш-политика реализована
- [ ] Тесты кэша работают
- [ ] Алёрты настроены
- [ ] Runbooks созданы
- [ ] Дашборды настроены

**Время выполнения:** ~8 часов

---

## 8. День 7: Фронт и финализация

### 🎯 Цель дня

Реализовать SSR/SSG для публичных страниц, настроить SEO, провести финальную проверку.

### 📋 Задачи

#### 8.1. Реализация SSR для публичных страниц (2 часа)

**Задачи:**
- [ ] Реализовать SSR для страниц Atlas
- [ ] Реализовать SSR для страниц Blog
- [ ] Реализовать SSR для страниц Pulse
- [ ] Настроить генерацию метаданных

**Пример:**
```typescript
// app/(public)/atlas/countries/[id]/page.tsx
export async function generateMetadata({ params }: { params: { id: string } }) {
  const country = await fetchCountry(params.id);
  return {
    title: `${country.name} - Go2Asia Atlas`,
    description: country.description,
    openGraph: {
      title: country.name,
      description: country.description,
      images: [country.flag],
    },
  };
}

export default async function CountryPage({ params }: { params: { id: string } }) {
  const country = await fetchCountry(params.id);
  return <CountryDetails country={country} />;
}
```

**Критерии готовности:**
- ✅ SSR работает для всех публичных страниц
- ✅ Метаданные генерируются
- ✅ Страницы загружаются быстро

---

#### 8.2. Реализация SSG для статических страниц (1 час)

**Задачи:**
- [ ] Настроить SSG для списков стран
- [ ] Настроить SSG для списков городов
- [ ] Настроить revalidation
- [ ] Оптимизировать сборку

**Пример:**
```typescript
// app/(public)/atlas/countries/page.tsx
export const revalidate = 3600; // Revalidate every hour

export default async function CountriesPage() {
  const countries = await fetchCountries();
  return <CountriesList countries={countries} />;
}
```

**Критерии готовности:**
- ✅ SSG работает
- ✅ Revalidation настроен
- ✅ Страницы статически генерируются

---

#### 8.3. Настройка SEO компонентов (1.5 часа)

**Задачи:**
- [ ] Создать `app/sitemap.ts`
- [ ] Создать `app/robots.ts`
- [ ] Настроить Open Graph метаданные
- [ ] Добавить структурированные данные (JSON-LD)

**Пример sitemap:**
```typescript
// app/sitemap.ts
export default async function sitemap() {
  const countries = await fetchCountries();
  const articles = await fetchArticles();
  
  return [
    ...countries.map((country) => ({
      url: `https://go2asia.space/atlas/countries/${country.id}`,
      lastModified: country.updatedAt,
    })),
    ...articles.map((article) => ({
      url: `https://go2asia.space/blog/${article.slug}`,
      lastModified: article.publishedAt,
    })),
  ];
}
```

**Критерии готовности:**
- ✅ Sitemap генерируется
- ✅ Robots.txt настроен
- ✅ Open Graph работает
- ✅ Структурированные данные добавлены

---

#### 8.4. Настройка Skeleton UI и обработки ошибок (1 час)

**Задачи:**
- [ ] Создать Skeleton компоненты
- [ ] Настроить `app/error.tsx`
- [ ] Настроить `app/not-found.tsx`
- [ ] Добавить обработку ошибок загрузки

**Пример:**
```typescript
// app/error.tsx
'use client';

export default function Error({
  error,
  reset,
}: {
  error: Error;
  reset: () => void;
}) {
  return (
    <div>
      <h2>Что-то пошло не так</h2>
      <button onClick={reset}>Попробовать снова</button>
    </div>
  );
}
```

**Критерии готовности:**
- ✅ Skeleton UI работает
- ✅ Ошибки обрабатываются
- ✅ UX улучшен

---

#### 8.5. Финальная проверка и Lighthouse (1.5 часа)

**Задачи:**
- [ ] Запустить Lighthouse CI
- [ ] Проверить все exit-критерии
- [ ] Запустить smoke-тесты
- [ ] Запустить E2E тесты
- [ ] Проверить метрики производительности

**Критерии Lighthouse:**
- Performance ≥ 85
- SEO ≥ 85
- Best Practices ≥ 85
- Accessibility ≥ 85

**Критерии готовности:**
- ✅ Lighthouse проходит все проверки
- ✅ Все exit-критерии выполнены
- ✅ Тесты проходят
- ✅ Метрики в норме

---

#### 8.6. Документирование и финализация (1 час)

**Задачи:**
- [ ] Обновить документацию
- [ ] Создать итоговый отчёт
- [ ] Обновить чек-лист прогресса
- [ ] Подготовить переход к Фазе 1

**Критерии готовности:**
- ✅ Документация обновлена
- ✅ Отчёт создан
- ✅ Чек-лист заполнен
- ✅ Готовность к Фазе 1 подтверждена

---

### ✅ Критерии завершения Дня 7

- [ ] SSR/SSG реализованы
- [ ] SEO компоненты настроены
- [ ] Skeleton UI и обработка ошибок работают
- [ ] Lighthouse проходит все проверки
- [ ] Все exit-критерии выполнены
- [ ] Документация обновлена

**Время выполнения:** ~8 часов

---

## 9. Критерии готовности

### ✅ Общие критерии Фазы 0

#### OpenAPI и генерация
- [ ] Все публичные маршруты описаны в OpenAPI
- [ ] Типы генерируются автоматически (`pnpm gen:types`)
- [ ] SDK генерируется автоматически (`pnpm gen:sdk`)
- [ ] CI проверяет соответствие типов
- [ ] OpenAPI спецификации валидны (spectral)

#### CI/CD
- [ ] PR pipeline работает (lint, build, validate, contract, E2E)
- [ ] Превью-деплои на каждую ветку
- [ ] Staging окружение настроено
- [ ] Prod окружение настроено (ручной promote)
- [ ] Smoke-тесты работают

#### Observability
- [ ] `/health` у всех сервисов
- [ ] `/ready` у всех сервисов
- [ ] RequestId трассировка работает
- [ ] Алёрты Cloudflare настроены
- [ ] Runbooks созданы
- [ ] Дашборды доступны

#### Кэширование
- [ ] Кэш-матрица документирована
- [ ] Публичные GET имеют правильные заголовки
- [ ] Приватные endpoints имеют `no-store`
- [ ] Тесты заголовков проходят

#### Безопасность
- [ ] Только `jose` для JWT (проверено в CI)
- [ ] Zod-валидация на Gateway
- [ ] Rate limiting настроен
- [ ] CORS настроен правильно

#### Данные
- [ ] Миграции работают (`up` и `down`)
- [ ] Seed файлы в UTF-8 (проверено в CI)
- [ ] Бэкапы настроены (PITR)
- [ ] Rollback протестирован на staging

#### Фронт
- [ ] SSR для публичных страниц работает
- [ ] SSG для статических страниц работает
- [ ] Sitemap генерируется автоматически
- [ ] Robots.txt настроен
- [ ] Lighthouse ≥ 85 (Perf/SEO/Best)

---

## 10. Риски и митигация

### 🚨 Риск 1: Несовпадение типов фронт/бэк

**Вероятность:** Средняя  
**Влияние:** Высокое

**Митигация:**
- ✅ OpenAPI-генерация типов обязательна
- ✅ PR падает при расхождении типов
- ✅ CI проверяет, что сгенерированные типы совпадают с коммитом

**План действий:**
1. При обнаружении несоответствия — остановить разработку
2. Обновить OpenAPI спецификацию
3. Регенерировать типы и SDK
4. Исправить код, использующий типы

---

### 🚨 Риск 2: JWT/библиотеки несовместимы

**Вероятность:** Низкая  
**Влияние:** Высокое

**Митигация:**
- ✅ Только `jose` везде
- ✅ Запретить другие JWT пакеты в lock-правилах
- ✅ Проверка в CI

**План действий:**
1. Проверить все зависимости на наличие других JWT библиотек
2. Удалить конфликтующие пакеты
3. Обновить код для использования только jose

---

### 🚨 Риск 3: Кэш "случайно" кеширует приватку

**Вероятность:** Средняя  
**Влияние:** Среднее

**Митигация:**
- ✅ `no-store` по умолчанию в личных маршрутах
- ✅ Автотест заголовков в CI
- ✅ Документированная матрица TTL

**План действий:**
1. Запустить тесты заголовков
2. Проверить все приватные эндпоинты
3. Исправить заголовки где необходимо

---

### 🚨 Риск 4: Семена "съезжают" кодировкой

**Вероятность:** Низкая  
**Влияние:** Среднее

**Митигация:**
- ✅ UTF-8-чекер в CI
- ✅ Миграции и семена строго через SQL-файлы
- ✅ Документировать требования

**План действий:**
1. Проверить кодировку всех SQL файлов
2. Конвертировать в UTF-8 если необходимо
3. Обновить файлы в репозитории

---

### 🚨 Риск 5: Незаметные 5xx

**Вероятность:** Средняя  
**Влияние:** Высокое

**Митигация:**
- ✅ Алёрты + SLO/SLI панели с первого дня
- ✅ Правило: без dashboards в prod — не деплоим
- ✅ Runbooks для типичных инцидентов

**План действий:**
1. Проверить настройку алёртов
2. Убедиться, что дашборды доступны
3. Протестировать алёрты на staging

---

### 🚨 Риск 6: Проблемы с миграциями БД

**Вероятность:** Средняя  
**Влияние:** Высокое

**Митигация:**
- ✅ Все миграции обратимы
- ✅ Тестирование на staging перед prod
- ✅ Rollback план готов

**План действий:**
1. Остановить деплой
2. Выполнить rollback миграции
3. Проверить целостность данных
4. Исправить миграцию и повторить

---

## 11. Чек-лист прогресса

### Неделя 1

#### День 1: Инициализация
- [ ] Монорепо инициализирован
- [ ] TypeScript конфиги настроены
- [ ] Next.js PWA Shell создан
- [ ] API Gateway создан
- [ ] Линтеры настроены

#### День 2: OpenAPI
- [ ] OpenAPI спецификации созданы
- [ ] Orval настроен
- [ ] Генерация типов работает
- [ ] Генерация SDK работает
- [ ] Валидация настроена

#### День 3: Безопасность
- [ ] Logger пакет создан
- [ ] jose настроен
- [ ] Zod-валидация работает
- [ ] Health/ready эндпоинты работают
- [ ] Трассировка настроена

#### День 4: CI/CD
- [ ] PR workflow работает
- [ ] Превью деплои настроены
- [ ] Contract-тесты работают
- [ ] E2E тесты работают
- [ ] Staging деплой настроен

#### День 5: Данные
- [ ] Neon окружения настроены
- [ ] Drizzle Kit настроен
- [ ] Миграции работают
- [ ] Seed файлы созданы
- [ ] Бэкапы настроены

#### День 6: Кэш и алёрты
- [ ] Кэш-политика реализована
- [ ] Тесты кэша работают
- [ ] Алёрты настроены
- [ ] Runbooks созданы
- [ ] Дашборды настроены

#### День 7: Фронт
- [ ] SSR реализован
- [ ] SSG реализован
- [ ] SEO компоненты настроены
- [ ] Lighthouse проходит
- [ ] Документация обновлена

### Финальная проверка

- [ ] Все exit-критерии выполнены
- [ ] Все типичные проблемы предотвращены
- [ ] Документация обновлена
- [ ] Команда обучена новым процессам
- [ ] Готовность к Фазе 1 подтверждена

---

## 📊 Метрики прогресса

### Общий прогресс: 0%

**Разбивка по категориям:**
- Структура и инфраструктура: 0%
- OpenAPI: 0%
- CI/CD: 0%
- Observability: 0%
- Безопасность: 0%
- Кэширование: 0%
- Данные: 0%
- Фронт: 0%

---

## 🎯 Следующие шаги после Фазы 0

После завершения Фазы 0 можно переходить к:

1. **Фаза 1:** Разработка сервисов (Auth, Content, Token, Referral)
2. **Интеграция:** Подключение фронтенда к API
3. **Тестирование:** Расширение тестового покрытия
4. **Масштабирование:** Оптимизация производительности

---

**Дата создания:** 2025-11-14  
**Автор:** AI Assistant (Composer)  
**Версия:** 1.0

