# 📋 Подробный план завершения Фазы 1

**Дата создания:** 01 декабря 2025  
**Статус:** Активный план  
**Версия:** 1.0  
**Текущий прогресс:** ~38% завершено

---

## 📊 Текущее состояние

| Модуль | UI | API интеграция | Общий прогресс |
|--------|----|----------------|----------------|
| **Atlas Asia** | ~70% | ~15% | ~42% |
| **Pulse Asia** | ~60% | ~10% | ~35% |
| **Blog Asia** | ~65% | ~10% | ~37% |
| **ИТОГО** | ~65% | ~12% | ~38% |

**Что готово:**
- ✅ Backend API (Content Service) — 100%
- ✅ SDK hooks сгенерированы — 100%
- ✅ UI компоненты созданы — ~65%
- ⚠️ Интеграция UI с API — ~12%

**Что осталось:**
- ❌ Интеграция всех страниц с API — ~88%
- ❌ Обработка ошибок и состояний загрузки — ~20%
- ❌ SEO оптимизация — 0%
- ❌ Удаление всех моков — 0%

---

## 🎯 Цель плана

Завершить Фазу 1 за **4 недели**, интегрировав все UI компоненты с реальным API, убрав все моки и обеспечив production-ready состояние всех контентных модулей.

---

## 📅 Общий план по неделям

| Неделя | Модуль | Задачи | Результат |
|--------|--------|--------|-----------|
| **Неделя 1** | Atlas Asia | Интеграция всех страниц Atlas | Atlas работает без моков |
| **Неделя 2** | Pulse Asia | Интеграция календаря и событий | Pulse работает без моков |
| **Неделя 3** | Blog Asia | Интеграция статей и категорий | Blog работает без моков |
| **Неделя 4** | Общие задачи | SEO, ошибки, тестирование | Production-ready состояние |

---

## 📖 Неделя 1: Atlas Asia интеграция

**Цель:** Подключить все страницы Atlas Asia к Content Service API, убрать все моки.

### День 1-2: Главная страница и списки

#### Задача 1.1: Главная страница Atlas (`/atlas/page.tsx`)

**Текущее состояние:**
- ✅ Частично подключён `useGetCountries()` для списка стран
- ✅ Частично подключён `useGetPlaces()` для популярных мест
- ❌ Нет загрузки последних гайдов
- ❌ Нет загрузки ближайших событий
- ❌ Слабая обработка ошибок

**Задачи:**
- [ ] Добавить загрузку последних гайдов через `useGetArticles({ type: 'guide', limit: 3 })`
- [ ] Добавить загрузку ближайших событий через `useGetEvents({ limit: 5 })`
- [ ] Улучшить обработку ошибок (try-catch, fallback UI)
- [ ] Добавить состояния загрузки (Skeleton компоненты)
- [ ] Добавить обработку пустых состояний

**Файлы для изменения:**
- `apps/go2asia-pwa-shell/app/(public)/atlas/page.tsx`
- `apps/go2asia-pwa-shell/app/(public)/atlas/AtlasHomeClient.tsx`

**SDK hooks:**
```typescript
import { useGetCountries } from '@go2asia/sdk/atlas';
import { useGetPlaces } from '@go2asia/sdk/atlas';
import { useGetArticles } from '@go2asia/sdk/blog';
import { useGetEvents } from '@go2asia/sdk/pulse';
```

---

#### Задача 1.2: Список стран (`/atlas/countries/page.tsx`)

**Текущее состояние:**
- ❌ Использует моки (`mockCountries`)
- ❌ Нет пагинации
- ❌ Нет фильтров
- ❌ Нет поиска

**Задачи:**
- [ ] Заменить моки на `useGetCountries()`
- [ ] Добавить пагинацию через параметры `offset` и `limit`
- [ ] Реализовать фильтрацию по региону через параметр `region`
- [ ] Добавить поиск по названию через параметр `search`
- [ ] Добавить сортировку (по популярности, названию)
- [ ] Добавить состояния загрузки и ошибок
- [ ] Удалить файл с моками после замены

**Файлы для изменения:**
- `apps/go2asia-pwa-shell/app/(public)/atlas/countries/page.tsx`
- Найти и удалить файлы с `mockCountries`

**SDK hooks:**
```typescript
import { useGetCountries } from '@go2asia/sdk/atlas';
```

**Параметры запроса:**
```typescript
{
  offset?: number;
  limit?: number;
  region?: string;
  search?: string;
  sortBy?: 'popularity' | 'name';
}
```

---

#### Задача 1.3: Список городов (`/atlas/cities/page.tsx`)

**Текущее состояние:**
- ❌ Использует моки
- ❌ Нет фильтрации по стране
- ❌ Нет поиска

**Задачи:**
- [ ] Заменить моки на `useGetCities()`
- [ ] Добавить фильтрацию по стране через параметр `countryId`
- [ ] Добавить поиск по названию
- [ ] Добавить пагинацию
- [ ] Добавить состояния загрузки и ошибок
- [ ] Удалить файлы с моками

**Файлы для изменения:**
- `apps/go2asia-pwa-shell/app/(public)/atlas/cities/page.tsx`

**SDK hooks:**
```typescript
import { useGetCities } from '@go2asia/sdk/atlas';
```

---

#### Задача 1.4: Список мест (`/atlas/places/page.tsx`)

**Текущее состояние:**
- ❌ Использует моки (`mockPlaces`)
- ❌ Нет фильтрации
- ❌ Нет поиска

**Задачи:**
- [ ] Заменить моки на `useGetPlaces()`
- [ ] Добавить фильтрацию по городу через `cityId`
- [ ] Добавить фильтрацию по категории через `category`
- [ ] Добавить поиск по названию
- [ ] Добавить пагинацию
- [ ] Добавить состояния загрузки и ошибок
- [ ] Удалить файлы с моками

**Файлы для изменения:**
- `apps/go2asia-pwa-shell/app/(public)/atlas/places/page.tsx`

**SDK hooks:**
```typescript
import { useGetPlaces } from '@go2asia/sdk/atlas';
```

---

#### Задача 1.5: Список гайдов (`/atlas/guides/page.tsx`)

**Текущее состояние:**
- ❌ Использует моки (`mockGuides`)
- ❌ Нет фильтрации
- ❌ Нет поиска

**Задачи:**
- [ ] Заменить моки на `useGetArticles({ type: 'guide' })`
- [ ] Добавить фильтрацию по стране через `countryId`
- [ ] Добавить фильтрацию по городу через `cityId`
- [ ] Добавить фильтрацию по теме через `tags`
- [ ] Добавить поиск по названию
- [ ] Добавить пагинацию
- [ ] Добавить состояния загрузки и ошибок
- [ ] Удалить файлы с моками

**Файлы для изменения:**
- `apps/go2asia-pwa-shell/app/(public)/atlas/guides/page.tsx`

**SDK hooks:**
```typescript
import { useGetArticles } from '@go2asia/sdk/blog';
```

---

### День 3-4: Детальные страницы стран и городов

#### Задача 1.6: Детальная страница страны (`/atlas/countries/[id]/page.tsx`)

**Текущее состояние:**
- ❌ Использует моки в layout
- ❌ Нет обработки ошибок (404, 500)
- ❌ Нет состояний загрузки

**Задачи:**
- [ ] Подключить `useGetCountryById(id)` для основных данных
- [ ] Подключить `useGetCities({ countryId: id })` для списка городов
- [ ] Подключить `useGetPlaces({ countryId: id })` для популярных мест
- [ ] Добавить обработку ошибок (404 → notFound(), 500 → error page)
- [ ] Добавить состояния загрузки (Skeleton)
- [ ] Улучшить обработку пустых состояний (нет городов, нет мест)
- [ ] Удалить моки из layout

**Файлы для изменения:**
- `apps/go2asia-pwa-shell/app/(public)/atlas/countries/[id]/page.tsx`
- `apps/go2asia-pwa-shell/app/(public)/atlas/countries/[id]/layout.tsx`

**SDK hooks:**
```typescript
import { useGetCountryById, useGetCities, useGetPlaces } from '@go2asia/sdk/atlas';
```

**Обработка ошибок:**
```typescript
if (error?.status === 404) {
  notFound();
}
```

---

#### Задача 1.7: Детальная страница города (`/atlas/cities/[id]/page.tsx`)

**Текущее состояние:**
- ❌ Использует моки в layout
- ❌ Нет обработки ошибок
- ❌ Нет состояний загрузки

**Задачи:**
- [ ] Подключить `useGetCityById(id)` для основных данных
- [ ] Подключить `useGetPlaces({ cityId: id })` для мест в городе
- [ ] Подключить `useGetEvents({ cityId: id })` для событий в городе
- [ ] Добавить обработку ошибок (404, 500)
- [ ] Добавить состояния загрузки
- [ ] Удалить моки из layout

**Файлы для изменения:**
- `apps/go2asia-pwa-shell/app/(public)/atlas/cities/[id]/page.tsx`
- `apps/go2asia-pwa-shell/app/(public)/atlas/cities/[id]/layout.tsx`

**SDK hooks:**
```typescript
import { useGetCityById, useGetPlaces } from '@go2asia/sdk/atlas';
import { useGetEvents } from '@go2asia/sdk/pulse';
```

---

### День 5: Детальные страницы мест и гайдов

#### Задача 1.8: Детальная страница места (`/atlas/places/[id]/page.tsx`)

**Текущее состояние:**
- ❌ Использует моки в layout
- ❌ Нет обработки ошибок
- ❌ Нет состояний загрузки

**Задачи:**
- [ ] Подключить `useGetPlaceById(id)` для основных данных
- [ ] Подключить связанные гайды через `useGetArticles({ placeId: id })`
- [ ] Добавить обработку ошибок (404, 500)
- [ ] Добавить состояния загрузки
- [ ] Удалить моки из layout

**Файлы для изменения:**
- `apps/go2asia-pwa-shell/app/(public)/atlas/places/[id]/page.tsx`
- `apps/go2asia-pwa-shell/app/(public)/atlas/places/[id]/layout.tsx`

**SDK hooks:**
```typescript
import { useGetPlaceById } from '@go2asia/sdk/atlas';
import { useGetArticles } from '@go2asia/sdk/blog';
```

---

#### Задача 1.9: Детальная страница гайда (`/atlas/guides/[id]/page.tsx`)

**Текущее состояние:**
- ❌ Использует моки в layout
- ❌ Нет обработки ошибок
- ❌ Нет состояний загрузки

**Задачи:**
- [ ] Подключить `useGetArticle(id)` для данных гайда (или `useGetArticleBySlug` если используется slug)
- [ ] Добавить обработку ошибок (404, 500)
- [ ] Добавить состояния загрузки
- [ ] Удалить моки из layout

**Файлы для изменения:**
- `apps/go2asia-pwa-shell/app/(public)/atlas/guides/[id]/page.tsx`
- `apps/go2asia-pwa-shell/app/(public)/atlas/guides/[id]/layout.tsx`

**SDK hooks:**
```typescript
import { useGetArticle } from '@go2asia/sdk/blog';
// или
import { useGetArticleBySlug } from '@go2asia/sdk/blog';
```

---

#### Задача 1.10: Список тем (`/atlas/themes/page.tsx`)

**Текущее состояние:**
- ❌ Нет API для тем (нужно использовать статьи с `type=theme`)

**Задачи:**
- [ ] Подключить `useGetArticles({ type: 'theme' })` для списка тем
- [ ] Добавить пагинацию
- [ ] Добавить фильтрацию и поиск
- [ ] Добавить состояния загрузки и ошибок

**Файлы для изменения:**
- `apps/go2asia-pwa-shell/app/(public)/atlas/themes/page.tsx`

**SDK hooks:**
```typescript
import { useGetArticles } from '@go2asia/sdk/blog';
```

---

#### Задача 1.11: Детальная страница темы (`/atlas/themes/[id]/page.tsx`)

**Текущее состояние:**
- ❌ Использует моки

**Задачи:**
- [ ] Подключить `useGetArticle(id)` для данных темы
- [ ] Подключить связанные места через `useGetPlaces({ tags: [themeTag] })`
- [ ] Подключить связанные гайды через `useGetArticles({ tags: [themeTag] })`
- [ ] Подключить связанные события через `useGetEvents({ tags: [themeTag] })`
- [ ] Добавить обработку ошибок
- [ ] Удалить моки

**Файлы для изменения:**
- `apps/go2asia-pwa-shell/app/(public)/atlas/themes/[id]/page.tsx`
- `apps/go2asia-pwa-shell/app/(public)/atlas/themes/[id]/layout.tsx`

**SDK hooks:**
```typescript
import { useGetArticle } from '@go2asia/sdk/blog';
import { useGetPlaces } from '@go2asia/sdk/atlas';
import { useGetArticles } from '@go2asia/sdk/blog';
import { useGetEvents } from '@go2asia/sdk/pulse';
```

---

### Итоги Недели 1

**Критерии завершения:**
- ✅ Все страницы Atlas подключены к API
- ✅ Все моки удалены из Atlas модуля
- ✅ Обработка ошибок реализована на всех страницах
- ✅ Состояния загрузки добавлены на всех страницах
- ✅ Пагинация работает где необходимо
- ✅ Фильтры и поиск работают через API

**Метрики:**
- Atlas Asia UI: 70% → **100%**
- Atlas Asia API интеграция: 15% → **100%**
- Atlas Asia общий прогресс: 42% → **100%**

---

## 📖 Неделя 2: Pulse Asia интеграция

**Цель:** Подключить все страницы Pulse Asia к Content Service API, реализовать фильтрацию и календарные виды.

### День 1-2: Главная страница и фильтрация

#### Задача 2.1: Главная страница Pulse (`/pulse/page.tsx`)

**Текущее состояние:**
- ✅ Частично подключён `useGetEvents()` для списка событий
- ❌ Фильтрация не работает через параметры запроса
- ❌ Нет пагинации
- ❌ Фильтры не синхронизированы с URL параметрами
- ❌ Слабая обработка ошибок

**Задачи:**
- [ ] Реализовать фильтрацию через параметры запроса API:
  - `countryId` — фильтр по стране
  - `cityId` — фильтр по городу
  - `category` — фильтр по категории
  - `startDate` / `endDate` — фильтр по дате
  - `search` — поиск по названию
- [ ] Синхронизировать фильтры с URL параметрами (уже частично реализовано)
- [ ] Добавить пагинацию через `offset` и `limit`
- [ ] Улучшить обработку ошибок и состояний загрузки
- [ ] Добавить обработку пустых состояний (нет событий)

**Файлы для изменения:**
- `apps/go2asia-pwa-shell/app/(public)/pulse/page.tsx`
- `apps/go2asia-pwa-shell/app/(public)/pulse/PulseClientWrapper.tsx`
- `apps/go2asia-pwa-shell/app/(public)/pulse/PulseClient.tsx`

**SDK hooks:**
```typescript
import { useGetEvents } from '@go2asia/sdk/pulse';
```

**Параметры запроса:**
```typescript
{
  offset?: number;
  limit?: number;
  countryId?: string;
  cityId?: string;
  category?: string;
  startDate?: string; // ISO date
  endDate?: string; // ISO date
  search?: string;
}
```

---

### День 3: Календарные виды

#### Задача 2.2: MonthView — вид месяца

**Текущее состояние:**
- ❌ Использует моки или не фильтрует события по дате

**Задачи:**
- [ ] Группировать события по дням из API данных
- [ ] Фильтровать события по текущему месяцу через параметры `startDate` и `endDate`
- [ ] Добавить загрузку событий при смене месяца
- [ ] Добавить состояния загрузки для каждого дня
- [ ] Оптимизировать запросы (кэширование по месяцу)

**Файлы для изменения:**
- `apps/go2asia-pwa-shell/components/pulse/MonthView.tsx`
- `apps/go2asia-pwa-shell/app/(public)/pulse/PulseClientWrapper.tsx`

---

#### Задача 2.3: WeekView — вид недели

**Текущее состояние:**
- ❌ Использует моки или не фильтрует события по дате

**Задачи:**
- [ ] Фильтровать события по текущей неделе через параметры `startDate` и `endDate`
- [ ] Группировать события по дням недели
- [ ] Добавить загрузку событий при смене недели
- [ ] Добавить состояния загрузки

**Файлы для изменения:**
- `apps/go2asia-pwa-shell/components/pulse/WeekView.tsx`
- `apps/go2asia-pwa-shell/app/(public)/pulse/PulseClientWrapper.tsx`

---

#### Задача 2.4: DayView — вид дня

**Текущее состояние:**
- ❌ Использует моки или не фильтрует события по дате

**Задачи:**
- [ ] Фильтровать события по выбранному дню через параметры `startDate` и `endDate`
- [ ] Группировать события по времени
- [ ] Добавить загрузку событий при смене дня
- [ ] Добавить состояния загрузки

**Файлы для изменения:**
- `apps/go2asia-pwa-shell/components/pulse/DayView.tsx`
- `apps/go2asia-pwa-shell/app/(public)/pulse/PulseClientWrapper.tsx`

---

#### Задача 2.5: AgendaView — вид списка/агенды

**Текущее состояние:**
- ❌ Использует моки или не фильтрует события по дате

**Задачи:**
- [ ] Показывать список событий из API
- [ ] Фильтровать события по выбранному периоду
- [ ] Добавить сортировку по дате
- [ ] Добавить пагинацию для больших списков
- [ ] Добавить состояния загрузки

**Файлы для изменения:**
- `apps/go2asia-pwa-shell/components/pulse/AgendaView.tsx`
- `apps/go2asia-pwa-shell/app/(public)/pulse/PulseClientWrapper.tsx`

---

### День 4: Детальная страница события

#### Задача 2.6: Детальная страница события (`/pulse/[id]/page.tsx`)

**Текущее состояние:**
- ❌ Использует моки
- ❌ Нет обработки ошибок
- ❌ Нет состояний загрузки

**Задачи:**
- [ ] Подключить `useGetEventById(id)` вместо моков
- [ ] Добавить обработку ошибок (404 → notFound(), 500 → error page)
- [ ] Добавить состояния загрузки (Skeleton)
- [ ] Добавить связанные события через `useGetEvents({ category: event.category, limit: 5 })`
- [ ] Улучшить обработку пустых состояний

**Файлы для изменения:**
- `apps/go2asia-pwa-shell/app/(public)/pulse/[id]/page.tsx`

**SDK hooks:**
```typescript
import { useGetEventById, useGetEvents } from '@go2asia/sdk/pulse';
```

**Обработка ошибок:**
```typescript
if (error?.status === 404) {
  notFound();
}
```

---

### День 5: Фильтры и поиск

#### Задача 2.7: Улучшение фильтров

**Текущее состояние:**
- ✅ Фильтры UI реализованы
- ✅ URL синхронизация частично реализована
- ❌ Фильтры не передаются в API запросы

**Задачи:**
- [ ] Связать UI фильтры с параметрами API запроса
- [ ] Реализовать фильтрацию по гео (страна, город)
- [ ] Реализовать фильтрацию по категории
- [ ] Реализовать фильтрацию по цене (бесплатно/платно) — если API поддерживает
- [ ] Реализовать фильтрацию по языку — если API поддерживает
- [ ] Реализовать фильтрацию по времени (сегодня, завтра, выходные)
- [ ] Реализовать поиск по названию через параметр `search`
- [ ] Добавить сброс фильтров
- [ ] Сохранять фильтры в URL для возможности поделиться ссылкой

**Файлы для изменения:**
- `apps/go2asia-pwa-shell/components/pulse/EventFilters.tsx`
- `apps/go2asia-pwa-shell/app/(public)/pulse/PulseClient.tsx`
- `apps/go2asia-pwa-shell/app/(public)/pulse/PulseClientWrapper.tsx`

---

### Итоги Недели 2

**Критерии завершения:**
- ✅ Все страницы Pulse подключены к API
- ✅ Все моки удалены из Pulse модуля
- ✅ Календарные виды работают с реальными данными
- ✅ Фильтрация работает через API параметры
- ✅ Обработка ошибок реализована
- ✅ Состояния загрузки добавлены

**Метрики:**
- Pulse Asia UI: 60% → **100%**
- Pulse Asia API интеграция: 10% → **100%**
- Pulse Asia общий прогресс: 35% → **100%**

---

## 📖 Неделя 3: Blog Asia интеграция

**Цель:** Подключить все страницы Blog Asia к Content Service API, реализовать фильтрацию и пагинацию.

### День 1-2: Главная страница и список статей

#### Задача 3.1: Главная страница Blog (`/blog/page.tsx`)

**Текущее состояние:**
- ✅ Частично подключён `useGetArticles()` для списка статей
- ❌ Нет пагинации
- ❌ Нет фильтрации по категориям через API
- ❌ Нет поиска
- ❌ Слабая обработка ошибок

**Задачи:**
- [ ] Добавить пагинацию через `offset` и `limit`
- [ ] Реализовать фильтрацию по категориям через параметр `category`
- [ ] Реализовать фильтрацию по тегам через параметр `tags`
- [ ] Добавить поиск по названию через параметр `search`
- [ ] Улучшить обработку ошибок и состояний загрузки
- [ ] Добавить обработку пустых состояний

**Файлы для изменения:**
- `apps/go2asia-pwa-shell/app/(public)/blog/page.tsx`
- `apps/go2asia-pwa-shell/app/(public)/blog/BlogClientWrapper.tsx`

**SDK hooks:**
```typescript
import { useGetArticles } from '@go2asia/sdk/blog';
```

**Параметры запроса:**
```typescript
{
  offset?: number;
  limit?: number;
  category?: string;
  tags?: string[];
  search?: string;
  type?: 'article' | 'guide' | 'theme';
}
```

---

### День 3: Детальная страница статьи

#### Задача 3.2: Детальная страница статьи (`/blog/[slug]/page.tsx`)

**Текущее состояние:**
- ❌ Использует моки (`articles[slug]`)
- ❌ Нет обработки ошибок
- ❌ Нет состояний загрузки

**Задачи:**
- [ ] Подключить `useGetArticleBySlug(slug)` вместо моков
- [ ] Добавить обработку ошибок (404 → notFound(), 500 → error page)
- [ ] Добавить состояния загрузки (Skeleton)
- [ ] Добавить связанные статьи через `useGetArticles({ category: article.category, limit: 5 })`
- [ ] Улучшить обработку пустых состояний
- [ ] Удалить моки (`articles` объект)

**Файлы для изменения:**
- `apps/go2asia-pwa-shell/app/(public)/blog/[slug]/page.tsx`

**SDK hooks:**
```typescript
import { useGetArticleBySlug, useGetArticles } from '@go2asia/sdk/blog';
```

**Обработка ошибок:**
```typescript
if (error?.status === 404) {
  notFound();
}
```

**Важно:** Страница должна быть Server Component для SEO, но может использовать Client Component для интерактивных частей.

---

### День 4-5: Страницы категорий и тем

#### Задача 3.3: Страница категории (`/blog/category/[id]/page.tsx`)

**Текущее состояние:**
- ❌ Не подключена к API

**Задачи:**
- [ ] Подключить `useGetArticles({ category: id })` для списка статей категории
- [ ] Добавить пагинацию
- [ ] Добавить информацию о категории (название, описание)
- [ ] Добавить состояния загрузки и ошибок
- [ ] Добавить обработку пустых состояний

**Файлы для изменения:**
- `apps/go2asia-pwa-shell/app/(public)/blog/category/[id]/page.tsx`

**SDK hooks:**
```typescript
import { useGetArticles } from '@go2asia/sdk/blog';
```

---

#### Задача 3.4: Страница темы (`/blog/theme/[id]/page.tsx`)

**Текущее состояние:**
- ❌ Не подключена к API

**Задачи:**
- [ ] Подключить `useGetArticles({ tags: [id] })` для списка статей по теме
- [ ] Добавить пагинацию
- [ ] Добавить информацию о теме (название, описание)
- [ ] Добавить состояния загрузки и ошибок
- [ ] Добавить обработку пустых состояний

**Файлы для изменения:**
- `apps/go2asia-pwa-shell/app/(public)/blog/theme/[id]/page.tsx`

**SDK hooks:**
```typescript
import { useGetArticles } from '@go2asia/sdk/blog';
```

---

### Итоги Недели 3

**Критерии завершения:**
- ✅ Все страницы Blog подключены к API
- ✅ Все моки удалены из Blog модуля
- ✅ Фильтрация работает через API
- ✅ Пагинация работает
- ✅ Обработка ошибок реализована
- ✅ Состояния загрузки добавлены

**Метрики:**
- Blog Asia UI: 65% → **100%**
- Blog Asia API интеграция: 10% → **100%**
- Blog Asia общий прогресс: 37% → **100%**

---

## 📖 Неделя 4: Финальная полировка

**Цель:** Обеспечить production-ready состояние: SEO, обработка ошибок, тестирование.

### День 1-2: SEO оптимизация

#### Задача 4.1: SEO metadata для Atlas Asia

**Задачи:**
- [ ] Добавить `metadata` для всех страниц Atlas:
  - Главная страница (`/atlas`)
  - Список стран (`/atlas/countries`)
  - Детальная страница страны (`/atlas/countries/[id]`)
  - Список городов (`/atlas/cities`)
  - Детальная страница города (`/atlas/cities/[id]`)
  - Список мест (`/atlas/places`)
  - Детальная страница места (`/atlas/places/[id]`)
  - Список гайдов (`/atlas/guides`)
  - Детальная страница гайда (`/atlas/guides/[id]`)
  - Список тем (`/atlas/themes`)
  - Детальная страница темы (`/atlas/themes/[id]`)
- [ ] Добавить Open Graph теги (`og:title`, `og:description`, `og:image`)
- [ ] Добавить Twitter Card теги
- [ ] Добавить структурированные данные (JSON-LD) для:
  - Страны (Place schema)
  - Города (City schema)
  - Места (TouristAttraction schema)
  - Статьи/Гайды (Article schema)

**Пример metadata:**
```typescript
export async function generateMetadata({ params }: Props): Promise<Metadata> {
  const country = await getCountryById(params.id);
  
  return {
    title: `${country.name} — Go2Asia Atlas`,
    description: country.description,
    openGraph: {
      title: country.name,
      description: country.description,
      images: [country.heroImage],
    },
  };
}
```

**Файлы для изменения:**
- Все `page.tsx` файлы в `apps/go2asia-pwa-shell/app/(public)/atlas/`

---

#### Задача 4.2: SEO metadata для Pulse Asia

**Задачи:**
- [ ] Добавить `metadata` для всех страниц Pulse:
  - Главная страница (`/pulse`)
  - Детальная страница события (`/pulse/[id]`)
- [ ] Добавить Open Graph теги
- [ ] Добавить Twitter Card теги
- [ ] Добавить структурированные данные (Event schema)

**Файлы для изменения:**
- `apps/go2asia-pwa-shell/app/(public)/pulse/page.tsx`
- `apps/go2asia-pwa-shell/app/(public)/pulse/[id]/page.tsx`

---

#### Задача 4.3: SEO metadata для Blog Asia

**Задачи:**
- [ ] Добавить `metadata` для всех страниц Blog:
  - Главная страница (`/blog`)
  - Детальная страница статьи (`/blog/[slug]`)
  - Страница категории (`/blog/category/[id]`)
  - Страница темы (`/blog/theme/[id]`)
- [ ] Добавить Open Graph теги
- [ ] Добавить Twitter Card теги
- [ ] Добавить структурированные данные (Article schema)
- [ ] Добавить RSS feed (`/blog/rss.xml`)

**Файлы для изменения:**
- Все `page.tsx` файлы в `apps/go2asia-pwa-shell/app/(public)/blog/`

---

#### Задача 4.4: Sitemap генерация

**Задачи:**
- [ ] Создать `app/sitemap.ts` для генерации sitemap
- [ ] Добавить все страницы Atlas:
  - `/atlas`
  - `/atlas/countries`
  - `/atlas/countries/[id]` (динамически)
  - `/atlas/cities`
  - `/atlas/cities/[id]` (динамически)
  - `/atlas/places`
  - `/atlas/places/[id]` (динамически)
  - `/atlas/guides`
  - `/atlas/guides/[id]` (динамически)
  - `/atlas/themes`
  - `/atlas/themes/[id]` (динамически)
- [ ] Добавить все страницы Pulse:
  - `/pulse`
  - `/pulse/[id]` (динамически)
- [ ] Добавить все страницы Blog:
  - `/blog`
  - `/blog/[slug]` (динамически)
  - `/blog/category/[id]` (динамически)
  - `/blog/theme/[id]` (динамически)
- [ ] Настроить приоритеты и частоту обновления

**Файлы для создания:**
- `apps/go2asia-pwa-shell/app/sitemap.ts`

**Пример:**
```typescript
export default async function sitemap() {
  const countries = await getCountries({ limit: 1000 });
  const cities = await getCities({ limit: 1000 });
  const places = await getPlaces({ limit: 1000 });
  const articles = await getArticles({ limit: 1000 });
  const events = await getEvents({ limit: 1000 });

  return [
    {
      url: 'https://go2asia.space/atlas',
      lastModified: new Date(),
      changeFrequency: 'daily',
      priority: 1,
    },
    ...countries.items.map((country) => ({
      url: `https://go2asia.space/atlas/countries/${country.id}`,
      lastModified: country.updatedAt,
      changeFrequency: 'weekly',
      priority: 0.8,
    })),
    // ... остальные страницы
  ];
}
```

---

### День 3: Обработка ошибок и состояний загрузки

#### Задача 4.5: Универсальная обработка ошибок

**Задачи:**
- [ ] Создать компонент `ErrorBoundary` для обработки ошибок React
- [ ] Создать компонент `ErrorPage` для отображения ошибок
- [ ] Добавить обработку 404 ошибок через `notFound()` во всех детальных страницах
- [ ] Добавить обработку 500 ошибок через `error.tsx` файлы
- [ ] Добавить обработку сетевых ошибок (timeout, connection error)
- [ ] Добавить информативные сообщения об ошибках
- [ ] Добавить кнопки "Попробовать снова" для retry логики

**Файлы для создания:**
- `apps/go2asia-pwa-shell/components/common/ErrorBoundary.tsx`
- `apps/go2asia-pwa-shell/components/common/ErrorPage.tsx`
- `apps/go2asia-pwa-shell/app/(public)/atlas/error.tsx`
- `apps/go2asia-pwa-shell/app/(public)/pulse/error.tsx`
- `apps/go2asia-pwa-shell/app/(public)/blog/error.tsx`

---

#### Задача 4.6: Состояния загрузки

**Задачи:**
- [ ] Создать универсальные Skeleton компоненты:
  - `SkeletonCard` — для карточек
  - `SkeletonList` — для списков
  - `SkeletonDetail` — для детальных страниц
- [ ] Добавить Skeleton компоненты на все страницы со списками
- [ ] Добавить индикаторы загрузки на все детальные страницы
- [ ] Оптимизировать UX во время загрузки (показывать частичный контент где возможно)

**Файлы для создания:**
- `apps/go2asia-pwa-shell/components/common/SkeletonCard.tsx`
- `apps/go2asia-pwa-shell/components/common/SkeletonList.tsx`
- `apps/go2asia-pwa-shell/components/common/SkeletonDetail.tsx`

---

### День 4: Тестирование и исправление багов

#### Задача 4.7: Функциональное тестирование

**Задачи:**
- [ ] Протестировать все страницы Atlas в браузере:
  - Главная страница загружается
  - Списки стран/городов/мест/гайдов работают
  - Детальные страницы открываются
  - Фильтры и поиск работают
  - Пагинация работает
- [ ] Протестировать все страницы Pulse в браузере:
  - Главная страница загружается
  - Календарные виды работают
  - Фильтры работают
  - Детальная страница события открывается
- [ ] Протестировать все страницы Blog в браузере:
  - Главная страница загружается
  - Детальная страница статьи открывается
  - Фильтрация по категориям работает
  - Пагинация работает
- [ ] Исправить найденные баги
- [ ] Проверить обработку ошибок (404, 500, сетевые ошибки)

---

#### Задача 4.8: Производительность

**Задачи:**
- [ ] Оптимизировать изображения (использовать `next/image` везде)
- [ ] Добавить кэширование для статических страниц через `revalidate`
- [ ] Оптимизировать bundle size (проверить через `next build --analyze`)
- [ ] Добавить lazy loading для компонентов где возможно
- [ ] Проверить производительность через Lighthouse

**Пример кэширования:**
```typescript
export const revalidate = 3600; // 1 час
```

---

### День 5: Документация и финальный ревью

#### Задача 4.9: Документация

**Задачи:**
- [ ] Обновить `docs/reports/PHASE1_STATUS_REPORT.md` с финальным статусом
- [ ] Создать документацию по использованию SDK hooks
- [ ] Создать документацию по обработке ошибок
- [ ] Создать документацию по SEO оптимизации
- [ ] Обновить README с инструкциями по запуску

**Файлы для обновления:**
- `docs/reports/PHASE1_STATUS_REPORT.md`
- `docs/frontend/SDK_USAGE.md` (создать)
- `docs/frontend/ERROR_HANDLING.md` (создать)
- `docs/frontend/SEO_GUIDE.md` (создать)

---

#### Задача 4.10: Финальный ревью

**Задачи:**
- [ ] Провести code review всех изменений
- [ ] Проверить соответствие код-стайлу проекта
- [ ] Проверить отсутствие console.log и debug кода
- [ ] Проверить отсутствие моков в коде
- [ ] Проверить наличие обработки ошибок везде
- [ ] Проверить наличие состояний загрузки везде
- [ ] Проверить SEO metadata на всех страницах
- [ ] Проверить sitemap генерацию

---

### Итоги Недели 4

**Критерии завершения:**
- ✅ SEO оптимизация выполнена для всех страниц
- ✅ Sitemap генерируется автоматически
- ✅ Обработка ошибок реализована везде
- ✅ Состояния загрузки добавлены везде
- ✅ Все страницы протестированы
- ✅ Нет критичных багов
- ✅ Документация обновлена

**Метрики:**
- SEO оптимизация: 0% → **100%**
- Обработка ошибок: 20% → **100%**
- Состояния загрузки: 30% → **100%**
- Тестирование: 0% → **100%**

---

## 📊 Итоговые метрики завершения Фазы 1

| Модуль | UI | API интеграция | SEO | Ошибки | Общий прогресс |
|--------|----|----------------|-----|--------|----------------|
| **Atlas Asia** | 100% | 100% | 100% | 100% | **100%** |
| **Pulse Asia** | 100% | 100% | 100% | 100% | **100%** |
| **Blog Asia** | 100% | 100% | 100% | 100% | **100%** |
| **ИТОГО** | **100%** | **100%** | **100%** | **100%** | **100%** |

---

## ✅ Критерии завершения Фазы 1

Фаза 1 считается завершённой, когда:

1. ✅ Все страницы Atlas, Pulse, Blog работают без моков
2. ✅ Все данные загружаются из Content Service API
3. ✅ Обработка ошибок реализована на всех страницах
4. ✅ Состояния загрузки реализованы на всех страницах
5. ✅ SEO оптимизация выполнена для всех страниц
6. ✅ Sitemap генерируется автоматически
7. ✅ Все страницы протестированы в браузере
8. ✅ Нет критичных багов
9. ✅ Документация обновлена
10. ✅ Code review пройден

---

## 🚨 Риски и митигация

### Риск 1: API не поддерживает нужные параметры фильтрации
**Митигация:** Проверить OpenAPI спецификацию перед началом работы, при необходимости расширить API

### Риск 2: Производительность при большом количестве данных
**Митигация:** Использовать пагинацию, кэширование, оптимизировать запросы

### Риск 3: Недостаточное тестовое покрытие
**Митигация:** Тестировать вручную каждую страницу, исправлять баги сразу

### Риск 4: Задержка из-за сложности интеграции
**Митигация:** Начать с простых страниц, постепенно усложнять

---

## 📝 Чек-лист прогресса

### Неделя 1: Atlas Asia
- [ ] Главная страница Atlas
- [ ] Список стран
- [ ] Детальная страница страны
- [ ] Список городов
- [ ] Детальная страница города
- [ ] Список мест
- [ ] Детальная страница места
- [ ] Список гайдов
- [ ] Детальная страница гайда
- [ ] Список тем
- [ ] Детальная страница темы

### Неделя 2: Pulse Asia
- [ ] Главная страница Pulse
- [ ] MonthView календарь
- [ ] WeekView календарь
- [ ] DayView календарь
- [ ] AgendaView календарь
- [ ] Детальная страница события
- [ ] Фильтры и поиск

### Неделя 3: Blog Asia
- [ ] Главная страница Blog
- [ ] Детальная страница статьи
- [ ] Страница категории
- [ ] Страница темы

### Неделя 4: Финальная полировка
- [ ] SEO metadata для Atlas
- [ ] SEO metadata для Pulse
- [ ] SEO metadata для Blog
- [ ] Sitemap генерация
- [ ] Обработка ошибок
- [ ] Состояния загрузки
- [ ] Тестирование
- [ ] Документация

---

**Дата создания:** 01 декабря 2025  
**Автор:** AI Assistant (Composer)  
**Версия:** 1.0

