# План до MVP - Конкретные задачи

## Дата: 2025-11-05

## ✅ Выполнено

1. ✅ **Создан enhanced-seed.sql** в `services/content-service/seeds/`
   - Дополнительные города с координатами
   - Дополнительные места с координатами и категориями
   - Нормализованные события с правильными типами и датами

2. ✅ **Добавлены счетчики в Content Service**
   - `GET /v1/countries` теперь возвращает `citiesCount` и `placesCount` через JOIN/COUNT
   - Используется `COALESCE(COUNT(DISTINCT ...), 0)` для корректной обработки NULL

3. ✅ **Добавлено кеширование в API Gateway**
   - Публичные GET запросы кешируются на 5 минут (`s-maxage=300`)
   - Используется `stale-while-revalidate=60` для улучшения UX
   - Кеширование применяется только к успешным ответам (200 OK)
   - ⚠️ **Примечание:** Кеширование временно отключено через Cloudflare Cache Rules для MVP фазы (см. `docs/planning/CACHE_STRATEGY.md`)

4. ✅ **Созданы UI компоненты для Token и Referral сервисов**
   - `ProfileBalanceWidget` — виджет баланса в шапке
   - `TransactionsPage` — страница истории транзакций (`/connect/transactions`)
   - `ReferralsPage` — страница рефералов с интеграцией API (`/connect/referrals`)
   - Обновлена навигация Connect модуля

5. ✅ **Созданы seed файлы для демо-данных**
   - `services/token-service/seeds/demo-seed.sql` — балансы и транзакции
   - `services/referral-service/seeds/demo-seed.sql` — рефералы и статистика

6. ✅ **Создана документация**
   - `docs/planning/DEMO_READINESS_CHECKLIST.md` — полный чек-лист для демо-готовности
   - Инструкции по применению seed данных

## 📋 Что нужно сделать дальше

### 1. Применить seed данные в Neon

См. подробные инструкции в `docs/planning/DEMO_READINESS_CHECKLIST.md`:

- **Content Service:** Применить `services/content-service/seeds/enhanced-seed.sql`
- **Token Service:** Применить `services/token-service/seeds/demo-seed.sql` (опционально, для демо)
- **Referral Service:** Применить `services/referral-service/seeds/demo-seed.sql` (опционально, для демо)

### 2. Включить Workers Logs в Cloudflare

См. инструкции в `docs/planning/DEMO_READINESS_CHECKLIST.md`, раздел 2.

### 3. Проверить UI компоненты после деплоя

- Проверить виджет баланса в шапке
- Проверить страницу `/connect/transactions`
- Проверить страницу `/connect/referrals`

### 4. Заполнить демо-данные (опционально)

См. `docs/planning/DEMO_READINESS_CHECKLIST.md`, раздел 4.

## 🎯 Критерии готовности MVP

- [x] Страницы Atlas и Pulse полностью на данных из БД (без моков)
- [x] Профиль показывает баланс поинтов; история транзакций открывается
- [x] Страница рефералов показывает stats/tree
- [x] Все публичные GET через API Gateway кешируются (проверка заголовков)
- [ ] Логи по requestId видны в Cloudflare, ошибки — редкие и понятные

## 📝 Следующие шаги

1. Применить `enhanced-seed.sql` в Neon (см. `DEMO_READINESS_CHECKLIST.md`)
2. Применить demo-seed.sql для Token и Referral сервисов (опционально, для демо)
3. Включить Workers Logs в Cloudflare Dashboard (см. `DEMO_READINESS_CHECKLIST.md`, раздел 2)
4. Проверить UI компоненты после деплоя
5. Заполнить демо-данные через API или SQL (см. `DEMO_READINESS_CHECKLIST.md`, раздел 4)


