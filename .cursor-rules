# ===========================
# Cursor Rules for Go2Asia
# ===========================

version: 1

# ---------------------------
# ⚠️ ВАЖНО: PowerShell и Git операции
# ---------------------------

## PowerShell (НЕ bash!)

**КРИТИЧЕСКИ ВАЖНО:** У пользователя Windows PowerShell, а не bash!

### Правила работы с командами:

1. **НЕ используй Unix-команды:**
   - ❌ `grep` → ✅ `Select-String`
   - ❌ `sed`, `awk`, `cat` → ✅ PowerShell эквиваленты
   - ❌ `| cat` → ✅ просто вывод или `| Out-String`

2. **Все команды должны быть PowerShell-совместимыми**

### Git операции:

**НЕ выполняй автоматически следующие команды:**
- ❌ `git push`
- ❌ `git pull`
- ❌ `git fetch`
- ❌ `git merge`
- ❌ `git checkout -b`

**Вместо этого:**
- ✅ ПИШИ пользователю список команд PowerShell для выполнения
- ✅ ЖДИ, пока пользователь сам их выполнит
- ✅ НЕ выполняй эти команды автоматически через `run_terminal_cmd`

### Проверка после операций с ветками:

**ОБЯЗАТЕЛЬНО** после любых операций с ветками добавляй команды проверки:

```powershell
git branch -a
git branch -r | Select-String "feature/"
```

**ВАЖНО:** Анализируй РЕАЛЬНЫЙ вывод команд, а не предполагаемое состояние!

### Пример правильного подхода:

```markdown
Выполните следующие команды PowerShell:

```powershell
git checkout main
git pull origin main
git checkout -b feature/space-asia
git add apps/go2asia-pwa-shell/components/space/
git commit -m "feat(space): базовая структура"
git push -u origin feature/space-asia
```

После выполнения проверьте:
```powershell
git branch -a
git branch -r | Select-String "feature/"
```
```

# ---------------------------
# CAPSULES CONFIG
# ---------------------------

capsules:
  # ---- MAIN APP SHELL ----
  - name: app-shell
    root: apps/go2asia-pwa-shell
    readOnly: false
    description: >
      Основной фронтенд Go2Asia. Разрешено редактирование,
      создание компонентов, страниц и интеграций.
    priority: high

  # ---- BACKEND SERVICES ----
  - name: services
    root: services
    readOnly: false
    description: >
      Все микросервисы Phase 0–1 (Auth, Token, Referral, Content).
      Cursor может обновлять логику, роуты, миграции и тесты.
    priority: high

  # ---- PROTOTYPES (BOLT.NEW) ----
  - name: prototypes
    root: prototypes
    readOnly: true
    ignoreGit: true
    description: >
      READ-ONLY. Прототипы Bolt.New.
      Cursor может читать эти файлы, анализировать их, брать UI/UX паттерны,
      Tailwind-классы, компоненты, структуры, но НЕ должен изменять файлы.
      Используется только как эталонный источник UI.
    priority: reference

  # ---- SHARED PACKAGES ----
  - name: packages
    root: packages
    readOnly: false
    description: >
      Общие пакеты: SDK, types, UI-компоненты и утилиты.
      Можно расширять, но аккуратно — важно не ломать публичный API.
    priority: medium

  # ---- DOCUMENTATION ----
  - name: docs
    root: docs
    readOnly: false
    description: >
      Документация проекта. Cursor может обновлять и расширять.
    priority: low


# ---------------------------
# GLOBAL RULES
# ---------------------------

rules:

  # ---- GIT IGNORED CONTENT ----
  - pattern: "/prototypes/**"
    action: allow-read
    description: >
      Прототипы Bolt.New можно читать, анализировать и использовать
      как визуальный и структурный reference.
      Изменять — нельзя.

  # ---- PROTECT PROTOTYPES ----
  - pattern: "/prototypes/**"
    action: prevent-write
    description: >
      Категорически запрещено изменять, переименовывать, удалять
      или создавать файлы в prototypes/.

  # ---- PROTECT PROTOTYPES FROM COMMITS ----
  - pattern: "/prototypes/**"
    action: prevent-commit
    description: >
      Файлы прототипов никогда не должны попасть в GitHub.

  # ---- FRONTEND APP SHELL RULES ----
  - pattern: "apps/go2asia-pwa-shell/**"
    action: allow-edit
    description: >
      Разрешено создавать/модифицировать компоненты, страницы,
      layout, хелперы, хуки и интеграции.

  # ---- BACKEND SERVICES RULES ----
  - pattern: "services/**"
    action: allow-edit
    description: >
      Разрешено менять код микросервисов, типы, роуты и тесты.

  # ---- SHARED PACKAGES RULES ----
  - pattern: "packages/**"
    action: allow-edit
    description: >
      Cursor может обновлять SDK, types и UI kit.

  # ---- DOCUMENTATION RULES ----
  - pattern: "docs/**"
    action: allow-edit
    description: >
      Документация может свободно редактироваться Cursor.


# ---------------------------
# SAFETY & SANITY RULES
# ---------------------------

sanity:

  # Не трогать node_modules
  - pattern: "**/node_modules/**"
    action: prevent-any

  # Не трогать билд артефакты
  - pattern: "**/dist/**"
    action: prevent-any

  # Не генерировать ненужные lock-файлы
  - pattern: "**/package-lock.json"
    action: prevent-write

  # Только pnpm
  - pattern: "*"
    enforce:
      - "использовать pnpm во всех командах"

# ---------------------------
# DESIGN SYSTEM RULES
# ---------------------------

## 🎨 Дизайн-система: Единственный источник правды

### ⚠️ КРИТИЧЕСКИ ВАЖНО: Два источника правды для компонентов

**packages/ui — ЕДИНСТВЕННЫЙ рабочий источник компонентов
•design-system/ — READ-ONLY документация и архив прототипов

### Правила работы с компонентами:

#### ✅ ОБЯЗАТЕЛЬНО:

1. **Все изменения компонентов ТОЛЬКО в `packages/ui/`**
   - Редактировать компоненты: `packages/ui/src/components/*`
   - Добавлять новые компоненты: `packages/ui/src/components/`
   - Обновлять Storybook: `packages/ui/src/components/*/*.stories.tsx`

2. **`design-system/` — ТОЛЬКО для чтения**
   - НЕ редактировать компоненты в `design-system/ui-components/*.tsx`
   - НЕ синхронизировать компоненты из `packages/ui` в `design-system/`
   - Использовать только для:
     - Чтения документации (`DESIGN_SYSTEM.md`, `DESIGN_TOKENS_GUIDE.md`)
     - Референса дизайн-токенов (`design-tokens.json`)
     - Референса конфигурации Tailwind (`tailwind.config.full.js`)

3. **Импорты компонентов ТОЛЬКО из `packages/ui`**
   ```tsx
   // ✅ Правильно
   import { Button, Card, ModuleTile } from '@go2asia/ui';
   
   // ❌ НЕПРАВИЛЬНО
   import { Button } from '../../design-system/ui-components/Button';
   ```

#### ❌ ЗАПРЕЩЕНО:

1. Редактировать файлы в `design-system/ui-components/`
2. Создавать локальные копии компонентов в `apps/go2asia-pwa-shell/components/`
3. Импортировать компоненты из `design-system/`
4. Синхронизировать компоненты между `packages/ui` и `design-system/`

### Структура дизайн-системы:

```
packages/ui/                    # ← РАБОЧИЙ источник правды
├── src/components/            # ← Редактировать здесь
├── .storybook/                # ← Storybook здесь
└── README.md

design-system/                 # ← READ-ONLY документация
├── DESIGN_SYSTEM.md           # ← Читать документацию
├── DESIGN_TOKENS_GUIDE.md     # ← Референс токенов
├── design-tokens.json         # ← Референс токенов
├── tailwind.config.full.js    # ← Референс конфигурации
└── ui-components/             # ← НЕ РЕДАКТИРОВАТЬ (архив)
```

### Процесс добавления/изменения компонента:

1. **Редактировать** компонент в `packages/ui/src/components/ComponentName/`
2. **Обновить Storybook** в `packages/ui/src/components/ComponentName/ComponentName.stories.tsx`
3. **Обновить документацию** в `packages/ui/README.md` (если нужно)
4. **НЕ трогать** `design-system/ui-components/`

---

## 📁 Структура монорепозитория

### Основные директории:

•apps/ — Frontend приложения (только go2asia-pwa-shell)
•services/ — Backend микросервисы (Cloudflare Workers)
•packages/ — Общие пакеты (ui, sdk, types, config, logger)
•docs/ — Документация проекта
•design-system/ — READ-ONLY документация дизайн-системы
•scripts/ — Вспомогательные скрипты
•tests/ — E2E тесты

### Правила работы:

1. **Монорепо верхнего уровня**: apps/, services/, packages/, docs/ — базовая структура
2. **PWA Shell**: apps/go2asia-pwa-shell/ — единственное frontend приложение
3. **Микросервисы**: services/* — Cloudflare Workers
4. **UI-kit**: packages/ui — единственный источник правды для компонентов
5. **OpenAPI-first**: docs/openapi → packages/types + packages/sdk

---

## 🔧 TypeScript и типы

### Генерация типов:

•Типы генерируются из OpenAPI: `pnpm gen:types`
•SDK генерируется из OpenAPI: `pnpm gen:sdk`
•НЕ редактировать вручную: `packages/types/src/index.ts`, `packages/sdk/src/*.ts`

### Использование типов:

```tsx
// ✅ Правильно
import { User } from '@go2asia/types';
import { useGetUser } from '@go2asia/sdk';

// ❌ НЕПРАВИЛЬНО
import { User } from './types/user'; // Локальные типы
```

---

## 🎨 Типографика (зафиксирована согласно прототипу Bolt.New)

### Размеры заголовков:

•H1: `text-h1 md:text-4xl lg:text-5xl` (30px / 36px / 48px) + `mb-3 md:mb-4`
•H2: `text-h2 md:text-3xl` (24px / 30px)
•H3: `text-h3 md:text-2xl` (20px / 24px)

### Grid модулей:

•Классы: `grid grid-cols-2 md:grid-cols-3 gap-3 md:gap-4`
•Mobile: 2 колонки (9 модулей = 5 рядов)
•Tablet/Desktop: 3 колонки (9 модулей = 3 ряда)

---

## 📚 Документация

### Источники правды:

•Style Guide: `docs/overview/go2asia_uiux_style_guide.md`
•Компоненты: `packages/ui/` + Storybook
•Дизайн-система: `design-system/DESIGN_SYSTEM.md` (read-only)
•Структура проекта: `docs/PROJECT_STRUCTURE.md`

---

## ✅ Чек-лист перед коммитом

- [ ] Все компоненты импортируются из `@go2asia/ui`
- [ ] НЕ редактировал файлы в `design-system/ui-components/`
- [ ] Обновил Storybook для изменённых компонентов
- [ ] Типографика соответствует прототипу Bolt.New
- [ ] Grid модулей: `grid-cols-2 md:grid-cols-3`
- [ ] Проверил линтинг: `pnpm lint`
- [ ] Проверил типы: `pnpm typecheck`

# ---------------------------
# DESIGN SYSTEM CAPSULE
# ---------------------------

capsule: design-system
readonly: true
ignore_in_git: true
description: |
  This capsule contains external design-token files generated by Bolt.New.
  Cursor must NEVER modify or rewrite these files.
  Cursor may only READ them as reference for UI implementation.

When building or modifying UI components, always copy exact styles, typography, spacing,
colors and structure from the design-system capsule. Never rely on default Tailwind.
Never improvise. Always match pixel-perfect Bolt.New design.
